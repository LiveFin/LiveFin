diff --git a/Beta/GuideView.swift b/Beta/GuideView.swift
deleted file mode 100644
index 625d6e5..0000000
--- a/Beta/GuideView.swift
+++ /dev/null
@@ -1,169 +0,0 @@
-//
-//  GuideView.swift
-//  LiveFin
-//
-//  Created by KPGamingz on 5/13/25.
-//
-
-import SwiftUI
-
-struct GuideView: View {
-    @EnvironmentObject var appState: AppState
-    @State private var channels: [LiveTvChannelDto] = []
-    @State private var programs: [BaseItemDto] = []
-    @State private var error: String?
-    @State private var isLoading = true
-    @State private var cacheURL: URL = {
-        let urls = FileManager.default.urls(for: .cachesDirectory, in: .userDomainMask)
-        return urls[0].appendingPathComponent("cached_programs.json")
-    }()
-
-    var body: some View {
-        NavigationStack {
-            VStack {
-                if isLoading {
-                    ProgressView("Loading guide...")
-                } else if let error = error {
-                    Text("Error: \(error)")
-                        .foregroundColor(.red)
-                } else {
-                    ScrollView {
-                        LazyVStack(alignment: .leading, spacing: 16) {
-                            ForEach(channels, id: \.id) { channel in
-                                VStack(alignment: .leading, spacing: 4) {
-                                    Text(channel.name ?? "Unnamed Channel")
-                                        .font(.headline)
-                                    ScrollView(.horizontal, showsIndicators: false) {
-                                        HStack(spacing: 8) {
-                                            ForEach(programs.filter { $0.channelId == channel.id }, id: \.id) { program in
-                                                VStack(alignment: .leading) {
-                                                    Text(program.name ?? "Untitled")
-                                                        .font(.subheadline)
-                                                    if let start = program.startDate, let end = program.endDate {
-                                                        Text("\(start.formatted(date: .omitted, time: .shortened)) - \(end.formatted(date: .omitted, time: .shortened))")
-                                                            .font(.caption)
-                                                            .foregroundColor(.gray)
-                                                    }
-                                                }
-                                                .padding(8)
-                                                .background(Color.gray.opacity(0.1))
-                                                .cornerRadius(6)
-                                            }
-                                        }
-                                    }
-                                }
-                            }
-                        }
-                        .padding()
-                    }
-                }
-            }
-            .navigationTitle("TV Guide")
-            .task {
-                await loadGuideData()
-            }
-        }
-    }
-
-    func loadCachedPrograms() {
-        do {
-            let data = try Data(contentsOf: cacheURL)
-            let decoder = JSONDecoder()
-            decoder.dateDecodingStrategy = .iso8601
-            let response = try decoder.decode(ProgramsResponse.self, from: data)
-            self.programs = response.items ?? []
-            print("Loaded programs from cache")
-        } catch {
-            print("No cached programs found or failed to decode: \(error)")
-        }
-    }
-
-    func saveProgramsToCache(_ programs: [BaseItemDto]) {
-        do {
-            let encoder = JSONEncoder()
-            encoder.dateEncodingStrategy = .iso8601
-            let response = ProgramsResponse(items: programs)
-            let data = try encoder.encode(response)
-            try data.write(to: cacheURL)
-            print("Programs cached to disk")
-        } catch {
-            print("Failed to cache programs: \(error)")
-        }
-    }
-
-    func loadGuideData() async {
-        loadCachedPrograms()
-        isLoading = true
-        error = nil
-
-        guard let client = appState.client else {
-            error = "Client not initialized"
-            isLoading = false
-            return
-        }
-
-        let now = Date()
-        let later = Calendar.current.date(byAdding: .hour, value: 4, to: now)!
-
-        do {
-            // Fetch channels
-            let channelURL = client.configuration.url.appendingPathComponent("/LiveTv/Channels")
-            var channelRequest = URLRequest(url: channelURL)
-            channelRequest.setValue("application/json", forHTTPHeaderField: "Content-Type")
-            channelRequest.setValue(appState.accessToken, forHTTPHeaderField: "X-Emby-Token")
-            let (channelData, _) = try await URLSession.shared.data(for: channelRequest)
-            let channelResponse = try JSONDecoder().decode(ChannelsResponse.self, from: channelData)
-            self.channels = channelResponse.items ?? []
-
-            // Fetch programs
-            let programURL = client.configuration.url.appendingPathComponent("/LiveTv/Programs")
-            var components = URLComponents(url: programURL, resolvingAgainstBaseURL: false)
-            components?.queryItems = [
-                URLQueryItem(name: "startDate", value: ISO8601DateFormatter().string(from: now)),
-                URLQueryItem(name: "endDate", value: ISO8601DateFormatter().string(from: later)),
-                URLQueryItem(name: "EnableImages", value: "false"),
-                URLQueryItem(name: "EnableUserData", value: "false")
-            ]
-
-            guard let finalURL = components?.url else {
-                error = "Invalid programs URL"
-                isLoading = false
-                return
-            }
-
-            var programRequest = URLRequest(url: finalURL)
-            programRequest.setValue("application/json", forHTTPHeaderField: "Content-Type")
-            programRequest.setValue(appState.accessToken, forHTTPHeaderField: "X-Emby-Token")
-            let (programData, _) = try await URLSession.shared.data(for: programRequest)
-
-            let decoder = JSONDecoder()
-            let isoFormatter = ISO8601DateFormatter()
-            isoFormatter.formatOptions = [.withInternetDateTime, .withFractionalSeconds]
-            decoder.dateDecodingStrategy = .custom { decoder in
-                let container = try decoder.singleValueContainer()
-                let dateStr = try container.decode(String.self)
-                guard let date = isoFormatter.date(from: dateStr) else {
-                    throw DecodingError.dataCorruptedError(in: container, debugDescription: "Cannot decode date: \(dateStr)")
-                }
-                return date
-            }
-
-            let programResponse = try decoder.decode(ProgramsResponse.self, from: programData)
-            self.programs = programResponse.items ?? []
-            saveProgramsToCache(self.programs)
-        } catch {
-            self.error = error.localizedDescription
-        }
-
-        isLoading = false
-    }
-}
-
-// Response model for Channels
-struct ChannelsResponse: Codable {
-    let items: [LiveTvChannelDto]?
-
-    enum CodingKeys: String, CodingKey {
-        case items = "Items"
-    }
-}
diff --git a/Beta/LiveFin tvOS/AppDelegate.swift b/Beta/LiveFin tvOS/AppDelegate.swift
deleted file mode 100644
index 573a1fe..0000000
--- a/Beta/LiveFin tvOS/AppDelegate.swift	
+++ /dev/null
@@ -1,40 +0,0 @@
-//
-//  AppDelegate.swift
-//  LiveFin tvOS
-//
-//  Created by Kervens on 10/14/25.
-//
-
-import UIKit
-
-@main
-class AppDelegate: UIResponder, UIApplicationDelegate {
-
-    var window: UIWindow?
-
-
-    func application(_ application: UIApplication, didFinishLaunchingWithOptions launchOptions: [UIApplication.LaunchOptionsKey: Any]?) -> Bool {
-        // Override point for customization after application launch.
-        return true
-    }
-
-    func applicationWillResignActive(_ application: UIApplication) {
-        // Sent when the application is about to move from active to inactive state. This can occur for certain types of temporary interruptions (such as an incoming phone call or SMS message) or when the user quits the application and it begins the transition to the background state.
-        // Use this method to pause ongoing tasks, disable timers, and throttle down OpenGL ES frame rates. Games should use this method to pause the game.
-    }
-
-    func applicationDidEnterBackground(_ application: UIApplication) {
-        // Use this method to release shared resources, save user data, invalidate timers, and store enough application state information to restore your application to its current state in case it is terminated later.
-    }
-
-    func applicationWillEnterForeground(_ application: UIApplication) {
-        // Called as part of the transition from the background to the active state; here you can undo many of the changes made on entering the background.
-    }
-
-    func applicationDidBecomeActive(_ application: UIApplication) {
-        // Restart any tasks that were paused (or not yet started) while the application was inactive. If the application was previously in the background, optionally refresh the user interface.
-    }
-
-
-}
-
diff --git a/Beta/LiveFin tvOS/Assets.xcassets/AccentColor.colorset/Contents.json b/Beta/LiveFin tvOS/Assets.xcassets/AccentColor.colorset/Contents.json
deleted file mode 100644
index eb87897..0000000
--- a/Beta/LiveFin tvOS/Assets.xcassets/AccentColor.colorset/Contents.json	
+++ /dev/null
@@ -1,11 +0,0 @@
-{
-  "colors" : [
-    {
-      "idiom" : "universal"
-    }
-  ],
-  "info" : {
-    "author" : "xcode",
-    "version" : 1
-  }
-}
diff --git a/Beta/LiveFin tvOS/Assets.xcassets/App Icon & Top Shelf Image.brandassets/App Icon - App Store.imagestack/Back.imagestacklayer/Content.imageset/Contents.json b/Beta/LiveFin tvOS/Assets.xcassets/App Icon & Top Shelf Image.brandassets/App Icon - App Store.imagestack/Back.imagestacklayer/Content.imageset/Contents.json
deleted file mode 100644
index 2e00335..0000000
--- a/Beta/LiveFin tvOS/Assets.xcassets/App Icon & Top Shelf Image.brandassets/App Icon - App Store.imagestack/Back.imagestacklayer/Content.imageset/Contents.json	
+++ /dev/null
@@ -1,11 +0,0 @@
-{
-  "images" : [
-    {
-      "idiom" : "tv"
-    }
-  ],
-  "info" : {
-    "author" : "xcode",
-    "version" : 1
-  }
-}
diff --git a/Beta/LiveFin tvOS/Assets.xcassets/App Icon & Top Shelf Image.brandassets/App Icon - App Store.imagestack/Back.imagestacklayer/Contents.json b/Beta/LiveFin tvOS/Assets.xcassets/App Icon & Top Shelf Image.brandassets/App Icon - App Store.imagestack/Back.imagestacklayer/Contents.json
deleted file mode 100644
index 73c0059..0000000
--- a/Beta/LiveFin tvOS/Assets.xcassets/App Icon & Top Shelf Image.brandassets/App Icon - App Store.imagestack/Back.imagestacklayer/Contents.json	
+++ /dev/null
@@ -1,6 +0,0 @@
-{
-  "info" : {
-    "author" : "xcode",
-    "version" : 1
-  }
-}
diff --git a/Beta/LiveFin tvOS/Assets.xcassets/App Icon & Top Shelf Image.brandassets/App Icon - App Store.imagestack/Contents.json b/Beta/LiveFin tvOS/Assets.xcassets/App Icon & Top Shelf Image.brandassets/App Icon - App Store.imagestack/Contents.json
deleted file mode 100644
index de59d88..0000000
--- a/Beta/LiveFin tvOS/Assets.xcassets/App Icon & Top Shelf Image.brandassets/App Icon - App Store.imagestack/Contents.json	
+++ /dev/null
@@ -1,17 +0,0 @@
-{
-  "info" : {
-    "author" : "xcode",
-    "version" : 1
-  },
-  "layers" : [
-    {
-      "filename" : "Front.imagestacklayer"
-    },
-    {
-      "filename" : "Middle.imagestacklayer"
-    },
-    {
-      "filename" : "Back.imagestacklayer"
-    }
-  ]
-}
diff --git a/Beta/LiveFin tvOS/Assets.xcassets/App Icon & Top Shelf Image.brandassets/App Icon - App Store.imagestack/Front.imagestacklayer/Content.imageset/Contents.json b/Beta/LiveFin tvOS/Assets.xcassets/App Icon & Top Shelf Image.brandassets/App Icon - App Store.imagestack/Front.imagestacklayer/Content.imageset/Contents.json
deleted file mode 100644
index 2e00335..0000000
--- a/Beta/LiveFin tvOS/Assets.xcassets/App Icon & Top Shelf Image.brandassets/App Icon - App Store.imagestack/Front.imagestacklayer/Content.imageset/Contents.json	
+++ /dev/null
@@ -1,11 +0,0 @@
-{
-  "images" : [
-    {
-      "idiom" : "tv"
-    }
-  ],
-  "info" : {
-    "author" : "xcode",
-    "version" : 1
-  }
-}
diff --git a/Beta/LiveFin tvOS/Assets.xcassets/App Icon & Top Shelf Image.brandassets/App Icon - App Store.imagestack/Front.imagestacklayer/Contents.json b/Beta/LiveFin tvOS/Assets.xcassets/App Icon & Top Shelf Image.brandassets/App Icon - App Store.imagestack/Front.imagestacklayer/Contents.json
deleted file mode 100644
index 73c0059..0000000
--- a/Beta/LiveFin tvOS/Assets.xcassets/App Icon & Top Shelf Image.brandassets/App Icon - App Store.imagestack/Front.imagestacklayer/Contents.json	
+++ /dev/null
@@ -1,6 +0,0 @@
-{
-  "info" : {
-    "author" : "xcode",
-    "version" : 1
-  }
-}
diff --git a/Beta/LiveFin tvOS/Assets.xcassets/App Icon & Top Shelf Image.brandassets/App Icon - App Store.imagestack/Middle.imagestacklayer/Content.imageset/Contents.json b/Beta/LiveFin tvOS/Assets.xcassets/App Icon & Top Shelf Image.brandassets/App Icon - App Store.imagestack/Middle.imagestacklayer/Content.imageset/Contents.json
deleted file mode 100644
index 2e00335..0000000
--- a/Beta/LiveFin tvOS/Assets.xcassets/App Icon & Top Shelf Image.brandassets/App Icon - App Store.imagestack/Middle.imagestacklayer/Content.imageset/Contents.json	
+++ /dev/null
@@ -1,11 +0,0 @@
-{
-  "images" : [
-    {
-      "idiom" : "tv"
-    }
-  ],
-  "info" : {
-    "author" : "xcode",
-    "version" : 1
-  }
-}
diff --git a/Beta/LiveFin tvOS/Assets.xcassets/App Icon & Top Shelf Image.brandassets/App Icon - App Store.imagestack/Middle.imagestacklayer/Contents.json b/Beta/LiveFin tvOS/Assets.xcassets/App Icon & Top Shelf Image.brandassets/App Icon - App Store.imagestack/Middle.imagestacklayer/Contents.json
deleted file mode 100644
index 73c0059..0000000
--- a/Beta/LiveFin tvOS/Assets.xcassets/App Icon & Top Shelf Image.brandassets/App Icon - App Store.imagestack/Middle.imagestacklayer/Contents.json	
+++ /dev/null
@@ -1,6 +0,0 @@
-{
-  "info" : {
-    "author" : "xcode",
-    "version" : 1
-  }
-}
diff --git a/Beta/LiveFin tvOS/Assets.xcassets/App Icon & Top Shelf Image.brandassets/App Icon.imagestack/Back.imagestacklayer/Content.imageset/Contents.json b/Beta/LiveFin tvOS/Assets.xcassets/App Icon & Top Shelf Image.brandassets/App Icon.imagestack/Back.imagestacklayer/Content.imageset/Contents.json
deleted file mode 100644
index 795cce1..0000000
--- a/Beta/LiveFin tvOS/Assets.xcassets/App Icon & Top Shelf Image.brandassets/App Icon.imagestack/Back.imagestacklayer/Content.imageset/Contents.json	
+++ /dev/null
@@ -1,16 +0,0 @@
-{
-  "images" : [
-    {
-      "idiom" : "tv",
-      "scale" : "1x"
-    },
-    {
-      "idiom" : "tv",
-      "scale" : "2x"
-    }
-  ],
-  "info" : {
-    "author" : "xcode",
-    "version" : 1
-  }
-}
diff --git a/Beta/LiveFin tvOS/Assets.xcassets/App Icon & Top Shelf Image.brandassets/App Icon.imagestack/Back.imagestacklayer/Contents.json b/Beta/LiveFin tvOS/Assets.xcassets/App Icon & Top Shelf Image.brandassets/App Icon.imagestack/Back.imagestacklayer/Contents.json
deleted file mode 100644
index 73c0059..0000000
--- a/Beta/LiveFin tvOS/Assets.xcassets/App Icon & Top Shelf Image.brandassets/App Icon.imagestack/Back.imagestacklayer/Contents.json	
+++ /dev/null
@@ -1,6 +0,0 @@
-{
-  "info" : {
-    "author" : "xcode",
-    "version" : 1
-  }
-}
diff --git a/Beta/LiveFin tvOS/Assets.xcassets/App Icon & Top Shelf Image.brandassets/App Icon.imagestack/Contents.json b/Beta/LiveFin tvOS/Assets.xcassets/App Icon & Top Shelf Image.brandassets/App Icon.imagestack/Contents.json
deleted file mode 100644
index de59d88..0000000
--- a/Beta/LiveFin tvOS/Assets.xcassets/App Icon & Top Shelf Image.brandassets/App Icon.imagestack/Contents.json	
+++ /dev/null
@@ -1,17 +0,0 @@
-{
-  "info" : {
-    "author" : "xcode",
-    "version" : 1
-  },
-  "layers" : [
-    {
-      "filename" : "Front.imagestacklayer"
-    },
-    {
-      "filename" : "Middle.imagestacklayer"
-    },
-    {
-      "filename" : "Back.imagestacklayer"
-    }
-  ]
-}
diff --git a/Beta/LiveFin tvOS/Assets.xcassets/App Icon & Top Shelf Image.brandassets/App Icon.imagestack/Front.imagestacklayer/Content.imageset/Contents.json b/Beta/LiveFin tvOS/Assets.xcassets/App Icon & Top Shelf Image.brandassets/App Icon.imagestack/Front.imagestacklayer/Content.imageset/Contents.json
deleted file mode 100644
index 795cce1..0000000
--- a/Beta/LiveFin tvOS/Assets.xcassets/App Icon & Top Shelf Image.brandassets/App Icon.imagestack/Front.imagestacklayer/Content.imageset/Contents.json	
+++ /dev/null
@@ -1,16 +0,0 @@
-{
-  "images" : [
-    {
-      "idiom" : "tv",
-      "scale" : "1x"
-    },
-    {
-      "idiom" : "tv",
-      "scale" : "2x"
-    }
-  ],
-  "info" : {
-    "author" : "xcode",
-    "version" : 1
-  }
-}
diff --git a/Beta/LiveFin tvOS/Assets.xcassets/App Icon & Top Shelf Image.brandassets/App Icon.imagestack/Front.imagestacklayer/Contents.json b/Beta/LiveFin tvOS/Assets.xcassets/App Icon & Top Shelf Image.brandassets/App Icon.imagestack/Front.imagestacklayer/Contents.json
deleted file mode 100644
index 73c0059..0000000
--- a/Beta/LiveFin tvOS/Assets.xcassets/App Icon & Top Shelf Image.brandassets/App Icon.imagestack/Front.imagestacklayer/Contents.json	
+++ /dev/null
@@ -1,6 +0,0 @@
-{
-  "info" : {
-    "author" : "xcode",
-    "version" : 1
-  }
-}
diff --git a/Beta/LiveFin tvOS/Assets.xcassets/App Icon & Top Shelf Image.brandassets/App Icon.imagestack/Middle.imagestacklayer/Content.imageset/Contents.json b/Beta/LiveFin tvOS/Assets.xcassets/App Icon & Top Shelf Image.brandassets/App Icon.imagestack/Middle.imagestacklayer/Content.imageset/Contents.json
deleted file mode 100644
index 795cce1..0000000
--- a/Beta/LiveFin tvOS/Assets.xcassets/App Icon & Top Shelf Image.brandassets/App Icon.imagestack/Middle.imagestacklayer/Content.imageset/Contents.json	
+++ /dev/null
@@ -1,16 +0,0 @@
-{
-  "images" : [
-    {
-      "idiom" : "tv",
-      "scale" : "1x"
-    },
-    {
-      "idiom" : "tv",
-      "scale" : "2x"
-    }
-  ],
-  "info" : {
-    "author" : "xcode",
-    "version" : 1
-  }
-}
diff --git a/Beta/LiveFin tvOS/Assets.xcassets/App Icon & Top Shelf Image.brandassets/App Icon.imagestack/Middle.imagestacklayer/Contents.json b/Beta/LiveFin tvOS/Assets.xcassets/App Icon & Top Shelf Image.brandassets/App Icon.imagestack/Middle.imagestacklayer/Contents.json
deleted file mode 100644
index 73c0059..0000000
--- a/Beta/LiveFin tvOS/Assets.xcassets/App Icon & Top Shelf Image.brandassets/App Icon.imagestack/Middle.imagestacklayer/Contents.json	
+++ /dev/null
@@ -1,6 +0,0 @@
-{
-  "info" : {
-    "author" : "xcode",
-    "version" : 1
-  }
-}
diff --git a/Beta/LiveFin tvOS/Assets.xcassets/App Icon & Top Shelf Image.brandassets/Contents.json b/Beta/LiveFin tvOS/Assets.xcassets/App Icon & Top Shelf Image.brandassets/Contents.json
deleted file mode 100644
index f47ba43..0000000
--- a/Beta/LiveFin tvOS/Assets.xcassets/App Icon & Top Shelf Image.brandassets/Contents.json	
+++ /dev/null
@@ -1,32 +0,0 @@
-{
-  "assets" : [
-    {
-      "filename" : "App Icon - App Store.imagestack",
-      "idiom" : "tv",
-      "role" : "primary-app-icon",
-      "size" : "1280x768"
-    },
-    {
-      "filename" : "App Icon.imagestack",
-      "idiom" : "tv",
-      "role" : "primary-app-icon",
-      "size" : "400x240"
-    },
-    {
-      "filename" : "Top Shelf Image Wide.imageset",
-      "idiom" : "tv",
-      "role" : "top-shelf-image-wide",
-      "size" : "2320x720"
-    },
-    {
-      "filename" : "Top Shelf Image.imageset",
-      "idiom" : "tv",
-      "role" : "top-shelf-image",
-      "size" : "1920x720"
-    }
-  ],
-  "info" : {
-    "author" : "xcode",
-    "version" : 1
-  }
-}
diff --git a/Beta/LiveFin tvOS/Assets.xcassets/App Icon & Top Shelf Image.brandassets/Top Shelf Image Wide.imageset/Contents.json b/Beta/LiveFin tvOS/Assets.xcassets/App Icon & Top Shelf Image.brandassets/Top Shelf Image Wide.imageset/Contents.json
deleted file mode 100644
index 795cce1..0000000
--- a/Beta/LiveFin tvOS/Assets.xcassets/App Icon & Top Shelf Image.brandassets/Top Shelf Image Wide.imageset/Contents.json	
+++ /dev/null
@@ -1,16 +0,0 @@
-{
-  "images" : [
-    {
-      "idiom" : "tv",
-      "scale" : "1x"
-    },
-    {
-      "idiom" : "tv",
-      "scale" : "2x"
-    }
-  ],
-  "info" : {
-    "author" : "xcode",
-    "version" : 1
-  }
-}
diff --git a/Beta/LiveFin tvOS/Assets.xcassets/App Icon & Top Shelf Image.brandassets/Top Shelf Image.imageset/Contents.json b/Beta/LiveFin tvOS/Assets.xcassets/App Icon & Top Shelf Image.brandassets/Top Shelf Image.imageset/Contents.json
deleted file mode 100644
index 795cce1..0000000
--- a/Beta/LiveFin tvOS/Assets.xcassets/App Icon & Top Shelf Image.brandassets/Top Shelf Image.imageset/Contents.json	
+++ /dev/null
@@ -1,16 +0,0 @@
-{
-  "images" : [
-    {
-      "idiom" : "tv",
-      "scale" : "1x"
-    },
-    {
-      "idiom" : "tv",
-      "scale" : "2x"
-    }
-  ],
-  "info" : {
-    "author" : "xcode",
-    "version" : 1
-  }
-}
diff --git a/Beta/LiveFin tvOS/Assets.xcassets/Contents.json b/Beta/LiveFin tvOS/Assets.xcassets/Contents.json
deleted file mode 100644
index 73c0059..0000000
--- a/Beta/LiveFin tvOS/Assets.xcassets/Contents.json	
+++ /dev/null
@@ -1,6 +0,0 @@
-{
-  "info" : {
-    "author" : "xcode",
-    "version" : 1
-  }
-}
diff --git a/Beta/LiveFin tvOS/Base.lproj/LaunchScreen.storyboard b/Beta/LiveFin tvOS/Base.lproj/LaunchScreen.storyboard
deleted file mode 100644
index 660ba53..0000000
--- a/Beta/LiveFin tvOS/Base.lproj/LaunchScreen.storyboard	
+++ /dev/null
@@ -1,24 +0,0 @@
-<?xml version="1.0" encoding="UTF-8" standalone="no"?>
-<document type="com.apple.InterfaceBuilder.AppleTV.Storyboard" version="3.0" toolsVersion="13122.16" targetRuntime="AppleTV" propertyAccessControl="none" useAutolayout="YES" launchScreen="YES" useTraitCollections="YES" useSafeAreas="YES" colorMatched="YES" initialViewController="BYZ-38-t0r">
-    <dependencies>
-        <plugIn identifier="com.apple.InterfaceBuilder.IBCocoaTouchPlugin" version="13104.12"/>
-        <capability name="Safe area layout guides" minToolsVersion="9.0"/>
-        <capability name="documents saved in the Xcode 8 format" minToolsVersion="8.0"/>
-    </dependencies>
-    <scenes>
-        <!--View Controller-->
-        <scene sceneID="tne-QT-ifu">
-            <objects>
-                <viewController id="BYZ-38-t0r" sceneMemberID="viewController">
-                    <view key="view" contentMode="scaleToFill" id="8bC-Xf-vdC">
-                        <rect key="frame" x="0.0" y="0.0" width="1920" height="1080"/>
-                        <autoresizingMask key="autoresizingMask" widthSizable="YES" heightSizable="YES"/>
-                        <color key="backgroundColor" red="0.0" green="0.0" blue="0.0" alpha="0.0" colorSpace="custom" customColorSpace="sRGB"/>
-                        <viewLayoutGuide key="safeArea" id="wu6-TO-1qx"/>
-                    </view>
-                </viewController>
-                <placeholder placeholderIdentifier="IBFirstResponder" id="dkx-z0-nzr" sceneMemberID="firstResponder"/>
-            </objects>
-        </scene>
-    </scenes>
-</document>
diff --git a/Beta/LiveFin tvOS/Base.lproj/Main.storyboard b/Beta/LiveFin tvOS/Base.lproj/Main.storyboard
deleted file mode 100644
index c277013..0000000
--- a/Beta/LiveFin tvOS/Base.lproj/Main.storyboard	
+++ /dev/null
@@ -1,28 +0,0 @@
-<?xml version="1.0" encoding="UTF-8"?>
-<document type="com.apple.InterfaceBuilder.AppleTV.Storyboard" version="3.0" toolsVersion="13122.16" targetRuntime="AppleTV" propertyAccessControl="none" useAutolayout="YES" useTraitCollections="YES" useSafeAreas="YES" colorMatched="YES" initialViewController="BYZ-38-t0r">
-    <dependencies>
-        <plugIn identifier="com.apple.InterfaceBuilder.IBCocoaTouchPlugin" version="13104.12"/>
-        <capability name="Safe area layout guides" minToolsVersion="9.0"/>
-        <capability name="documents saved in the Xcode 8 format" minToolsVersion="8.0"/>
-    </dependencies>
-    <scenes>
-        <!--View Controller-->
-        <scene sceneID="tne-QT-ifu">
-            <objects>
-                <viewController id="BYZ-38-t0r" customClass="ViewController" customModuleProvider="target" sceneMemberID="viewController">
-                    <layoutGuides>
-                        <viewControllerLayoutGuide type="top" id="y3c-jy-aDJ"/>
-                        <viewControllerLayoutGuide type="bottom" id="wfy-db-euE"/>
-                    </layoutGuides>
-                    <view key="view" contentMode="scaleToFill" id="8bC-Xf-vdC">
-                        <rect key="frame" x="0.0" y="0.0" width="1920" height="1080"/>
-                        <autoresizingMask key="autoresizingMask" widthSizable="YES" heightSizable="YES"/>
-                        <color key="backgroundColor" red="0.0" green="0.0" blue="0.0" alpha="0.0" colorSpace="custom" customColorSpace="sRGB"/>
-                        <viewLayoutGuide key="safeArea" id="wu6-TO-1qx"/>
-                    </view>
-                </viewController>
-                <placeholder placeholderIdentifier="IBFirstResponder" id="dkx-z0-nzr" sceneMemberID="firstResponder"/>
-            </objects>
-        </scene>
-    </scenes>
-</document>
diff --git a/Beta/LiveFin tvOS/ViewController.swift b/Beta/LiveFin tvOS/ViewController.swift
deleted file mode 100644
index 2f8a0fb..0000000
--- a/Beta/LiveFin tvOS/ViewController.swift	
+++ /dev/null
@@ -1,19 +0,0 @@
-//
-//  ViewController.swift
-//  LiveFin tvOS
-//
-//  Created by Kervens on 10/14/25.
-//
-
-import UIKit
-
-class ViewController: UIViewController {
-
-    override func viewDidLoad() {
-        super.viewDidLoad()
-        // Do any additional setup after loading the view.
-    }
-
-
-}
-
diff --git a/LiveFin Logo.icon/Assets/New Project.png b/LiveFin Logo.icon/Assets/New Project.png
deleted file mode 100644
index 666cd38..0000000
Binary files a/LiveFin Logo.icon/Assets/New Project.png and /dev/null differ
diff --git a/LiveFin Logo.icon/Assets/image-from-rawpixel-id-6480690-png.png b/LiveFin Logo.icon/Assets/image-from-rawpixel-id-6480690-png.png
deleted file mode 100644
index d78f2ad..0000000
Binary files a/LiveFin Logo.icon/Assets/image-from-rawpixel-id-6480690-png.png and /dev/null differ
diff --git a/LiveFin Logo.icon/icon.json b/LiveFin Logo.icon/icon.json
deleted file mode 100644
index c67ab5d..0000000
--- a/LiveFin Logo.icon/icon.json	
+++ /dev/null
@@ -1,121 +0,0 @@
-{
-  "fill-specializations" : [
-    {
-      "value" : {
-        "linear-gradient" : [
-          "display-p3:0.00000,1.00000,0.78702,1.00000",
-          "display-p3:0.00000,0.58508,0.96783,1.00000"
-        ]
-      }
-    },
-    {
-      "appearance" : "dark",
-      "value" : {
-        "linear-gradient" : [
-          "display-p3:0.00047,0.29389,0.23969,1.00000",
-          "display-p3:0.00000,0.12785,0.21149,1.00000"
-        ]
-      }
-    }
-  ],
-  "groups" : [
-    {
-      "blend-mode-specializations" : [
-        {
-          "appearance" : "dark",
-          "value" : "normal"
-        },
-        {
-          "appearance" : "tinted",
-          "value" : "normal"
-        }
-      ],
-      "blur-material" : null,
-      "layers" : [
-        {
-          "glass" : true,
-          "hidden" : false,
-          "image-name" : "New Project.png",
-          "name" : "New Project",
-          "position" : {
-            "scale" : 1,
-            "translation-in-points" : [
-              -53.25,
-              -29.828125
-            ]
-          }
-        }
-      ],
-      "position" : {
-        "scale" : 1,
-        "translation-in-points" : [
-          20.875,
-          64.1171875
-        ]
-      },
-      "shadow" : {
-        "kind" : "neutral",
-        "opacity" : 0.7
-      },
-      "specular" : true,
-      "translucency" : {
-        "enabled" : false,
-        "value" : 0.5
-      }
-    },
-    {
-      "blend-mode-specializations" : [
-        {
-          "value" : "normal"
-        },
-        {
-          "appearance" : "dark",
-          "value" : "hard-light"
-        }
-      ],
-      "blur-material" : null,
-      "layers" : [
-        {
-          "blend-mode-specializations" : [
-            {
-              "appearance" : "dark",
-              "value" : "overlay"
-            }
-          ],
-          "glass" : false,
-          "hidden" : false,
-          "image-name" : "image-from-rawpixel-id-6480690-png.png",
-          "name" : "image-from-rawpixel-id-6480690-png",
-          "opacity-specializations" : [
-            {
-              "appearance" : "dark",
-              "value" : 1
-            }
-          ],
-          "position" : {
-            "scale" : 2,
-            "translation-in-points" : [
-              -7.03125,
-              273.5859375
-            ]
-          }
-        }
-      ],
-      "shadow" : {
-        "kind" : "neutral",
-        "opacity" : 0.5
-      },
-      "specular" : false,
-      "translucency" : {
-        "enabled" : false,
-        "value" : 0.5
-      }
-    }
-  ],
-  "supported-platforms" : {
-    "circles" : [
-      "watchOS"
-    ],
-    "squares" : "shared"
-  }
-}
\ No newline at end of file
diff --git a/LiveFin tvOS Watch App/Assets.xcassets/AccentColor.colorset/Contents.json b/LiveFin tvOS Watch App/Assets.xcassets/AccentColor.colorset/Contents.json
deleted file mode 100644
index eb87897..0000000
--- a/LiveFin tvOS Watch App/Assets.xcassets/AccentColor.colorset/Contents.json	
+++ /dev/null
@@ -1,11 +0,0 @@
-{
-  "colors" : [
-    {
-      "idiom" : "universal"
-    }
-  ],
-  "info" : {
-    "author" : "xcode",
-    "version" : 1
-  }
-}
diff --git a/LiveFin tvOS Watch App/Assets.xcassets/AppIcon.appiconset/Contents.json b/LiveFin tvOS Watch App/Assets.xcassets/AppIcon.appiconset/Contents.json
deleted file mode 100644
index 49c81cd..0000000
--- a/LiveFin tvOS Watch App/Assets.xcassets/AppIcon.appiconset/Contents.json	
+++ /dev/null
@@ -1,13 +0,0 @@
-{
-  "images" : [
-    {
-      "idiom" : "universal",
-      "platform" : "watchos",
-      "size" : "1024x1024"
-    }
-  ],
-  "info" : {
-    "author" : "xcode",
-    "version" : 1
-  }
-}
diff --git a/LiveFin tvOS Watch App/Assets.xcassets/Contents.json b/LiveFin tvOS Watch App/Assets.xcassets/Contents.json
deleted file mode 100644
index 73c0059..0000000
--- a/LiveFin tvOS Watch App/Assets.xcassets/Contents.json	
+++ /dev/null
@@ -1,6 +0,0 @@
-{
-  "info" : {
-    "author" : "xcode",
-    "version" : 1
-  }
-}
diff --git a/LiveFin tvOS Watch App/ContentView.swift b/LiveFin tvOS Watch App/ContentView.swift
deleted file mode 100644
index c2bf3af..0000000
--- a/LiveFin tvOS Watch App/ContentView.swift	
+++ /dev/null
@@ -1,24 +0,0 @@
-//
-//  ContentView.swift
-//  LiveFin tvOS Watch App
-//
-//  Created by Kervens on 9/26/25.
-//
-
-import SwiftUI
-
-struct ContentView: View {
-    var body: some View {
-        VStack {
-            Image(systemName: "globe")
-                .imageScale(.large)
-                .foregroundStyle(.tint)
-            Text("Hello, world!")
-        }
-        .padding()
-    }
-}
-
-#Preview {
-    ContentView()
-}
diff --git a/LiveFin tvOS Watch App/LiveFin_tvOSApp.swift b/LiveFin tvOS Watch App/LiveFin_tvOSApp.swift
deleted file mode 100644
index bf20594..0000000
--- a/LiveFin tvOS Watch App/LiveFin_tvOSApp.swift	
+++ /dev/null
@@ -1,17 +0,0 @@
-//
-//  LiveFin_tvOSApp.swift
-//  LiveFin tvOS Watch App
-//
-//  Created by Kervens on 9/26/25.
-//
-
-import SwiftUI
-
-@main
-struct LiveFin_tvOS_Watch_AppApp: App {
-    var body: some Scene {
-        WindowGroup {
-            ContentView()
-        }
-    }
-}
diff --git a/LiveFin watchOS/ContentView.swift b/LiveFin watchOS/ContentView.swift
index 0ce1aca..207076b 100644
--- a/LiveFin watchOS/ContentView.swift	
+++ b/LiveFin watchOS/ContentView.swift	
@@ -7,18 +7,65 @@
 
 import SwiftUI
 
-struct ContentView: View {
+struct WatchRootView: View { // Renamed from ContentView
+    @EnvironmentObject var appState: WatchAppState
+
     var body: some View {
-        VStack {
-            Image(systemName: "globe")
-                .imageScale(.large)
-                .foregroundStyle(.tint)
-            Text("Hello, world!")
+        Group {
+            if !appState.isAuthenticated {
+                ScrollView { // Added ScrollView for scrollability on small screens
+                    VStack(spacing: 12) {
+                        Image(systemName: "lock.circle")
+                            .font(.system(size: 42))
+                            .foregroundColor(.accentColor)
+                        Text("Login on iPhone")
+                            .font(.headline)
+                        Text("The watch app will pick up your session automatically.")
+                            .font(.footnote)
+                            .multilineTextAlignment(.center)
+                            .padding(.horizontal)
+                        Button("Retry") {
+                            appState.restoreCredentials()
+                            Task { await appState.loadChannelsIfNeeded(force: true) }
+                        }
+                        .buttonStyle(.borderedProminent)
+                    }
+                    .padding()
+                    .frame(maxWidth: .infinity)
+                }
+            } else {
+                NavigationStack {
+                    List {
+                        if appState.isLoadingChannels {
+                            ProgressView("Loading…")
+                        } else if appState.channels.isEmpty {
+                            Text("No channels")
+                                .foregroundColor(.secondary)
+                        } else {
+                            ForEach(appState.channels) { ch in
+                                NavigationLink(destination: WatchChannelDetailView(channel: ch).environmentObject(appState)) {
+                                    WatchChannelRow(channel: ch, baseURL: appState.serverURL, apiKey: appState.apiKey)
+                                }
+                            }
+                        }
+                    }
+                    .navigationTitle("Channels")
+                    .toolbar { ToolbarItem(placement: .topBarTrailing) { refreshButton } }
+                    .task { await appState.loadChannelsIfNeeded(force: false) }
+                    .refreshable { await appState.loadChannelsIfNeeded(force: true) }
+                }
+            }
         }
-        .padding()
+        .onAppear { appState.restoreCredentials() }
+    }
+
+    private var refreshButton: some View {
+        Button(action: { Task { await appState.loadChannelsIfNeeded(force: true) } }) { Image(systemName: "arrow.clockwise") }
+            .disabled(appState.isLoadingChannels)
     }
 }
 
 #Preview {
-    ContentView()
+    WatchRootView()
+        .environmentObject(WatchAppState())
 }
diff --git a/LiveFin watchOS/LiveFin_watchOSApp.swift b/LiveFin watchOS/LiveFin_watchOSApp.swift
index c267177..288874b 100644
--- a/LiveFin watchOS/LiveFin_watchOSApp.swift	
+++ b/LiveFin watchOS/LiveFin_watchOSApp.swift	
@@ -9,9 +9,16 @@ import SwiftUI
 
 @main
 struct LiveFin_watchOS_Watch_AppApp: App {
+    @StateObject private var appState = WatchAppState()
+
     var body: some Scene {
         WindowGroup {
-            ContentView()
+            WatchRootView() // renamed from ContentView
+                .environmentObject(appState)
+                .onAppear {
+                    appState.restoreCredentials()
+                    appState.startConnectivity()
+                }
         }
     }
 }
diff --git a/LiveFin.xcodeproj/project.pbxproj b/LiveFin.xcodeproj/project.pbxproj
index 0045795..da3e648 100644
--- a/LiveFin.xcodeproj/project.pbxproj
+++ b/LiveFin.xcodeproj/project.pbxproj
@@ -7,6 +7,7 @@
 	objects = {
 
 /* Begin PBXBuildFile section */
+		EB2FC8732E8B7FB900915765 /* LiveFin watchOS.app in Embed Watch Content */ = {isa = PBXBuildFile; fileRef = EB2FC8692E8B7FB700915765 /* LiveFin watchOS.app */; settings = {ATTRIBUTES = (RemoveHeadersOnCopy, ); }; };
 		EBCC8A802DA88B95003DF1A6 /* JellyfinAPI in Frameworks */ = {isa = PBXBuildFile; productRef = EBCC8A7F2DA88B95003DF1A6 /* JellyfinAPI */; };
 		EBCC8A862DA96727003DF1A6 /* NukeUI in Frameworks */ = {isa = PBXBuildFile; productRef = EBCC8A852DA96727003DF1A6 /* NukeUI */; };
 		EBCC8A882DA967E2003DF1A6 /* Nuke in Frameworks */ = {isa = PBXBuildFile; productRef = EBCC8A872DA967E2003DF1A6 /* Nuke */; };
@@ -15,6 +16,13 @@
 /* End PBXBuildFile section */
 
 /* Begin PBXContainerItemProxy section */
+		EB2FC8712E8B7FB900915765 /* PBXContainerItemProxy */ = {
+			isa = PBXContainerItemProxy;
+			containerPortal = EBCC8A042DA88899003DF1A6 /* Project object */;
+			proxyType = 1;
+			remoteGlobalIDString = EB2FC8682E8B7FB700915765;
+			remoteInfo = "LiveFin watchOS Watch App";
+		};
 		EBCC8A1A2DA8889B003DF1A6 /* PBXContainerItemProxy */ = {
 			isa = PBXContainerItemProxy;
 			containerPortal = EBCC8A042DA88899003DF1A6 /* Project object */;
@@ -31,7 +39,22 @@
 		};
 /* End PBXContainerItemProxy section */
 
+/* Begin PBXCopyFilesBuildPhase section */
+		EBDDC7142E875F5B00E66039 /* Embed Watch Content */ = {
+			isa = PBXCopyFilesBuildPhase;
+			buildActionMask = 2147483647;
+			dstPath = "$(CONTENTS_FOLDER_PATH)/Watch";
+			dstSubfolderSpec = 16;
+			files = (
+				EB2FC8732E8B7FB900915765 /* LiveFin watchOS.app in Embed Watch Content */,
+			);
+			name = "Embed Watch Content";
+			runOnlyForDeploymentPostprocessing = 0;
+		};
+/* End PBXCopyFilesBuildPhase section */
+
 /* Begin PBXFileReference section */
+		EB2FC8692E8B7FB700915765 /* LiveFin watchOS.app */ = {isa = PBXFileReference; explicitFileType = wrapper.application; includeInIndex = 0; path = "LiveFin watchOS.app"; sourceTree = BUILT_PRODUCTS_DIR; };
 		EBCC8A0C2DA88899003DF1A6 /* LiveFin.app */ = {isa = PBXFileReference; explicitFileType = wrapper.application; includeInIndex = 0; path = LiveFin.app; sourceTree = BUILT_PRODUCTS_DIR; };
 		EBCC8A192DA8889B003DF1A6 /* LiveFinTests.xctest */ = {isa = PBXFileReference; explicitFileType = wrapper.cfbundle; includeInIndex = 0; path = LiveFinTests.xctest; sourceTree = BUILT_PRODUCTS_DIR; };
 		EBCC8A232DA8889B003DF1A6 /* LiveFinUITests.xctest */ = {isa = PBXFileReference; explicitFileType = wrapper.cfbundle; includeInIndex = 0; path = LiveFinUITests.xctest; sourceTree = BUILT_PRODUCTS_DIR; };
@@ -41,7 +64,6 @@
 		EBAF6C812DCB05B10054C539 /* Exceptions for "Beta" folder in "LiveFinTests" target */ = {
 			isa = PBXFileSystemSynchronizedBuildFileExceptionSet;
 			membershipExceptions = (
-				"Launch Screen.storyboard",
 				SplashScreenView.swift,
 			);
 			target = EBCC8A182DA8889B003DF1A6 /* LiveFinTests */;
@@ -53,9 +75,21 @@
 			);
 			target = EBCC8A0B2DA88899003DF1A6 /* LiveFin */;
 		};
+		EBDB53412EB6DFD1001BCF33 /* Exceptions for "LiveFin" folder in "LiveFin watchOS" target */ = {
+			isa = PBXFileSystemSynchronizedBuildFileExceptionSet;
+			membershipExceptions = (
+				LiveFin.icon,
+			);
+			target = EB2FC8682E8B7FB700915765 /* LiveFin watchOS */;
+		};
 /* End PBXFileSystemSynchronizedBuildFileExceptionSet section */
 
 /* Begin PBXFileSystemSynchronizedRootGroup section */
+		EB9F7D352E878F1200597D25 /* LiveFin watchOS */ = {
+			isa = PBXFileSystemSynchronizedRootGroup;
+			path = "LiveFin watchOS";
+			sourceTree = "<group>";
+		};
 		EBAF6C7C2DCB058A0054C539 /* Beta */ = {
 			isa = PBXFileSystemSynchronizedRootGroup;
 			exceptions = (
@@ -68,6 +102,7 @@
 			isa = PBXFileSystemSynchronizedRootGroup;
 			exceptions = (
 				EBCC8A812DA88C5E003DF1A6 /* Exceptions for "LiveFin" folder in "LiveFin" target */,
+				EBDB53412EB6DFD1001BCF33 /* Exceptions for "LiveFin" folder in "LiveFin watchOS" target */,
 			);
 			path = LiveFin;
 			sourceTree = "<group>";
@@ -75,6 +110,13 @@
 /* End PBXFileSystemSynchronizedRootGroup section */
 
 /* Begin PBXFrameworksBuildPhase section */
+		EB2FC8662E8B7FB700915765 /* Frameworks */ = {
+			isa = PBXFrameworksBuildPhase;
+			buildActionMask = 2147483647;
+			files = (
+			);
+			runOnlyForDeploymentPostprocessing = 0;
+		};
 		EBCC8A092DA88899003DF1A6 /* Frameworks */ = {
 			isa = PBXFrameworksBuildPhase;
 			buildActionMask = 2147483647;
@@ -108,6 +150,7 @@
 			isa = PBXGroup;
 			children = (
 				EBCC8A0E2DA88899003DF1A6 /* LiveFin */,
+				EB9F7D352E878F1200597D25 /* LiveFin watchOS */,
 				EBAF6C7C2DCB058A0054C539 /* Beta */,
 				EBCC8A0D2DA88899003DF1A6 /* Products */,
 			);
@@ -119,6 +162,7 @@
 				EBCC8A0C2DA88899003DF1A6 /* LiveFin.app */,
 				EBCC8A192DA8889B003DF1A6 /* LiveFinTests.xctest */,
 				EBCC8A232DA8889B003DF1A6 /* LiveFinUITests.xctest */,
+				EB2FC8692E8B7FB700915765 /* LiveFin watchOS.app */,
 			);
 			name = Products;
 			sourceTree = "<group>";
@@ -126,6 +170,28 @@
 /* End PBXGroup section */
 
 /* Begin PBXNativeTarget section */
+		EB2FC8682E8B7FB700915765 /* LiveFin watchOS */ = {
+			isa = PBXNativeTarget;
+			buildConfigurationList = EB2FC8742E8B7FB900915765 /* Build configuration list for PBXNativeTarget "LiveFin watchOS" */;
+			buildPhases = (
+				EB2FC8652E8B7FB700915765 /* Sources */,
+				EB2FC8662E8B7FB700915765 /* Frameworks */,
+				EB2FC8672E8B7FB700915765 /* Resources */,
+			);
+			buildRules = (
+			);
+			dependencies = (
+			);
+			fileSystemSynchronizedGroups = (
+				EB9F7D352E878F1200597D25 /* LiveFin watchOS */,
+			);
+			name = "LiveFin watchOS";
+			packageProductDependencies = (
+			);
+			productName = "LiveFin watchOS Watch App";
+			productReference = EB2FC8692E8B7FB700915765 /* LiveFin watchOS.app */;
+			productType = "com.apple.product-type.application";
+		};
 		EBCC8A0B2DA88899003DF1A6 /* LiveFin */ = {
 			isa = PBXNativeTarget;
 			buildConfigurationList = EBCC8A2D2DA8889B003DF1A6 /* Build configuration list for PBXNativeTarget "LiveFin" */;
@@ -133,10 +199,12 @@
 				EBCC8A082DA88899003DF1A6 /* Sources */,
 				EBCC8A092DA88899003DF1A6 /* Frameworks */,
 				EBCC8A0A2DA88899003DF1A6 /* Resources */,
+				EBDDC7142E875F5B00E66039 /* Embed Watch Content */,
 			);
 			buildRules = (
 			);
 			dependencies = (
+				EB2FC8722E8B7FB900915765 /* PBXTargetDependency */,
 			);
 			fileSystemSynchronizedGroups = (
 				EBAF6C7C2DCB058A0054C539 /* Beta */,
@@ -201,9 +269,12 @@
 			isa = PBXProject;
 			attributes = {
 				BuildIndependentTargetsInParallel = 1;
-				LastSwiftUpdateCheck = 1630;
+				LastSwiftUpdateCheck = 2600;
 				LastUpgradeCheck = 1630;
 				TargetAttributes = {
+					EB2FC8682E8B7FB700915765 = {
+						CreatedOnToolsVersion = 26.0.1;
+					};
 					EBCC8A0B2DA88899003DF1A6 = {
 						CreatedOnToolsVersion = 16.3;
 					};
@@ -237,6 +308,7 @@
 			projectRoot = "";
 			targets = (
 				EBCC8A0B2DA88899003DF1A6 /* LiveFin */,
+				EB2FC8682E8B7FB700915765 /* LiveFin watchOS */,
 				EBCC8A182DA8889B003DF1A6 /* LiveFinTests */,
 				EBCC8A222DA8889B003DF1A6 /* LiveFinUITests */,
 			);
@@ -244,6 +316,13 @@
 /* End PBXProject section */
 
 /* Begin PBXResourcesBuildPhase section */
+		EB2FC8672E8B7FB700915765 /* Resources */ = {
+			isa = PBXResourcesBuildPhase;
+			buildActionMask = 2147483647;
+			files = (
+			);
+			runOnlyForDeploymentPostprocessing = 0;
+		};
 		EBCC8A0A2DA88899003DF1A6 /* Resources */ = {
 			isa = PBXResourcesBuildPhase;
 			buildActionMask = 2147483647;
@@ -268,6 +347,13 @@
 /* End PBXResourcesBuildPhase section */
 
 /* Begin PBXSourcesBuildPhase section */
+		EB2FC8652E8B7FB700915765 /* Sources */ = {
+			isa = PBXSourcesBuildPhase;
+			buildActionMask = 2147483647;
+			files = (
+			);
+			runOnlyForDeploymentPostprocessing = 0;
+		};
 		EBCC8A082DA88899003DF1A6 /* Sources */ = {
 			isa = PBXSourcesBuildPhase;
 			buildActionMask = 2147483647;
@@ -292,6 +378,11 @@
 /* End PBXSourcesBuildPhase section */
 
 /* Begin PBXTargetDependency section */
+		EB2FC8722E8B7FB900915765 /* PBXTargetDependency */ = {
+			isa = PBXTargetDependency;
+			target = EB2FC8682E8B7FB700915765 /* LiveFin watchOS */;
+			targetProxy = EB2FC8712E8B7FB900915765 /* PBXContainerItemProxy */;
+		};
 		EBCC8A1B2DA8889B003DF1A6 /* PBXTargetDependency */ = {
 			isa = PBXTargetDependency;
 			target = EBCC8A0B2DA88899003DF1A6 /* LiveFin */;
@@ -305,11 +396,82 @@
 /* End PBXTargetDependency section */
 
 /* Begin XCBuildConfiguration section */
+		EB2FC8752E8B7FB900915765 /* Debug */ = {
+			isa = XCBuildConfiguration;
+			buildSettings = {
+				ASSETCATALOG_COMPILER_APPICON_NAME = LiveFin;
+				ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME = AccentColor;
+				ASSETCATALOG_COMPILER_INCLUDE_ALL_APPICON_ASSETS = NO;
+				CODE_SIGN_STYLE = Automatic;
+				CURRENT_PROJECT_VERSION = 1;
+				DEVELOPMENT_TEAM = KFQZK38LL3;
+				ENABLE_PREVIEWS = YES;
+				GENERATE_INFOPLIST_FILE = YES;
+				INFOPLIST_KEY_CFBundleDisplayName = LiveFin;
+				INFOPLIST_KEY_UISupportedInterfaceOrientations = "UIInterfaceOrientationPortrait UIInterfaceOrientationPortraitUpsideDown";
+				INFOPLIST_KEY_WKCompanionAppBundleIdentifier = com.kpgamingz.LiveFin;
+				LD_RUNPATH_SEARCH_PATHS = (
+					"$(inherited)",
+					"@executable_path/Frameworks",
+				);
+				MARKETING_VERSION = 1.0;
+				PRODUCT_BUNDLE_IDENTIFIER = com.kpgamingz.LiveFin.watchkitapp;
+				PRODUCT_NAME = "$(TARGET_NAME)";
+				SDKROOT = watchos;
+				SKIP_INSTALL = YES;
+				STRING_CATALOG_GENERATE_SYMBOLS = YES;
+				SWIFT_APPROACHABLE_CONCURRENCY = YES;
+				SWIFT_DEFAULT_ACTOR_ISOLATION = MainActor;
+				SWIFT_EMIT_LOC_STRINGS = YES;
+				SWIFT_UPCOMING_FEATURE_MEMBER_IMPORT_VISIBILITY = YES;
+				SWIFT_VERSION = 5.0;
+				TARGETED_DEVICE_FAMILY = 4;
+				WATCHOS_DEPLOYMENT_TARGET = 26.0;
+			};
+			name = Debug;
+		};
+		EB2FC8762E8B7FB900915765 /* Release */ = {
+			isa = XCBuildConfiguration;
+			buildSettings = {
+				ASSETCATALOG_COMPILER_APPICON_NAME = LiveFin;
+				ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME = AccentColor;
+				ASSETCATALOG_COMPILER_INCLUDE_ALL_APPICON_ASSETS = NO;
+				CODE_SIGN_STYLE = Automatic;
+				CURRENT_PROJECT_VERSION = 1;
+				DEVELOPMENT_TEAM = KFQZK38LL3;
+				ENABLE_PREVIEWS = YES;
+				GENERATE_INFOPLIST_FILE = YES;
+				INFOPLIST_KEY_CFBundleDisplayName = LiveFin;
+				INFOPLIST_KEY_UISupportedInterfaceOrientations = "UIInterfaceOrientationPortrait UIInterfaceOrientationPortraitUpsideDown";
+				INFOPLIST_KEY_WKCompanionAppBundleIdentifier = com.kpgamingz.LiveFin;
+				LD_RUNPATH_SEARCH_PATHS = (
+					"$(inherited)",
+					"@executable_path/Frameworks",
+				);
+				MARKETING_VERSION = 1.0;
+				PRODUCT_BUNDLE_IDENTIFIER = com.kpgamingz.LiveFin.watchkitapp;
+				PRODUCT_NAME = "$(TARGET_NAME)";
+				SDKROOT = watchos;
+				SKIP_INSTALL = YES;
+				STRING_CATALOG_GENERATE_SYMBOLS = YES;
+				SWIFT_APPROACHABLE_CONCURRENCY = YES;
+				SWIFT_DEFAULT_ACTOR_ISOLATION = MainActor;
+				SWIFT_EMIT_LOC_STRINGS = YES;
+				SWIFT_UPCOMING_FEATURE_MEMBER_IMPORT_VISIBILITY = YES;
+				SWIFT_VERSION = 5.0;
+				TARGETED_DEVICE_FAMILY = 4;
+				WATCHOS_DEPLOYMENT_TARGET = 26.0;
+			};
+			name = Release;
+		};
 		EBCC8A2B2DA8889B003DF1A6 /* Debug */ = {
 			isa = XCBuildConfiguration;
 			buildSettings = {
 				ALWAYS_SEARCH_USER_PATHS = NO;
+				ASSETCATALOG_COMPILER_ALTERNATE_APPICON_NAMES = "";
+				ASSETCATALOG_COMPILER_APPICON_NAME = "";
 				ASSETCATALOG_COMPILER_GENERATE_SWIFT_ASSET_SYMBOL_EXTENSIONS = YES;
+				ASSETCATALOG_COMPILER_INCLUDE_ALL_APPICON_ASSETS = YES;
 				CLANG_ANALYZER_NONNULL = YES;
 				CLANG_ANALYZER_NUMBER_OBJECT_CONVERSION = YES_AGGRESSIVE;
 				CLANG_CXX_LANGUAGE_STANDARD = "gnu++20";
@@ -372,7 +534,10 @@
 			isa = XCBuildConfiguration;
 			buildSettings = {
 				ALWAYS_SEARCH_USER_PATHS = NO;
+				ASSETCATALOG_COMPILER_ALTERNATE_APPICON_NAMES = "";
+				ASSETCATALOG_COMPILER_APPICON_NAME = "";
 				ASSETCATALOG_COMPILER_GENERATE_SWIFT_ASSET_SYMBOL_EXTENSIONS = YES;
+				ASSETCATALOG_COMPILER_INCLUDE_ALL_APPICON_ASSETS = YES;
 				CLANG_ANALYZER_NONNULL = YES;
 				CLANG_ANALYZER_NUMBER_OBJECT_CONVERSION = YES_AGGRESSIVE;
 				CLANG_CXX_LANGUAGE_STANDARD = "gnu++20";
@@ -427,29 +592,32 @@
 		EBCC8A2E2DA8889B003DF1A6 /* Debug */ = {
 			isa = XCBuildConfiguration;
 			buildSettings = {
-				ASSETCATALOG_COMPILER_APPICON_NAME = "LiveFin Logo";
+				ASSETCATALOG_COMPILER_APPICON_NAME = LiveFin;
 				ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME = AccentColor;
-				ASSETCATALOG_COMPILER_INCLUDE_ALL_APPICON_ASSETS = YES;
+				ASSETCATALOG_COMPILER_INCLUDE_ALL_APPICON_ASSETS = NO;
+				ASSETCATALOG_COMPILER_STANDALONE_ICON_BEHAVIOR = default;
 				CODE_SIGN_ENTITLEMENTS = LiveFin/LiveFin.entitlements;
 				CODE_SIGN_STYLE = Automatic;
-				CURRENT_PROJECT_VERSION = 4;
+				CURRENT_PROJECT_VERSION = 1;
 				DEVELOPMENT_TEAM = KFQZK38LL3;
 				ENABLE_PREVIEWS = YES;
 				GENERATE_INFOPLIST_FILE = YES;
 				INFOPLIST_FILE = LiveFin/Info.plist;
 				INFOPLIST_KEY_CFBundleDisplayName = LiveFin;
+				INFOPLIST_KEY_ITSAppUsesNonExemptEncryption = NO;
 				INFOPLIST_KEY_LSApplicationCategoryType = "public.app-category.entertainment";
-				INFOPLIST_KEY_NSLocalNetworkUsageDescription = "This app requires access to the local network to connect to your Jellyfin server.";
+				INFOPLIST_KEY_NSLocalNetworkUsageDescription = "This app needs access to devices on your local network to discover and connect to media servers.";
 				INFOPLIST_KEY_UIApplicationSupportsIndirectInputEvents = YES;
 				INFOPLIST_KEY_UILaunchScreen_Generation = YES;
+				INFOPLIST_KEY_UISupportedInterfaceOrientations = "UIInterfaceOrientationPortrait UIInterfaceOrientationLandscapeLeft UIInterfaceOrientationLandscapeRight";
 				INFOPLIST_KEY_UISupportedInterfaceOrientations_iPad = "UIInterfaceOrientationPortrait UIInterfaceOrientationPortraitUpsideDown UIInterfaceOrientationLandscapeLeft UIInterfaceOrientationLandscapeRight";
 				INFOPLIST_KEY_UISupportedInterfaceOrientations_iPhone = "UIInterfaceOrientationPortrait UIInterfaceOrientationLandscapeLeft UIInterfaceOrientationLandscapeRight";
-				IPHONEOS_DEPLOYMENT_TARGET = 16.0;
+				IPHONEOS_DEPLOYMENT_TARGET = 18.0;
 				LD_RUNPATH_SEARCH_PATHS = (
 					"$(inherited)",
 					"@executable_path/Frameworks",
 				);
-				MARKETING_VERSION = 1.3;
+				MARKETING_VERSION = 2.1.1;
 				PRODUCT_BUNDLE_IDENTIFIER = com.kpgamingz.LiveFin;
 				PRODUCT_NAME = "$(TARGET_NAME)";
 				SWIFT_EMIT_LOC_STRINGS = YES;
@@ -461,29 +629,32 @@
 		EBCC8A2F2DA8889B003DF1A6 /* Release */ = {
 			isa = XCBuildConfiguration;
 			buildSettings = {
-				ASSETCATALOG_COMPILER_APPICON_NAME = "LiveFin Logo";
+				ASSETCATALOG_COMPILER_APPICON_NAME = LiveFin;
 				ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME = AccentColor;
-				ASSETCATALOG_COMPILER_INCLUDE_ALL_APPICON_ASSETS = YES;
+				ASSETCATALOG_COMPILER_INCLUDE_ALL_APPICON_ASSETS = NO;
+				ASSETCATALOG_COMPILER_STANDALONE_ICON_BEHAVIOR = default;
 				CODE_SIGN_ENTITLEMENTS = LiveFin/LiveFin.entitlements;
 				CODE_SIGN_STYLE = Automatic;
-				CURRENT_PROJECT_VERSION = 4;
+				CURRENT_PROJECT_VERSION = 1;
 				DEVELOPMENT_TEAM = KFQZK38LL3;
 				ENABLE_PREVIEWS = YES;
 				GENERATE_INFOPLIST_FILE = YES;
 				INFOPLIST_FILE = LiveFin/Info.plist;
 				INFOPLIST_KEY_CFBundleDisplayName = LiveFin;
+				INFOPLIST_KEY_ITSAppUsesNonExemptEncryption = NO;
 				INFOPLIST_KEY_LSApplicationCategoryType = "public.app-category.entertainment";
-				INFOPLIST_KEY_NSLocalNetworkUsageDescription = "This app requires access to the local network to connect to your Jellyfin server.";
+				INFOPLIST_KEY_NSLocalNetworkUsageDescription = "This app needs access to devices on your local network to discover and connect to media servers.";
 				INFOPLIST_KEY_UIApplicationSupportsIndirectInputEvents = YES;
 				INFOPLIST_KEY_UILaunchScreen_Generation = YES;
+				INFOPLIST_KEY_UISupportedInterfaceOrientations = "UIInterfaceOrientationPortrait UIInterfaceOrientationLandscapeLeft UIInterfaceOrientationLandscapeRight";
 				INFOPLIST_KEY_UISupportedInterfaceOrientations_iPad = "UIInterfaceOrientationPortrait UIInterfaceOrientationPortraitUpsideDown UIInterfaceOrientationLandscapeLeft UIInterfaceOrientationLandscapeRight";
 				INFOPLIST_KEY_UISupportedInterfaceOrientations_iPhone = "UIInterfaceOrientationPortrait UIInterfaceOrientationLandscapeLeft UIInterfaceOrientationLandscapeRight";
-				IPHONEOS_DEPLOYMENT_TARGET = 16.0;
+				IPHONEOS_DEPLOYMENT_TARGET = 18.0;
 				LD_RUNPATH_SEARCH_PATHS = (
 					"$(inherited)",
 					"@executable_path/Frameworks",
 				);
-				MARKETING_VERSION = 1.3;
+				MARKETING_VERSION = 2.1.1;
 				PRODUCT_BUNDLE_IDENTIFIER = com.kpgamingz.LiveFin;
 				PRODUCT_NAME = "$(TARGET_NAME)";
 				SWIFT_EMIT_LOC_STRINGS = YES;
@@ -568,6 +739,15 @@
 /* End XCBuildConfiguration section */
 
 /* Begin XCConfigurationList section */
+		EB2FC8742E8B7FB900915765 /* Build configuration list for PBXNativeTarget "LiveFin watchOS" */ = {
+			isa = XCConfigurationList;
+			buildConfigurations = (
+				EB2FC8752E8B7FB900915765 /* Debug */,
+				EB2FC8762E8B7FB900915765 /* Release */,
+			);
+			defaultConfigurationIsVisible = 0;
+			defaultConfigurationName = Release;
+		};
 		EBCC8A072DA88899003DF1A6 /* Build configuration list for PBXProject "LiveFin" */ = {
 			isa = XCConfigurationList;
 			buildConfigurations = (
diff --git a/LiveFin.xcodeproj/project.xcworkspace/xcuserdata/kp.xcuserdatad/WorkspaceSettings.xcsettings b/LiveFin.xcodeproj/project.xcworkspace/xcuserdata/kp.xcuserdatad/WorkspaceSettings.xcsettings
index bbfef02..723a561 100644
--- a/LiveFin.xcodeproj/project.xcworkspace/xcuserdata/kp.xcuserdatad/WorkspaceSettings.xcsettings
+++ b/LiveFin.xcodeproj/project.xcworkspace/xcuserdata/kp.xcuserdatad/WorkspaceSettings.xcsettings
@@ -4,6 +4,8 @@
 <dict>
 	<key>BuildLocationStyle</key>
 	<string>UseAppPreferences</string>
+	<key>CompilationCachingSetting</key>
+	<string>Default</string>
 	<key>CustomBuildLocationType</key>
 	<string>RelativeToDerivedData</string>
 	<key>DerivedDataLocationStyle</key>
diff --git a/LiveFin.xcodeproj/xcuserdata/kp.xcuserdatad/xcschemes/xcschememanagement.plist b/LiveFin.xcodeproj/xcuserdata/kp.xcuserdatad/xcschemes/xcschememanagement.plist
index c6c1e8a..bb1e413 100644
--- a/LiveFin.xcodeproj/xcuserdata/kp.xcuserdatad/xcschemes/xcschememanagement.plist
+++ b/LiveFin.xcodeproj/xcuserdata/kp.xcuserdatad/xcschemes/xcschememanagement.plist
@@ -4,11 +4,44 @@
 <dict>
 	<key>SchemeUserState</key>
 	<dict>
+		<key>LiveFin watchOS Watch App.xcscheme_^#shared#^_</key>
+		<dict>
+			<key>orderHint</key>
+			<integer>1</integer>
+		</dict>
 		<key>LiveFin.xcscheme_^#shared#^_</key>
 		<dict>
 			<key>orderHint</key>
 			<integer>0</integer>
 		</dict>
 	</dict>
+	<key>SuppressBuildableAutocreation</key>
+	<dict>
+		<key>EB2FC8682E8B7FB700915765</key>
+		<dict>
+			<key>primary</key>
+			<true/>
+		</dict>
+		<key>EB9F7D332E878F1200597D25</key>
+		<dict>
+			<key>primary</key>
+			<true/>
+		</dict>
+		<key>EBCC8A0B2DA88899003DF1A6</key>
+		<dict>
+			<key>primary</key>
+			<true/>
+		</dict>
+		<key>EBCC8A182DA8889B003DF1A6</key>
+		<dict>
+			<key>primary</key>
+			<true/>
+		</dict>
+		<key>EBCC8A222DA8889B003DF1A6</key>
+		<dict>
+			<key>primary</key>
+			<true/>
+		</dict>
+	</dict>
 </dict>
 </plist>
diff --git a/LiveFin/App Specific/AboutView.swift b/LiveFin/App Specific/AboutView.swift
deleted file mode 100644
index c5a9267..0000000
--- a/LiveFin/App Specific/AboutView.swift	
+++ /dev/null
@@ -1,58 +0,0 @@
-import SwiftUI
-
-struct AboutView: View {
-    @Environment(\.dismiss) var dismiss
-    @EnvironmentObject var appState: AppState
-    
-    // Helper to strip protocol from serverURL
-    var serverURLStripped: String {
-        appState.serverURL.replacingOccurrences(of: "https://", with: "").replacingOccurrences(of: "http://", with: "")
-    }
-    var body: some View {
-        NavigationStack {
-            VStack(spacing: 24) {
-                Image("Logo")
-                    .resizable()
-                    .aspectRatio(contentMode: .fit)
-                    .frame(width: 120, height: 120)
-                    .foregroundColor(.accentColor)
-                Text("LiveFin")
-                    .font(.largeTitle)
-                    .fontWeight(.bold)
-                Text("Version \(appState.clientVersion)")
-                    .font(.subheadline)
-                    .foregroundColor(.secondary)
-                Text("LiveFin is an open-source TV streaming app. Developed by KPGamingz.")
-                    .multilineTextAlignment(.center)
-                    .padding(.horizontal)
-                // --- AppState Info Section ---
-                VStack(alignment: .leading, spacing: 12) {
-                    Divider()
-                    Text("User: \(appState.username.isEmpty ? (appState.user?.name ?? "-") : appState.username)")
-                        .font(.body)
-                    Text("Device: \(appState.clientDevice)")
-                        .font(.body)
-                    Text("Server Name: \(appState.serverName.isEmpty ? "-" : appState.serverName)")
-                        .font(.body)
-                    Text("Server Version: \(appState.serverVersion.isEmpty ? "-" : appState.serverVersion)")
-                        .font(.body)
-                    Text("Server URL: \(serverURLStripped)")
-                        .font(.body)
-                }
-                .foregroundColor(.secondary)
-                .frame(maxWidth: .infinity, alignment: .leading)
-                .padding(.top, 8)
-                Spacer()
-            }
-            .padding()
-            .navigationTitle("About")
-            .toolbar {
-                ToolbarItem(placement: .cancellationAction) {
-                    Button(action: { dismiss() }) {
-                        Image(systemName: "xmark")
-                    }
-                }
-            }
-        }
-    }
-}
diff --git a/LiveFin/App Specific/AppState.swift b/LiveFin/App Specific/AppState.swift
index 3c6b68e..917542a 100644
--- a/LiveFin/App Specific/AppState.swift	
+++ b/LiveFin/App Specific/AppState.swift	
@@ -1,5 +1,6 @@
 import SwiftUI
 import Foundation
+import UIKit
 #if canImport(WatchConnectivity)
 import WatchConnectivity
 #endif
@@ -20,14 +21,27 @@ final class AppState: ObservableObject {
         return newUUID
     }()
     @Published var apiKey: String = "" // Add this to AppState
-    
+    // User profile image cache
+    @Published var userPrimaryImageTag: String? = nil
+    @Published var userProfileImage: UIImage? = nil
+
     // New playback-related published properties
     @Published var currentPlaybackItemId: String? = nil
     @Published var isPlaying: Bool = false
     @Published var currentChannelImageUrl: String? = nil
     @Published var currentProgramTitle: String? = nil
     @Published var currentProgramSubtitle: String? = nil
-
+    // New: program identity + image tag for Now Playing artwork
+    @Published var currentProgramId: String? = nil
+    @Published var currentProgramPrimaryImageTag: String? = nil
+    @Published var currentProgramImageType: String? = nil // "Primary" or "Thumb"
+    @Published var currentProgramThumbImageTag: String? = nil // New: capture Thumb tag when available
+    // New: genres/tags for the current program (used by UI / related queries)
+    @Published var currentProgramGenres: [String]? = nil
+    // New: whether the current program is a movie (used to suppress subtitles in player)
+    @Published var currentProgramIsMovie: Bool = false
+    @Published var currentProgramStartDate: Date? = nil // New: track start time
+    @Published var currentProgramEndDate: Date? = nil   // New: track end time
     // Global channel name cache (Id -> Name) populated by HomeViewModel and others
     @Published var channelNames: [String: String] = [:]
 
@@ -47,6 +61,18 @@ final class AppState: ObservableObject {
     @Published var isDemoMode: Bool = false
     @Published var loginError: String? = nil // Add this line
 
+    // Helper: build absolute URL safely from serverURL and a path (handles leading slash)
+    private func buildURL(_ path: String) -> URL? {
+        guard !serverURL.isEmpty else { return nil }
+        let base = serverURL.hasSuffix("/") ? String(serverURL.dropLast()) : serverURL
+        let normalizedPath = path.hasPrefix("/") ? path : "/\(path)"
+        return URL(string: base + normalizedPath)
+    }
+
+    // Small date helpers used across AppState
+    private func startOfDay(_ date: Date) -> Date { Calendar.current.startOfDay(for: date) }
+    private func endOfDay(_ date: Date) -> Date { Calendar.current.date(byAdding: .day, value: 1, to: startOfDay(date)) ?? date.addingTimeInterval(24*3600) }
+
     // MARK: - Demo Mode Activation
     @MainActor
     private func activateDemoMode() {
@@ -86,6 +112,9 @@ final class AppState: ObservableObject {
 #if canImport(WatchConnectivity)
         WatchSyncManager.shared.sendLogin(serverURL: serverURL, accessToken: accessToken, apiKey: self.apiKey, userId: userId)
 #endif
+        Task { await self.refreshUserProfileInfoAndImage() }
+        // Report capabilities (includes IconUrl) so the server shows our logo
+        self.reportFullClientCapabilities()
     }
 
     // MARK: - State Reset
@@ -101,6 +130,8 @@ final class AppState: ObservableObject {
         self.serverURL = ""
         self.apiKey = ""
         self.loginError = nil
+        self.userPrimaryImageTag = nil
+        self.userProfileImage = nil
 #if canImport(WatchConnectivity)
         WatchSyncManager.shared.sendLogout()
 #endif
@@ -110,7 +141,7 @@ final class AppState: ObservableObject {
     func login(server: URL, username: String, password: String) async {
         await MainActor.run { self.resetState() }
         if username == "appledemo" && password == "review" {
-            await activateDemoMode()
+            activateDemoMode()
             print("Demo Mode activated for App Store review.")
             return
         }
@@ -162,10 +193,13 @@ final class AppState: ObservableObject {
             }
             do {
                 let authResponse = try JSONDecoder().decode(LoginResponse.self, from: data)
-                await activateNormalUser(userId: authResponse.User.Id, userName: authResponse.User.Name, accessToken: authResponse.AccessToken, client: client, serverURL: server.absoluteString)
+                // Recreate client with access token so SDK calls are authenticated
+                let clientWithToken = JellyfinClient(configuration: config, accessToken: authResponse.AccessToken)
+                activateNormalUser(userId: authResponse.User.Id, userName: authResponse.User.Name, accessToken: authResponse.AccessToken, client: clientWithToken, serverURL: server.absoluteString)
                 print("Debug: Access token set to: \(self.accessToken)")
                 print("Debug: Client initialized: \(self.client != nil)")
                 await fetchServerName()
+                Task { await self.refreshUserProfileInfoAndImage() }
             } catch {
                 if let responseString = String(data: data, encoding: .utf8) {
                     print("Login failed: Could not decode response. Response body: \(responseString)")
@@ -199,7 +233,8 @@ final class AppState: ObservableObject {
             version: clientVersion
         )
         print("[RESTORE] Using deviceId=\(stableDeviceId) for restored session")
-        self.client = JellyfinClient(configuration: config)
+        // Initialize client with existing access token so SDK calls are authorized
+        self.client = JellyfinClient(configuration: config, accessToken: token)
         self.serverURL = server
         self.accessToken = token
         self.username = username
@@ -214,11 +249,14 @@ final class AppState: ObservableObject {
             WatchSyncManager.shared.sendLogin(serverURL: server, accessToken: token, apiKey: self.apiKey, userId: self.userID)
         }
 #endif
+        Task { await self.refreshUserProfileInfoAndImage() }
+        // On restore, also ensure capabilities are sent so the device icon is available to the server
+        self.reportFullClientCapabilities()
     }
 
     @MainActor
     func fetchServerName() async {
-        guard !serverURL.isEmpty, let url = URL(string: serverURL)?.appendingPathComponent("/System/Info/Public") else { return }
+        guard let url = buildURL("/System/Info/Public") else { return }
         do {
             let (data, _) = try await URLSession.shared.data(from: url)
             if let json = try? JSONSerialization.jsonObject(with: data) as? [String: Any] {
@@ -246,7 +284,7 @@ final class AppState: ObservableObject {
     // MARK: - Jellyfin Playstate Reporting (centralized)
     @MainActor
     func reportPlaybackStart(itemId: String, canSeek: Bool = true, playMethod: String = "DirectPlay", repeatMode: String = "RepeatNone") {
-        guard !serverURL.isEmpty, !accessToken.isEmpty, let url = URL(string: serverURL)?.appendingPathComponent("/Sessions/Playing") else {
+        guard let url = buildURL("/Sessions/Playing"), !accessToken.isEmpty else {
             print("AppState: Missing server URL or access token")
             return
         }
@@ -280,7 +318,7 @@ final class AppState: ObservableObject {
 
     @MainActor
     func reportPlaybackProgress(itemId: String, positionTicks: Int64, canSeek: Bool = true, isPaused: Bool = false, playMethod: String = "DirectPlay", repeatMode: String = "RepeatNone") {
-        guard !serverURL.isEmpty, !accessToken.isEmpty, let url = URL(string: serverURL)?.appendingPathComponent("/Sessions/Playing/Progress") else {
+        guard let url = buildURL("/Sessions/Playing/Progress"), !accessToken.isEmpty else {
             print("AppState: Missing server URL or access token")
             return
         }
@@ -312,7 +350,7 @@ final class AppState: ObservableObject {
 
     @MainActor
     func reportPlaybackStopped(itemId: String, positionTicks: Int64, playMethod: String = "DirectPlay") {
-        guard !serverURL.isEmpty, !accessToken.isEmpty, let url = URL(string: serverURL)?.appendingPathComponent("/Sessions/Playing/Stopped") else {
+        guard let url = buildURL("/Sessions/Playing/Stopped"), !accessToken.isEmpty else {
             print("AppState: Missing server URL or access token")
             return
         }
@@ -368,22 +406,26 @@ final class AppState: ObservableObject {
 
     @MainActor
     func fetchCurrentProgram(channelId: String) async {
-        guard !serverURL.isEmpty else { return }
-        // Build start and end times (now -> +1 hour)
+        guard let base = buildURL("/LiveTv/Programs") else { return }
+        // Build start and end times (now -> +1 hour) in UTC to avoid server TZ ambiguity
         let now = Date()
         let end = now.addingTimeInterval(3600)
         let iso = ISO8601DateFormatter()
-        iso.formatOptions = [.withInternetDateTime]
+        iso.timeZone = TimeZone(secondsFromGMT: 0)
+        iso.formatOptions = [.withInternetDateTime, .withFractionalSeconds]
         let startStr = iso.string(from: now)
         let endStr = iso.string(from: end)
 
-        guard let base = URL(string: serverURL)?.appendingPathComponent("/LiveTv/Programs") else { return }
         var comps = URLComponents(url: base, resolvingAgainstBaseURL: false)
         comps?.queryItems = [
-            URLQueryItem(name: "StartDateUtc", value: startStr),
-            URLQueryItem(name: "EndDateUtc", value: endStr),
-            URLQueryItem(name: "ChannelIds", value: channelId)
+            URLQueryItem(name: "channelIds", value: channelId),
+            URLQueryItem(name: "startDate", value: startStr),
+            URLQueryItem(name: "endDate", value: endStr),
+            URLQueryItem(name: "EnableImages", value: "true"),
+            URLQueryItem(name: "EnableUserData", value: "false"),
+            URLQueryItem(name: "fields", value: "SeriesName,Overview,ImageTags,ChannelId,ProgramId,Type,IsMovie")
         ]
+        if let uid = user?.id, !uid.isEmpty { comps?.queryItems?.append(URLQueryItem(name: "userId", value: uid)) }
         guard let url = comps?.url else { return }
 
         var request = URLRequest(url: url)
@@ -397,51 +439,517 @@ final class AppState: ObservableObject {
             guard let http = response as? HTTPURLResponse, http.statusCode == 200 else {
                 return
             }
-            // Attempt to decode JSON array of program objects
-            if let json = try JSONSerialization.jsonObject(with: data) as? [[String: Any]] {
-                // Find program that contains 'now' within StartDate and EndDate
-                let isoParser = ISO8601DateFormatter()
-                isoParser.formatOptions = [.withInternetDateTime]
-                var found: [String: Any]? = nil
-                for prog in json {
-                    if let startS = prog["StartDate"] as? String ?? prog["StartDateUtc"] as? String,
-                       let endS = prog["EndDate"] as? String ?? prog["EndDateUtc"] as? String,
-                       let startD = isoParser.date(from: startS),
-                       let endD = isoParser.date(from: endS) {
-                        if startD <= now && now <= endD {
-                            found = prog
-                            break
-                        }
-                    }
+            // Decode response (prefer { Items: [...] }, fallback to array)
+            var list: [[String: Any]] = []
+            if let root = try JSONSerialization.jsonObject(with: data) as? [String: Any] {
+                if let items = root["Items"] as? [[String: Any]] { list = items }
+                else if let items = root["items"] as? [[String: Any]] { list = items }
+            } else if let arr = try JSONSerialization.jsonObject(with: data) as? [[String: Any]] {
+                list = arr
+            }
+
+            guard !list.isEmpty else {
+                await MainActor.run {
+                    self.currentProgramTitle = nil
+                    self.currentProgramSubtitle = nil
+                    self.currentProgramId = nil
+                    self.currentProgramPrimaryImageTag = nil
+                    self.currentProgramImageType = nil
+                    self.currentProgramThumbImageTag = nil
+                    self.currentProgramIsMovie = false
+                    self.currentProgramStartDate = nil
+                    self.currentProgramEndDate = nil
                 }
+                return
+            }
 
-                if let program = found ?? json.first {
-                    let title = program["Name"] as? String ?? program["SeriesName"] as? String
-                    let overview = program["Overview"] as? String
-                    await MainActor.run {
-                        self.currentProgramTitle = title
-                        if let ov = overview, !ov.isEmpty {
-                            self.currentProgramSubtitle = ov
-                        } else if let series = program["SeriesName"] as? String, series != title {
-                            self.currentProgramSubtitle = series
-                        } else {
-                            self.currentProgramSubtitle = nil
-                        }
+            // Find program that contains 'now'; else choose nearest upcoming; else latest before
+            let parser = ISO8601DateFormatter()
+            parser.formatOptions = [.withInternetDateTime, .withFractionalSeconds]
+            var selected: [String: Any]? = nil
+            var latestBeforeNow: (prog: [String: Any], start: Date)? = nil
+            var earliestAfterNow: (prog: [String: Any], start: Date)? = nil
+            for prog in list {
+                let startS = (prog["StartDate"] as? String) ?? (prog["StartDateUtc"] as? String)
+                let endS = (prog["EndDate"] as? String) ?? (prog["EndDateUtc"] as? String)
+                // Try with fractional seconds, then without as a fallback to be robust
+                func parse(_ s: String?) -> Date? {
+                    guard let s = s else { return nil }
+                    if let d = parser.date(from: s) { return d }
+                    let fallback = ISO8601DateFormatter()
+                    fallback.formatOptions = [.withInternetDateTime]
+                    return fallback.date(from: s)
+                }
+                guard let startD = parse(startS), let endD = parse(endS) else { continue }
+                if startD <= now && now <= endD { selected = prog; break }
+                if startD <= now {
+                    if let latest = latestBeforeNow {
+                        if startD > latest.start { latestBeforeNow = (prog, startD) }
+                    } else {
+                        latestBeforeNow = (prog, startD)
                     }
-                    return
+                }
+                if startD > now {
+                    if let earliest = earliestAfterNow {
+                        if startD < earliest.start { earliestAfterNow = (prog, startD) }
+                    } else {
+                        earliestAfterNow = (prog, startD)
+                    }
+                }
+            }
+            let program = selected ?? earliestAfterNow?.prog ?? latestBeforeNow?.prog ?? list.first!
+            // Extract times again for chosen program
+            let startTimeString = program["StartDate"] as? String ?? program["StartDateUtc"] as? String
+            let endTimeString = program["EndDate"] as? String ?? program["EndDateUtc"] as? String
+            let programStartDate = (parser.date(from: startTimeString ?? "")) ?? {
+                let fb = ISO8601DateFormatter(); fb.formatOptions = [.withInternetDateTime]; return fb.date(from: startTimeString ?? "")
+            }()
+            let programEndDate = (parser.date(from: endTimeString ?? "")) ?? {
+                let fb = ISO8601DateFormatter(); fb.formatOptions = [.withInternetDateTime]; return fb.date(from: endTimeString ?? "")
+            }()
+
+            let title = program["Name"] as? String ?? program["SeriesName"] as? String
+            let overview = program["Overview"] as? String
+            // Use Id for images; ProgramId may not be a valid item endpoint
+            let programId = (program["Id"] as? String)
+            var primaryTag: String? = nil
+            var imageType: String? = nil
+            var thumbTag: String? = nil
+            if let imageTags = program["ImageTags"] as? [String: Any] {
+                if let t = imageTags["Primary"] as? String {
+                    primaryTag = t
+                    imageType = "Primary"
+                }
+                if let th = imageTags["Thumb"] as? String {
+                    thumbTag = th
+                    if primaryTag == nil {
+                        primaryTag = th
+                        imageType = "Thumb"
+                    }
+                }
+            }
+            // Detect if program is a movie via common fields
+            let typeStr = (program["Type"] as? String) ?? (program["ProgramType"] as? String)
+            let isMovie = (typeStr?.caseInsensitiveCompare("Movie") == .orderedSame) || (program["IsMovie"] as? Bool == true)
+
+            await MainActor.run {
+                self.currentProgramTitle = title
+                // Prefer series name or explicit episode title for subtitle. Do NOT use the program Overview (description)
+                if let series = program["SeriesName"] as? String, series != title {
+                    self.currentProgramSubtitle = series
                 } else {
-                    await MainActor.run {
-                        self.currentProgramTitle = nil
+                    // Try common episode title keys (EPG sources vary)
+                    let episodeTitle = (program["EpisodeTitle"] as? String) ?? (program["EpisodeName"] as? String) ?? (program["PartTitle"] as? String)
+                    if let ep = episodeTitle, !ep.isEmpty {
+                        self.currentProgramSubtitle = ep
+                    } else {
                         self.currentProgramSubtitle = nil
                     }
                 }
+                // Extract genres/tags from several possible shapes the server may return
+                var resolvedGenres: [String]? = nil
+                if let gs = program["Genres"] as? [String] {
+                    resolvedGenres = gs
+                } else if let gsArr = program["Genres"] as? [[String: Any]] {
+                    // Some servers return array of objects { Name: "..." }
+                    resolvedGenres = gsArr.compactMap { $0["Name"] as? String }
+                } else if let tags = program["Tags"] as? [String] {
+                    resolvedGenres = tags
+                }
+                self.currentProgramGenres = resolvedGenres
+                 self.currentProgramId = programId
+                 self.currentProgramPrimaryImageTag = primaryTag
+                 self.currentProgramImageType = imageType
+                 self.currentProgramThumbImageTag = thumbTag
+                 self.currentProgramIsMovie = isMovie
+                 self.currentProgramStartDate = programStartDate
+                 self.currentProgramEndDate = programEndDate
+             }
+         } catch {
+             print("EPG fetch error: \(error.localizedDescription)")
+         }
+     }
+
+    // MARK: - Report full client capabilities (adds ImageUrl for server devices/playstate)
+    func reportFullClientCapabilities() {
+        guard !accessToken.isEmpty else { return }
+        // Use the exact logo URL requested by the app
+        let iconUrl = "https://raw.githubusercontent.com/LiveFin/web/refs/heads/master/assets/LiveFin%20Logo%20No%20Background.png"
+
+        // Compose supported commands once
+        let supportedEnum: [JellyfinAPI.GeneralCommandType] = [
+            .play,
+            .mute,
+            .unmute,
+            .setVolume,
+            .setAudioStreamIndex,
+            .setSubtitleStreamIndex,
+            .setRepeatMode,
+            .setMaxStreamingBitrate,
+            .setPlaybackOrder
+        ]
+
+        // Prefer SDK which knows the exact schema and enums; include device id as query parameter
+        if let client = self.client {
+            let capabilities = JellyfinAPI.ClientCapabilitiesDto(
+                appStoreURL: nil,
+                deviceProfile: nil,
+                iconURL: iconUrl,
+                playableMediaTypes: [.video],
+                supportedCommands: supportedEnum,
+                isSupportsMediaControl: true,
+                isSupportsPersistentIdentifier: true
+            )
+            Task {
+                do {
+                    _ = try await client.send(Paths.postFullCapabilities(id: self.deviceId, capabilities))
+                } catch {
+                    print("[Capabilities] SDK postFullCapabilities failed: \(error). Trying /Sessions/Capabilities (query).")
+                    do {
+                        let params = Paths.PostCapabilitiesParameters(
+                            id: self.deviceId,
+                            playableMediaTypes: [.video],
+                            supportedCommands: supportedEnum,
+                            isSupportsMediaControl: true,
+                            isSupportsPersistentIdentifier: true
+                        )
+                        _ = try await client.send(Paths.postCapabilities(parameters: params))
+                        return
+                    } catch {
+                        print("[Capabilities] SDK postCapabilities failed: \(error). Falling back to manual request(s).")
+                        self.postCapabilitiesManuallyLegacy(iconUrl: iconUrl, supported: supportedEnum.map { $0.rawValue })
+                    }
+                }
+            }
+            return
+        }
+        // Fallback if client is nil
+        postCapabilitiesManuallyLegacy(iconUrl: iconUrl, supported: supportedEnum.map { $0.rawValue })
+    }
+
+    // Legacy/manual capabilities for older servers: POST /Sessions/Capabilities with query params
+    private func postCapabilitiesManuallyLegacy(iconUrl: String, supported: [String]) {
+        // Try /Sessions/Capabilities (query)
+        guard !serverURL.isEmpty else { return }
+        let base = serverURL.hasSuffix("/") ? String(serverURL.dropLast()) : serverURL
+        // 1) /Sessions/Capabilities (query)
+        if var comps = URLComponents(string: base + "/Sessions/Capabilities") {
+            var items: [URLQueryItem] = []
+            items.append(URLQueryItem(name: "id", value: deviceId))
+            items.append(URLQueryItem(name: "supportsMediaControl", value: "true"))
+            items.append(URLQueryItem(name: "supportsPersistentIdentifier", value: "true"))
+            // Repeat for each value
+            items.append(URLQueryItem(name: "playableMediaTypes", value: "Video"))
+            for cmd in supported { items.append(URLQueryItem(name: "supportedCommands", value: cmd)) }
+            comps.queryItems = items
+            if let url = comps.url {
+                var req = URLRequest(url: url)
+                req.httpMethod = "POST"
+                req.setValue(accessToken, forHTTPHeaderField: "X-Emby-Token")
+                URLSession.shared.dataTask(with: req) { data, response, error in
+                    if let http = response as? HTTPURLResponse {
+                        print("[Capabilities] (/Sessions/Capabilities) status=\(http.statusCode)")
+                        if http.statusCode == 200 || http.statusCode == 204 { return }
+                    }
+                    if let data = data, let s = String(data: data, encoding: .utf8) { print("[Capabilities] (/Sessions/Capabilities) body=\(s)") }
+                    // 2) Fallback to /Sessions/Capabilities/Full JSON body
+                    self.postCapabilitiesManually(iconUrl: iconUrl)
+                }.resume()
+                return
+            }
+        }
+        // If we couldn't build URL, fallback to JSON body variant
+        self.postCapabilitiesManually(iconUrl: iconUrl)
+    }
+
+    // Manual JSON-body capabilities POST to /Sessions/Capabilities/Full (used as last resort)
+    private func postCapabilitiesManually(iconUrl: String) {
+        guard let baseURL = buildURL("/Sessions/Capabilities/Full"), !accessToken.isEmpty else { return }
+        var comps = URLComponents(url: baseURL, resolvingAgainstBaseURL: false)
+        var q = comps?.queryItems ?? []
+        q.append(URLQueryItem(name: "id", value: self.deviceId))
+        comps?.queryItems = q
+        guard let url = comps?.url else { return }
+
+        var request = URLRequest(url: url)
+        request.httpMethod = "POST"
+        request.setValue("application/json", forHTTPHeaderField: "Content-Type")
+        request.setValue(accessToken, forHTTPHeaderField: "X-Emby-Token")
+        let authHeader = "MediaBrowser Client=\"LiveFin\", Device=\"\(clientDevice)\", DeviceId=\"\(deviceId)\", Version=\"\(clientVersion)\", Token=\"\(accessToken)\""
+        request.setValue(authHeader, forHTTPHeaderField: "X-Emby-Authorization")
+
+        let supported: [String] = [
+            JellyfinAPI.GeneralCommandType.play.rawValue,
+            JellyfinAPI.GeneralCommandType.mute.rawValue,
+            JellyfinAPI.GeneralCommandType.unmute.rawValue,
+            JellyfinAPI.GeneralCommandType.setVolume.rawValue,
+            JellyfinAPI.GeneralCommandType.setAudioStreamIndex.rawValue,
+            JellyfinAPI.GeneralCommandType.setSubtitleStreamIndex.rawValue,
+            JellyfinAPI.GeneralCommandType.setRepeatMode.rawValue,
+            JellyfinAPI.GeneralCommandType.setMaxStreamingBitrate.rawValue,
+            JellyfinAPI.GeneralCommandType.setPlaybackOrder.rawValue
+        ]
+        let body: [String: Any] = [
+            "PlayableMediaTypes": ["Video"],
+            "SupportedCommands": supported,
+            "SupportsMediaControl": true,
+            "SupportsPersistentIdentifier": true,
+            "IconUrl": iconUrl
+        ]
+
+        if let dbg = try? JSONSerialization.data(withJSONObject: body, options: .prettyPrinted), let dbgStr = String(data: dbg, encoding: .utf8) {
+            print("[Capabilities] Posting body: \n\(dbgStr)")
+        }
+        request.httpBody = try? JSONSerialization.data(withJSONObject: body)
+
+        let serverBase = serverURL.hasSuffix("/") ? String(serverURL.dropLast()) : serverURL
+        let token = accessToken
+        let deviceIdLocal = deviceId
+        let deviceNameLocal = clientDevice
+
+        URLSession.shared.dataTask(with: request) { data, response, error in
+            if let error = error {
+                print("[Capabilities] Failed to report: \(error)")
+                return
+            }
+            if let http = response as? HTTPURLResponse {
+                print("[Capabilities] Server response: \(http.statusCode)")
+                if http.statusCode != 204 && http.statusCode != 200 {
+                    if let data = data, let s = String(data: data, encoding: .utf8) { print("[Capabilities] Body: \(s)") }
+                }
+            }
+            // Optional: fetch sessions and print stored IconUrl/ImageUrl for this device for debugging
+            if !serverBase.isEmpty, let sessionsURL = URL(string: serverBase + "/Sessions") {
+                var req = URLRequest(url: sessionsURL)
+                req.httpMethod = "GET"
+                if !token.isEmpty { req.setValue(token, forHTTPHeaderField: "X-Emby-Token") }
+                URLSession.shared.dataTask(with: req) { data2, resp2, err2 in
+                    if let err2 = err2 { print("[Sessions] fetch error: \(err2)"); return }
+                    guard let data2 = data2, let arr = try? JSONSerialization.jsonObject(with: data2) as? [[String: Any]] else { return }
+                    if let session = arr.first(where: { ($0["DeviceId"] as? String) == deviceIdLocal }) ?? arr.first(where: { ($0["DeviceName"] as? String) == deviceNameLocal }) {
+                        if let caps = session["ClientCapabilities"] as? [String: Any] {
+                            if let icon = caps["IconUrl"] as? String { print("[Sessions] ClientCapabilities.IconUrl = \(icon)") }
+                            if let image = caps["ImageUrl"] as? String { print("[Sessions] ClientCapabilities.ImageUrl = \(image)") }
+                        }
+                        if let icon = session["IconUrl"] as? String { print("[Sessions] Session.IconUrl = \(icon)") }
+                        if let image = session["ImageUrl"] as? String { print("[Sessions] Session.ImageUrl = \(image)") }
+                    } else {
+                        print("[Sessions] No session matched deviceId=\(deviceIdLocal)")
+                    }
+                }.resume()
+            }
+        }.resume()
+    }
+
+    // Debug: inspect sessions and print any IconUrl/ImageUrl stored for this device
+    private func debugLogCurrentSessionIcon() {
+        guard let url = buildURL("/Sessions"), !accessToken.isEmpty else { return }
+        var req = URLRequest(url: url)
+        req.httpMethod = "GET"
+        req.setValue(accessToken, forHTTPHeaderField: "X-Emby-Token")
+        let deviceIdLocal = self.deviceId
+        let deviceNameLocal = self.clientDevice
+        URLSession.shared.dataTask(with: req) { data, resp, err in
+            if let err = err { print("[Sessions] fetch error: \(err)"); return }
+            guard let data = data, let arr = try? JSONSerialization.jsonObject(with: data) as? [[String: Any]] else { return }
+            if let session = arr.first(where: { ($0["DeviceId"] as? String) == deviceIdLocal }) ?? arr.first(where: { ($0["DeviceName"] as? String) == deviceNameLocal }) {
+                if let caps = session["ClientCapabilities"] as? [String: Any] {
+                    if let icon = caps["IconUrl"] as? String { print("[Sessions] ClientCapabilities.IconUrl = \(icon)") }
+                    if let image = caps["ImageUrl"] as? String { print("[Sessions] ClientCapabilities.ImageUrl = \(image)") }
+                }
+                if let icon = session["IconUrl"] as? String { print("[Sessions] Session.IconUrl = \(icon)") }
+                if let image = session["ImageUrl"] as? String { print("[Sessions] Session.ImageUrl = \(image)") }
+                if let any = AppState.findIconUrlRecursively(in: session) { print("[Sessions] Found Icon/Image URL (recursive) = \(any)") }
+            } else {
+                print("[Sessions] No session matched deviceId=\(deviceIdLocal)")
+            }
+        }.resume()
+    }
+
+    private static func findIconUrlRecursively(in dict: [String: Any]) -> String? {
+        for (k, v) in dict {
+            if (k == "IconUrl" || k == "ImageUrl"), let s = v as? String { return s }
+            if let sub = v as? [String: Any], let s = findIconUrlRecursively(in: sub) { return s }
+            if let arr = v as? [[String: Any]] {
+                for el in arr { if let s = findIconUrlRecursively(in: el) { return s } }
+            }
+        }
+        return nil
+    }
+
+    // MARK: - User Profile Image Handling
+    @MainActor
+    func refreshUserProfileInfoAndImage() async {
+        guard !serverURL.isEmpty, !accessToken.isEmpty, !userID.isEmpty else { return }
+        await fetchUserPrimaryImageTag()
+        await fetchUserPrimaryImage()
+    }
+
+    @MainActor
+    private func fetchUserPrimaryImageTag() async {
+        guard !serverURL.isEmpty, !accessToken.isEmpty, !userID.isEmpty else { return }
+        let base = serverURL.hasSuffix("/") ? String(serverURL.dropLast()) : serverURL
+        guard let url = URL(string: base + "/Users/\(userID)") else { return }
+        var req = URLRequest(url: url)
+        req.httpMethod = "GET"
+        req.setValue(accessToken, forHTTPHeaderField: "X-Emby-Token")
+        do {
+            let (data, resp) = try await URLSession.shared.data(for: req)
+            guard let http = resp as? HTTPURLResponse, http.statusCode == 200 else { return }
+            struct UserDetail: Decodable { let PrimaryImageTag: String? }
+            if let detail = try? JSONDecoder().decode(UserDetail.self, from: data) {
+                if self.userPrimaryImageTag != detail.PrimaryImageTag {
+                    self.userPrimaryImageTag = detail.PrimaryImageTag
+                }
+            }
+        } catch { print("[ProfileImage] Tag fetch failed: \(error)") }
+    }
+
+    @MainActor
+    private func fetchUserPrimaryImage() async {
+        guard !serverURL.isEmpty, !accessToken.isEmpty, !userID.isEmpty else { return }
+        let base = serverURL.hasSuffix("/") ? String(serverURL.dropLast()) : serverURL
+        var urlString = base + "/Users/\(userID)/Images/Primary"
+        if let tag = userPrimaryImageTag, !tag.isEmpty { urlString += "?tag=\(tag)&quality=80" } else { urlString += "?quality=80" }
+        guard let url = URL(string: urlString) else { return }
+        var req = URLRequest(url: url)
+        req.httpMethod = "GET"
+        req.setValue(accessToken, forHTTPHeaderField: "X-Emby-Token")
+        do {
+            let (data, resp) = try await URLSession.shared.data(for: req)
+            guard let http = resp as? HTTPURLResponse, http.statusCode == 200 else { return }
+            if let image = UIImage(data: data) { self.userProfileImage = image }
+        } catch { print("[ProfileImage] Image fetch failed: \(error)") }
+    }
+
+    // MARK: - Background EPG refresh
+    // This will be invoked from BGAppRefresh handler to warm local EPG cache files
+    @MainActor
+    func performBackgroundEPGRefresh() async {
+        guard !serverURL.isEmpty else { return }
+        guard !accessToken.isEmpty else { return }
+
+        let cal = Calendar.current
+        let today = startOfDay(Date())
+        let offsets = [-1, 0, 1]
+
+        for off in offsets {
+            let day = cal.date(byAdding: .day, value: off, to: today) ?? today
+            await fetchAndCacheEPG(for: day)
+        }
+
+        // Prune old files (same horizon as GuideView)
+        pruneOldGuideEPGCacheFiles()
+    }
+
+    private func fetchAndCacheEPG(for day: Date) async {
+        let iso = ISO8601DateFormatter()
+        iso.formatOptions = [.withInternetDateTime]
+        let start = startOfDay(day)
+        let end = Calendar.current.date(byAdding: .day, value: 1, to: start) ?? start.addingTimeInterval(24*3600)
+
+        guard let base = buildURL("/LiveTv/Programs") else { return }
+        var comps = URLComponents(url: base, resolvingAgainstBaseURL: false)
+        comps?.queryItems = [
+            URLQueryItem(name: "startDate", value: iso.string(from: start)),
+            URLQueryItem(name: "endDate", value: iso.string(from: end)),
+            URLQueryItem(name: "EnableImages", value: "false"),
+            URLQueryItem(name: "EnableUserData", value: "false"),
+            URLQueryItem(name: "fields", value: "Overview,OfficialRating,EpisodeTitle,ParentIndexNumber,IndexNumber,IsRepeat,IsMovie")
+        ]
+        if let uid = user?.id, !uid.isEmpty { comps?.queryItems?.append(URLQueryItem(name: "userId", value: uid)) }
+        guard let url = comps?.url else { return }
+
+        var req = URLRequest(url: url)
+        req.httpMethod = "GET"
+        req.setValue("application/json", forHTTPHeaderField: "Content-Type")
+        if !accessToken.isEmpty { req.setValue(accessToken, forHTTPHeaderField: "X-Emby-Token") }
+
+        do {
+            let (data, resp) = try await URLSession.shared.data(for: req)
+            if let http = resp as? HTTPURLResponse, http.statusCode != 200 { return }
+
+            let dec = JSONDecoder()
+            dec.dateDecodingStrategy = .custom { d in
+                let c = try d.singleValueContainer(); let s = try c.decode(String.self)
+                let f1 = ISO8601DateFormatter(); f1.formatOptions = [.withInternetDateTime, .withFractionalSeconds]
+                if let dt = f1.date(from: s) { return dt }
+                let f2 = ISO8601DateFormatter(); if let dt2 = f2.date(from: s) { return dt2 }
+                throw DecodingError.dataCorruptedError(in: c, debugDescription: "Cannot parse date: \(s)")
+            }
+
+            struct EPGProgramsResponseLocal: Decodable { let Items: [BaseItemDto]? }
+            let decoded = try dec.decode(EPGProgramsResponseLocal.self, from: data)
+            let items = decoded.Items ?? []
+
+            // Save to cache in GuideCache format
+            try saveGuideEPGCache(for: day, items: items)
+
+        } catch {
+            print("Background EPG fetch failed for \(day): \(error)")
+        }
+    }
+
+    // Guide cache helpers (mirrors GuideView cache naming/format)
+    private var guideCacheFolderName: String { "GuideCache" }
+    private var epgFilePrefix: String { "epg_day_" }
+    private var epgFileExt: String { ".json" }
+
+    private func guideCacheDirectoryURL() throws -> URL {
+        let fm = FileManager.default
+        let base = try fm.url(for: .cachesDirectory, in: .userDomainMask, appropriateFor: nil, create: true)
+        let dir = base.appendingPathComponent(guideCacheFolderName, isDirectory: true)
+        if !fm.fileExists(atPath: dir.path) {
+            try fm.createDirectory(at: dir, withIntermediateDirectories: true)
+        }
+        return dir
+    }
+
+    private func epgCacheFileURL(for day: Date) throws -> URL {
+        let key = dayFileKey(day)
+        return try guideCacheDirectoryURL().appendingPathComponent(epgFilePrefix + key + epgFileExt)
+    }
+
+    private func dayFileKey(_ date: Date) -> String {
+        let df = DateFormatter()
+        df.calendar = Calendar(identifier: .gregorian)
+        df.locale = Locale(identifier: "en_US_POSIX")
+        df.timeZone = .current
+        df.dateFormat = "yyyy-MM-dd"
+        return df.string(from: startOfDay(date))
+    }
+
+    private func saveGuideEPGCache(for day: Date, items: [BaseItemDto]) throws {
+        struct EPGCacheFileLocal: Codable { let dayKey: String; let timestamp: Date; let items: [BaseItemDto] }
+        let payload = EPGCacheFileLocal(dayKey: dayFileKey(day), timestamp: Date(), items: items)
+        let data = try JSONEncoder().encode(payload)
+        let url = try epgCacheFileURL(for: day)
+        try data.write(to: url, options: [.atomic])
+    }
+
+    private func pruneOldGuideEPGCacheFiles() {
+        do {
+            let dir = try guideCacheDirectoryURL()
+            let fm = FileManager.default
+            let files = try fm.contentsOfDirectory(at: dir, includingPropertiesForKeys: nil)
+            let horizon = Calendar.current.date(byAdding: .day, value: -14, to: startOfDay(Date())) ?? Date.distantPast
+            for url in files where url.lastPathComponent.hasPrefix(epgFilePrefix) && url.pathExtension == "json" {
+                let name = url.deletingPathExtension().lastPathComponent
+                let key = String(name.dropFirst(epgFilePrefix.count))
+                let df = DateFormatter()
+                df.calendar = Calendar(identifier: .gregorian)
+                df.locale = Locale(identifier: "en_US_POSIX")
+                df.timeZone = .current
+                df.dateFormat = "yyyy-MM-dd"
+                if let d = df.date(from: key), startOfDay(d) < horizon {
+                    try? fm.removeItem(at: url)
+                }
             }
         } catch {
-            print("EPG fetch error: \(error.localizedDescription)")
+            // ignore
         }
     }
 }
-
 #if canImport(WatchConnectivity)
 final class WatchSyncManager: NSObject, WCSessionDelegate {
     static let shared = WatchSyncManager()
diff --git a/LiveFin/App Specific/ChannelDetailView.swift b/LiveFin/App Specific/ChannelDetailView.swift
deleted file mode 100644
index df48606..0000000
--- a/LiveFin/App Specific/ChannelDetailView.swift	
+++ /dev/null
@@ -1,409 +0,0 @@
-import SwiftUI
-import Foundation
-import AVKit
-import WebKit
-
-struct MediaSourceDto: Codable {
-    let id: String?
-
-    enum CodingKeys: String, CodingKey {
-        case id = "Id"
-    }
-}
-
-struct BaseItemDto: Identifiable, Codable {
-    let id: String?
-    let name: String?
-    let startDate: Date?
-    let endDate: Date?
-    let overview: String?
-    let channelId: String?
-    let mediaSources: [MediaSourceDto]?
-    let officialRating: String?
-    let episodeTitle: String?
-    let parentIndexNumber: Int?
-    let indexNumber: Int?
-    let isRepeat: Bool? // <-- Added isRepeat
-
-    enum CodingKeys: String, CodingKey {
-        case id = "Id"
-        case name = "Name"
-        case startDate = "StartDate"
-        case endDate = "EndDate"
-        case overview = "Overview"
-        case channelId = "ChannelId"
-        case mediaSources = "MediaSources"
-        case officialRating = "OfficialRating"
-        case episodeTitle = "EpisodeTitle"
-        case parentIndexNumber = "ParentIndexNumber"
-        case indexNumber = "IndexNumber"
-        case isRepeat = "IsRepeat" // <-- Added isRepeat
-    }
-
-    init(from decoder: Decoder) throws {
-        let container = try decoder.container(keyedBy: CodingKeys.self)
-
-        id = try? container.decode(String.self, forKey: .id)
-        name = try? container.decode(String.self, forKey: .name)
-        startDate = try? container.decode(Date.self, forKey: .startDate)
-        endDate = try? container.decode(Date.self, forKey: .endDate)
-        overview = try? container.decode(String.self, forKey: .overview)
-        channelId = try? container.decode(String.self, forKey: .channelId)
-        mediaSources = try? container.decode([MediaSourceDto].self, forKey: .mediaSources)
-        officialRating = try? container.decode(String.self, forKey: .officialRating)
-        episodeTitle = try? container.decode(String.self, forKey: .episodeTitle)
-        parentIndexNumber = try? container.decode(Int.self, forKey: .parentIndexNumber)
-        indexNumber = try? container.decode(Int.self, forKey: .indexNumber)
-        isRepeat = try? container.decode(Bool.self, forKey: .isRepeat) // <-- Added isRepeat
-    }
-}
-
-struct ProgramsResponse: Codable {
-    let items: [BaseItemDto]?
-
-    enum CodingKeys: String, CodingKey {
-        case items = "Items"
-    }
-}
-
-struct ChannelDetailView: View {
-    let channel: LiveTvChannelDto
-    @EnvironmentObject var appState: AppState
-    @State private var programs: [BaseItemDto] = []
-    @State private var selectedStreamURL: URL? = nil
-    @State private var selectedProgramTitle: String? = nil
-    @State private var selectedProgramSubtitle: String? = nil
-
-    var filteredPrograms: [BaseItemDto] {
-        let now = Date()
-        return programs.filter { program in
-            guard let end = program.endDate else { return false }
-            return end >= now
-        }
-    }
-
-    var groupedPrograms: [Date: [BaseItemDto]] {
-        Dictionary(grouping: filteredPrograms, by: {
-            Calendar.current.startOfDay(for: $0.startDate ?? .distantPast)
-        })
-    }
-    
-    var programSections: some View {
-        let sortedDates = groupedPrograms.keys.sorted()
-        return ForEach(sortedDates, id: \.self) { date in
-            let programsForDate = groupedPrograms[date] ?? []
-            Section(header: Text(date.formatted(date: .abbreviated, time: .omitted))) {
-                ForEach(programsForDate) { item in
-                    if let converted = JFProgram(epg: item, channelName: channel.name, channelId: channel.id) {
-                        NavigationLink(destination: ProgramView(program: converted)
-                            .environmentObject(appState)
-                            .environmentObject(HomeViewModel())) {
-                                ProgramRowView(program: item, programs: programs, channelId: channel.id, appState: appState)
-                        }
-                        .buttonStyle(.plain)
-                    } else {
-                        ProgramRowView(program: item, programs: programs, channelId: channel.id, appState: appState)
-                    }
-                }
-            }
-        }
-    }
-
-    var body: some View {
-        Group {
-            if filteredPrograms.isEmpty {
-                if programs.isEmpty {
-                    ProgressView("Loading programs...")
-                        .frame(maxWidth: .infinity)
-                } else {
-                    ScrollView {
-                        Text("No programs available.")
-                            .foregroundColor(.secondary)
-                            .padding()
-                    }
-                }
-            } else {
-                List {
-                    programSections
-                }
-                .listStyle(.plain)
-                .refreshable {
-                    await fetchPrograms()
-                }
-            }
-        }
-        .navigationTitle(channel.name ?? "Channel")
-        .toolbar {
-            ToolbarItem(placement: .navigationBarTrailing) {
-                Button(action: {
-                    let channelId = channel.id
-                    let userId = appState.user?.id ?? ""
-                    let serverURL = appState.serverURL
-                    let accessToken = appState.accessToken
-                    let deviceId = UIDevice.current.identifierForVendor?.uuidString ?? UUID().uuidString
-                    let playSessionId = UUID().uuidString
-
-                    // Find the currently airing program (if any) from the already-fetched programs list
-                    let now = Date()
-                    let currentProgram = programs.first(where: { prog in
-                        if let start = prog.startDate, let end = prog.endDate {
-                            return start <= now && now <= end
-                        }
-                        return false
-                    })
-
-                    // Prepare title/subtitle to forward into the player as initial values
-                    let initialTitle = currentProgram?.name
-                    let initialSubtitle = currentProgram?.episodeTitle ?? currentProgram?.overview
-
-                    Task {
-                        if let streamURLString = await LiveTVStreamService.fetchStreamURL(
-                            channelId: channelId,
-                            userId: userId,
-                            serverURL: serverURL,
-                            accessToken: accessToken,
-                            deviceId: deviceId,
-                            playSessionId: playSessionId
-                        ), let url = URL(string: streamURLString) {
-                            // Store initial program info and trigger the player
-                            selectedProgramTitle = initialTitle
-                            selectedProgramSubtitle = initialSubtitle
-                            selectedStreamURL = url
-                        } else {
-                            print("No stream URL available.")
-                        }
-                    }
-                }) {
-                    Image(systemName: "play.circle.fill")
-                        .resizable()
-                        .frame(width: 28, height: 28)
-                        .foregroundColor(.blue)
-                }
-            }
-        }
-        .fullScreenCover(item: $selectedStreamURL) { url in
-            VideoPlayerView(
-                streamURL: url,
-                channel: channel,
-                nextProgramTitle: selectedProgramTitle,
-                nextProgramSubtitle: selectedProgramSubtitle
-            )
-            .environmentObject(appState)
-        }
-        .task {
-            await fetchPrograms()
-        }
-    }
-    
-    func fetchPrograms() async {
-        guard let client = appState.client else {
-            return
-        }
-
-        let accessToken = appState.accessToken
-        guard !accessToken.isEmpty else {
-            return
-        }
-
-        let now = Date()
-        let later = Calendar.current.date(byAdding: .day, value: 1, to: now)!
-
-        do {
-            let url = client.configuration.url.appendingPathComponent("/LiveTv/Programs")
-            var components = URLComponents(url: url, resolvingAgainstBaseURL: false)
-            components?.queryItems = [
-                URLQueryItem(name: "channelIds", value: channel.id),
-                URLQueryItem(name: "startDate", value: ISO8601DateFormatter().string(from: now)),
-                URLQueryItem(name: "endDate", value: ISO8601DateFormatter().string(from: later)),
-                URLQueryItem(name: "StartIndex", value: "0"),
-                URLQueryItem(name: "Limit", value: "1000"),
-                URLQueryItem(name: "fields", value: "MediaSources,Overview,OfficialRating,EpisodeTitle,ParentIndexNumber,IndexNumber"),
-                URLQueryItem(name: "EnableImages", value: "true"),
-                URLQueryItem(name: "EnableUserData", value: "true")
-            ]
-
-            guard let userId = appState.user?.id else {
-                print("DEBUG: User ID is missing")
-                return
-            }
-            components?.queryItems?.append(URLQueryItem(name: "userId", value: userId))
-
-            guard let finalURL = components?.url else {
-                return
-            }
-
-            var request = URLRequest(url: finalURL)
-            request.httpMethod = "GET"
-            request.setValue("application/json", forHTTPHeaderField: "Content-Type")
-            request.setValue(accessToken, forHTTPHeaderField: "X-Emby-Token")
-
-            let (data, response) = try await URLSession.shared.data(for: request)
-
-            guard let httpResponse = response as? HTTPURLResponse, httpResponse.statusCode == 200 else {
-                return
-            }
-
-            let decoder = JSONDecoder()
-            let isoFormatter = ISO8601DateFormatter()
-            isoFormatter.formatOptions = [.withInternetDateTime, .withFractionalSeconds]
-            decoder.dateDecodingStrategy = .custom { decoder in
-                let container = try decoder.singleValueContainer()
-                let dateStr = try container.decode(String.self)
-                guard let date = isoFormatter.date(from: dateStr) else {
-                    throw DecodingError.dataCorruptedError(in: container, debugDescription: "Cannot decode date: \(dateStr)")
-                }
-                return date
-            }
-
-            let decoded = try decoder.decode(ProgramsResponse.self, from: data)
-            self.programs = decoded.items ?? []
-
-        } catch {
-        }
-    }
-}
-
-struct ProgramRowView: View {
-    let program: BaseItemDto
-    let programs: [BaseItemDto]
-    let channelId: String
-    @ObservedObject var appState: AppState
-    @State private var isExpanded = false
-    @State private var selectedStreamURL: URL?
-    @State private var showPlayer = false
-
-    var body: some View {
-        let isLive: Bool = {
-            if let start = program.startDate, let end = program.endDate {
-                return start <= Date() && end >= Date()
-            }
-            return false
-        }()
-        let showNew: Bool = {
-            (program.isRepeat == false || program.isRepeat == nil) && program.episodeTitle != nil
-        }()
-        HStack(alignment: .top) {
-            VStack(alignment: .leading, spacing: 6) {
-                // Title, subtitle, and labels
-                VStack(alignment: .leading, spacing: 2) {
-                    HStack(alignment: .firstTextBaseline, spacing: 8) {
-                        if let title = program.name {
-                            Text(title)
-                                .font(.title3)
-                                .bold()
-                        }
-                        if showNew {
-                            Text("New")
-                                .font(.caption2)
-                                .foregroundColor(.white)
-                                .padding(.horizontal, 6)
-                                .padding(.vertical, 2)
-                                .background(Color.blue)
-                                .cornerRadius(4)
-                        }
-                        if program.episodeTitle == nil, let rating = program.officialRating {
-                            Text(rating)
-                                .font(.caption)
-                                .padding(.horizontal, 10)
-                                .padding(.vertical, 6)
-                                .background(Color.gray.opacity(0.2))
-                                .clipShape(Capsule())
-                        }
-                    }
-                    // Show subtitle even if only episode title or only season/episode exists
-                    let subtitleText: String? = {
-                        let seasonEpisode: String? = {
-                            if let season = program.parentIndexNumber, let episodeNum = program.indexNumber {
-                                return String(format: "S%02dE%02d", season, episodeNum)
-                            }
-                            return nil
-                        }()
-                        if let epTitle = program.episodeTitle, !epTitle.isEmpty {
-                            if let seasonEpisode = seasonEpisode {
-                                return "\(seasonEpisode) • \(epTitle)"
-                            } else {
-                                return epTitle
-                            }
-                        } else {
-                            return seasonEpisode
-                        }
-                    }()
-                    if let subtitleText = subtitleText {
-                        Text(subtitleText)
-                            .font(.subheadline)
-                            .foregroundColor(.secondary)
-                    }
-                    // Move time box here, under title/subtitle
-                    if let start = program.startDate, let end = program.endDate {
-                        Text("\(start.formatted(date: .omitted, time: .shortened)) - \(end.formatted(date: .omitted, time: .shortened))")
-                            .font(.subheadline)
-                            .foregroundColor(.primary) // Changed from .blue to .primary
-                    }
-                    if let rating = program.officialRating, program.episodeTitle != nil {
-                        Text(rating)
-                            .font(.caption)
-                            .padding(.horizontal, 10)
-                            .padding(.vertical, 6)
-                            .background(Color.gray.opacity(0.2))
-                            .clipShape(Capsule())
-                    }
-                }
-                // Show overview always, not in a dropdown
-                let overviewText: String = {
-                    if let raw = program.overview?.trimmingCharacters(in: .whitespacesAndNewlines), !raw.isEmpty {
-                        return raw
-                    } else {
-                        return "No description available."
-                    }
-                }()
-                Text(overviewText)
-                    .font(.body)
-                    .foregroundColor(.secondary)
-            }
-            Spacer()
-            VStack(alignment: .trailing, spacing: 4) {
-                if isLive {
-                    HStack(spacing: 6) {
-                        Circle()
-                            .fill(Color.red)
-                            .frame(width: 12, height: 12)
-                        Text("LIVE")
-                            .font(.subheadline)
-                            .foregroundColor(.red)
-                    }                }
-                // Play button removed from here
-            }
-        }
-    }
-}
-
-// Make URL Identifiable for use with .fullScreenCover(item:)
-extension URL: Identifiable {
-    public var id: String { absoluteString }
-}
-
-extension JFProgram {
-    init?(epg base: BaseItemDto, channelName: String?, channelId: String?) {
-        guard let bid = base.id else { return nil }
-        self.id = bid
-        self.name = base.name ?? ""
-        self.overview = base.overview
-        self.startDate = base.startDate
-        self.endDate = base.endDate
-        self.channelId = channelId
-        self.channelName = channelName
-        // Heuristics
-        self.isMovie = false
-        self.isSeries = (base.episodeTitle != nil) || (base.parentIndexNumber != nil)
-        self.isNews = false
-        self.isSports = false
-        self.isKids = false
-        self.episodeTitle = base.episodeTitle
-        self.seriesName = nil
-        self.runTimeTicks = nil
-        self.officialRating = base.officialRating
-        self.genres = nil
-        self.parentIndexNumber = base.parentIndexNumber
-        self.indexNumber = base.indexNumber
-    }
-}
diff --git a/LiveFin/App Specific/ChannelsView.swift b/LiveFin/App Specific/ChannelsView.swift
deleted file mode 100644
index bc6755d..0000000
--- a/LiveFin/App Specific/ChannelsView.swift	
+++ /dev/null
@@ -1,222 +0,0 @@
-//
-//  ChannelsView.swift
-//  LiveFin
-//
-//  Created by KPGamingz on 4/12/25.
-//
-
-import SwiftUI
-import Foundation
-
-struct LiveTvChannelDto: Codable, Identifiable {
-    let id: String
-    let name: String?
-    let number: String?
-    let startDate: Date?
-    let endDate: Date?
-    let baseURL: String
-    var currentProgram: BaseItemDto?
-
-    var streamUrl: String { "/LiveTv/LiveStream?channelId=\(id)" }
-
-    enum CodingKeys: String, CodingKey {
-        case id = "Id"
-        case name = "Name"
-        case number = "Number"
-        case startDate = "StartDate"
-        case endDate = "EndDate"
-    }
-
-    init(id: String, name: String?, number: String?, startDate: Date?, endDate: Date?, baseURL: String) {
-        self.id = id
-        self.name = name
-        self.number = number
-        self.startDate = startDate
-        self.endDate = endDate
-        self.baseURL = baseURL
-        self.currentProgram = nil
-    }
-
-    init(from decoder: Decoder) throws {
-        let container = try decoder.container(keyedBy: CodingKeys.self)
-        self.id = try container.decode(String.self, forKey: .id)
-        self.name = try container.decodeIfPresent(String.self, forKey: .name)
-        self.number = try container.decodeIfPresent(String.self, forKey: .number)
-        self.startDate = try container.decodeIfPresent(Date.self, forKey: .startDate)
-        self.endDate = try container.decodeIfPresent(Date.self, forKey: .endDate)
-        self.baseURL = "YOUR_SERVER_BASE_URL"
-        self.currentProgram = nil
-    }
-
-    func encode(to encoder: Encoder) throws {
-        var container = encoder.container(keyedBy: CodingKeys.self)
-        try container.encode(id, forKey: .id)
-        try container.encodeIfPresent(name, forKey: .name)
-        try container.encodeIfPresent(number, forKey: .number)
-        try container.encodeIfPresent(startDate, forKey: .startDate)
-        try container.encodeIfPresent(endDate, forKey: .endDate)
-    }
-}
-
-struct ChannelRowView: View {
-    let channel: LiveTvChannelDto
-    let baseURL: String
-    let apiKey: String
-
-    var body: some View {
-        HStack {
-            ChannelImageView(baseUrl: baseURL, apiKey: apiKey, channelId: channel.id)
-                .frame(width: 50, height: 50)
-                .id(channel.id)
-            VStack(alignment: .leading) {
-                Text(channel.name ?? "Unnamed Channel")
-                    .font(.headline)
-                if let number = channel.number {
-                    HStack(spacing: 4) {
-                        Text("Channel \(number)")
-                            .font(.subheadline)
-                            .foregroundColor(.gray)
-                        if let currentProgram = channel.currentProgram {
-                            Text("• \(currentProgram.name ?? "No program")")
-                                .font(.subheadline)
-                                .foregroundColor(.red)
-                                .lineLimit(1)
-                                .truncationMode(.tail)
-                        }
-                    }
-                }
-                if let startDate = channel.startDate, let endDate = channel.endDate {
-                    Text("\(startDate.formatted(date: .abbreviated, time: .shortened)) - \(endDate.formatted(date: .abbreviated, time: .shortened))")
-                        .font(.subheadline)
-                        .foregroundColor(.gray)
-                }
-            }
-        }
-    }
-}
-
-struct ChannelsView: View {
-    @EnvironmentObject var appState: AppState
-    @State private var channels: [LiveTvChannelDto] = []
-    @State private var isLoading = false
-    @State private var error: String?
-
-    // Natural sort helpers
-    private func channelNumericComponents(_ number: String?) -> [Int] {
-        guard let number, !number.isEmpty else { return [Int.max] }
-        let parts = number.split { !$0.isNumber }
-        if parts.isEmpty { return [Int.max] }
-        return parts.map { Int($0) ?? Int.max }
-    }
-    private func channelLessThan(_ a: LiveTvChannelDto, _ b: LiveTvChannelDto) -> Bool {
-        let aNum = a.number ?? ""
-        let bNum = b.number ?? ""
-        let aHas = !aNum.isEmpty
-        let bHas = !bNum.isEmpty
-        if aHas != bHas { return aHas }
-        let ac = channelNumericComponents(aNum)
-        let bc = channelNumericComponents(bNum)
-        if ac != bc { return ac.lexicographicallyPrecedes(bc) }
-        return (a.name ?? "") < (b.name ?? "")
-    }
-
-    var body: some View {
-        NavigationStack {
-            List {
-                if isLoading {
-                    ProgressView()
-                } else if let error = error {
-                    Text("Error: \(error)").foregroundColor(.red)
-                } else if channels.isEmpty {
-                    Text("No channels available.")
-                } else {
-                    ForEach(channels, id: \.id) { channel in
-                        NavigationLink(destination: ChannelDetailView(channel: channel)) {
-                            ChannelRowView(channel: channel, baseURL: appState.serverURL, apiKey: appState.apiKey)
-                        }
-                    }
-                }
-            }
-            .id("channelsList")
-            .refreshable { await fetchChannels(force: true) }
-            .navigationTitle("Channels")
-            .onAppear {
-                if channels.isEmpty {
-                    loadChannelsFromCache()
-                    Task { await fetchChannels(force: false) }
-                }
-            }
-        }
-    }
-
-    struct ChannelsResponse: Codable { let items: [LiveTvChannelDto]?; enum CodingKeys: String, CodingKey { case items = "Items" } }
-    struct ProgramsResponse: Codable { let items: [BaseItemDto]?; enum CodingKeys: String, CodingKey { case items = "Items" } }
-
-    func fetchChannels(force: Bool = false) async {
-        guard let client = appState.client else { error = "Client not initialized"; return }
-        guard !appState.accessToken.isEmpty else { error = "Access token is missing"; return }
-        if !force && !channels.isEmpty { return }
-        isLoading = true; error = nil; defer { isLoading = false }
-        do {
-            let url = client.configuration.url.appendingPathComponent("/LiveTv/Channels")
-            var request = URLRequest(url: url)
-            request.httpMethod = "GET"
-            request.setValue("application/json", forHTTPHeaderField: "Content-Type")
-            request.setValue(appState.accessToken, forHTTPHeaderField: "X-Emby-Token")
-            let (data, response) = try await URLSession.shared.data(for: request)
-            if let http = response as? HTTPURLResponse, http.statusCode != 200 { error = "Failed to fetch channels. Status code: \(http.statusCode)"; return }
-            let decoded = try JSONDecoder().decode(ChannelsResponse.self, from: data)
-            let loaded = decoded.items ?? []
-            self.channels = loaded.sorted(by: channelLessThan)
-            await fetchCurrentPrograms(for: channels)
-            await saveChannelsToCache()
-        } catch { self.error = error.localizedDescription }
-    }
-
-    func fetchCurrentPrograms(for channels: [LiveTvChannelDto]) async {
-        guard let client = appState.client else { return }
-        let now = Date(); let later = Calendar.current.date(byAdding: .hour, value: 1, to: now)!
-        let programURL = client.configuration.url.appendingPathComponent("/LiveTv/Programs")
-        var components = URLComponents(url: programURL, resolvingAgainstBaseURL: false)
-        components?.queryItems = [
-            URLQueryItem(name: "startDate", value: ISO8601DateFormatter().string(from: now)),
-            URLQueryItem(name: "endDate", value: ISO8601DateFormatter().string(from: later)),
-            URLQueryItem(name: "IsAiring", value: "true")
-        ]
-        guard let finalURL = components?.url else { return }
-        var request = URLRequest(url: finalURL)
-        request.setValue("application/json", forHTTPHeaderField: "Content-Type")
-        request.setValue(appState.accessToken, forHTTPHeaderField: "X-Emby-Token")
-        do {
-            let (data, _) = try await URLSession.shared.data(for: request)
-            let programResponse = try JSONDecoder().decode(ProgramsResponse.self, from: data)
-            let currentPrograms = programResponse.items ?? []
-            var updated: [LiveTvChannelDto] = []
-            for var channel in channels {
-                if let currentProgram = currentPrograms.first(where: { $0.channelId == channel.id }) { channel.currentProgram = currentProgram }
-                updated.append(channel)
-            }
-            self.channels = updated.sorted(by: channelLessThan)
-        } catch { print("Error fetching current programs: \(error)") }
-    }
-
-    func saveChannelsToCache() async {
-        do { let data = try JSONEncoder().encode(channels); let url = try getCacheFileURL(); try data.write(to: url, options: [.atomic]) } catch { print("Failed to save channels to cache: \(error.localizedDescription)") }
-    }
-
-    func loadChannelsFromCache() {
-        do {
-            let url = try getCacheFileURL()
-            let data = try Data(contentsOf: url)
-            let cached = try JSONDecoder().decode([LiveTvChannelDto].self, from: data)
-            self.channels = cached.sorted(by: channelLessThan)
-            Task { await fetchCurrentPrograms(for: cached) }
-        } catch { print("Failed to load channels from cache: \(error.localizedDescription)") }
-    }
-
-    func getCacheFileURL() throws -> URL {
-        let fm = FileManager.default
-        let docsURL = try fm.url(for: .documentDirectory, in: .userDomainMask, appropriateFor: nil, create: true)
-        return docsURL.appendingPathComponent("cachedChannels.json")
-    }
-}
diff --git a/LiveFin/App Specific/GuideView.swift b/LiveFin/App Specific/GuideView.swift
deleted file mode 100644
index f79cb45..0000000
--- a/LiveFin/App Specific/GuideView.swift	
+++ /dev/null
@@ -1,817 +0,0 @@
-//
-//  GuideView.swift
-//  LiveFin
-//
-//  Created by KPGamingz on 5/13/25.
-//
-
-import SwiftUI
-
-struct GuideView: View {
-    @EnvironmentObject var appState: AppState
-    @State private var channels: [LiveTvChannelDto] = []
-    @State private var programs: [BaseItemDto] = []
-    @State private var error: String?
-    @State private var isLoading = true
-    @State private var selectedDate: Date = Date()
-    @State private var cacheURL: URL = {
-        let urls = FileManager.default.urls(for: .cachesDirectory, in: .userDomainMask)
-        return urls[0].appendingPathComponent("cached_programs.json")
-    }()
-    @StateObject private var homeVM = HomeViewModel() // Added for ProgramView environment
-
-    // EPG grid constants
-    let minutesPerPixel: CGFloat = 4.0 // pixels per minute (30‑min slot = 120px)
-    let timeBarHeight: CGFloat = 24 // reduced from 32
-    let channelColWidth: CGFloat = 130 // shrunk from 150
-    let programRowHeight: CGFloat = 56 // slightly reduced from 64 for a denser layout
-    let timeWindowHours: Int = 24 // expanded to full day
-    let prefetchHours: Int = 2
-    let prefetchTriggerRemainingHours: Int = 3
-
-    @State private var programCache: [Date: [BaseItemDto]] = [:] // dayStart -> programs
-    @State private var prefetchedEarly: [Date: [BaseItemDto]] = [:] // next day start -> early programs slice
-    @State private var loadedDayStarts: Set<Date> = []
-    @State private var timelineStart: Date? = nil
-    @State private var timelineEnd: Date? = nil // exclusive end
-
-    @State private var horizontalOffset: CGFloat = 0 // track horizontal scroll for pinned time bar (unused now)
-    @State private var isProgrammaticDateChange: Bool = false // added
-    @State private var didInitialLoad: Bool = false
-
-    // Channel cache URL (documents directory)
-    private var channelCacheURL: URL {
-        let fm = FileManager.default
-        let docs = try? fm.url(for: .documentDirectory, in: .userDomainMask, appropriateFor: nil, create: true)
-        return (docs ?? URL(fileURLWithPath: NSTemporaryDirectory())).appendingPathComponent("cachedChannels.json")
-    }
-
-    private var dateRange: ClosedRange<Date> {
-        let cal = Calendar.current
-        let today = cal.startOfDay(for: Date())
-        let max = cal.date(byAdding: .day, value: 14, to: today) ?? today
-        return today...max
-    }
-
-    @AppStorage("GuideLastSelectedDate") private var lastSelectedDateInterval: Double = Date().timeIntervalSince1970
-
-    var body: some View {
-        NavigationStack {
-            VStack(spacing: 0) {
-                // Date picker + day navigation
-                HStack(spacing: 8) { // reduced spacing and vertical padding
-                    Button(action: { shiftDay(-1) }) {
-                        Image(systemName: "chevron.left")
-                            .imageScale(.small)
-                            .font(.body)
-                    }
-                    .disabled(!canShift(-1))
-
-                    DatePicker("", selection: $selectedDate, in: dateRange, displayedComponents: .date)
-                        .datePickerStyle(.compact)
-                        .labelsHidden()
-                        .frame(maxWidth: 160)
-                        .scaleEffect(0.95)
-
-                    Button(action: { shiftDay(1) }) {
-                        Image(systemName: "chevron.right")
-                            .imageScale(.small)
-                            .font(.body)
-                    }
-                    .disabled(!canShift(1))
-
-                    Spacer(minLength: 0)
-                }
-                .padding(.horizontal, 12)
-                .padding(.vertical, 2)
-                .frame(height: 32)
-                .background(Color(UIColor.secondarySystemBackground))
-
-                if isLoading {
-                    ProgressView("Loading TV Guide...")
-                        .frame(maxWidth: .infinity, maxHeight: .infinity)
-                } else if let error = error {
-                    Text("Error: \(error)")
-                        .foregroundColor(.red)
-                        .frame(maxWidth: .infinity, maxHeight: .infinity)
-                } else if channels.isEmpty {
-                    Text("No channels available")
-                        .foregroundColor(.secondary)
-                        .frame(maxWidth: .infinity, maxHeight: .infinity)
-                } else if let tStart = timelineStart, let tEnd = timelineEnd {
-                    EPGGridView(
-                        channels: channels,
-                        programs: aggregatedPrograms,
-                        timelineStart: tStart,
-                        timelineEnd: tEnd,
-                        minutesPerPixel: minutesPerPixel,
-                        timeBarHeight: timeBarHeight,
-                        channelColWidth: channelColWidth,
-                        programRowHeight: programRowHeight,
-                        requestMore: loadNextDayIfPossible,
-                        onHorizontalOffsetChange: handleHorizontalOffset(_:),
-                        refreshAction: refreshEPG
-                    )
-                    .environmentObject(appState)
-                    .environmentObject(homeVM)
-                }
-            }
-            .navigationTitle("TV Guide")
-            .task { if !didInitialLoad { await initialGuideLoad() } }
-            .onAppear {
-                // Restore last selected date if within range
-                let restored = Date(timeIntervalSince1970: lastSelectedDateInterval)
-                if dateRange.contains(restored) { selectedDate = restored }
-            }
-            .onChange(of: selectedDate) { newValue in
-                lastSelectedDateInterval = newValue.timeIntervalSince1970
-                if isProgrammaticDateChange {
-                    isProgrammaticDateChange = false
-                } else {
-                    Task { await focusOnSelectedDate(newValue) }
-                }
-            }
-        }
-    }
-
-    private var aggregatedPrograms: [BaseItemDto] {
-        var all: [BaseItemDto] = []
-        for day in loadedDayStarts.sorted() { if let list = programCache[day] { all.append(contentsOf: list) } }
-        return all
-    }
-
-    func loadCachedPrograms() {
-        do {
-            let data = try Data(contentsOf: cacheURL)
-            let decoder = JSONDecoder()
-            decoder.dateDecodingStrategy = .iso8601
-            let response = try decoder.decode(ProgramsResponse.self, from: data)
-            self.programs = response.items ?? []
-        } catch {
-            // Ignore cache errors
-        }
-    }
-
-    func saveProgramsToCache(_ programs: [BaseItemDto]) {
-        do {
-            let encoder = JSONEncoder()
-            encoder.dateEncodingStrategy = .iso8601
-            let response = ProgramsResponse(items: programs)
-            let data = try encoder.encode(response)
-            try data.write(to: cacheURL)
-        } catch {
-            // Ignore cache errors
-        }
-    }
-
-    func loadGuideData() async {
-        print("GuideView: Starting loadGuideData")
-        loadCachedPrograms()
-        isLoading = true
-        error = nil
-
-        guard let client = appState.client else {
-            print("GuideView: appState.client is nil")
-            error = "Client not initialized"
-            isLoading = false
-            return
-        }
-        print("GuideView: appState.client is set")
-        print("GuideView: appState.accessToken is \(appState.accessToken)")
-
-        let cal = Calendar.current
-        let dayStart = cal.startOfDay(for: selectedDate)
-        let diskURL = cacheFileURL(for: dayStart)
-        // Memory cache
-        if let cached = programCache[dayStart] {
-            print("GuideView: Using memory cache for \(dayStart)")
-            self.programs = cached
-            self.isLoading = false
-            Task { await prefetchNextDayIfNeeded() }
-            return
-        }
-        // Disk cache
-        if let data = try? Data(contentsOf: diskURL) {
-            let dec = JSONDecoder(); dec.dateDecodingStrategy = .iso8601
-            if let resp = try? dec.decode(ProgramsResponse.self, from: data) {
-                print("GuideView: Using disk cache for \(dayStart)")
-                let items = resp.items ?? []
-                self.programs = items
-                self.programCache[dayStart] = items
-                self.isLoading = false
-                Task { await prefetchNextDayIfNeeded() }
-                return
-            }
-        }
-
-        let fullDayEnd = cal.date(byAdding: .day, value: 1, to: dayStart) ?? dayStart.addingTimeInterval(86400)
-        let now = Date()
-        let isToday = cal.isDate(selectedDate, inSameDayAs: now)
-        let fetchStart = dayStart // always fetch entire day for better caching
-        let fetchEnd = fullDayEnd
-
-        do {
-            // Fetch channels
-            let channelURL = client.configuration.url.appendingPathComponent("/LiveTv/Channels")
-            var channelRequest = URLRequest(url: channelURL)
-            channelRequest.setValue("application/json", forHTTPHeaderField: "Content-Type")
-            channelRequest.setValue(appState.accessToken, forHTTPHeaderField: "X-Emby-Token")
-            let (channelData, _) = try await URLSession.shared.data(for: channelRequest)
-            let channelResponse = try JSONDecoder().decode(ChannelsResponse.self, from: channelData)
-            self.channels = channelResponse.items ?? []
-            print("GuideView: Loaded \(self.channels.count) channels")
-
-            // Fetch programs
-            let programURL = client.configuration.url.appendingPathComponent("/LiveTv/Programs")
-            var components = URLComponents(url: programURL, resolvingAgainstBaseURL: false)
-            let iso = ISO8601DateFormatter()
-            components?.queryItems = [
-                URLQueryItem(name: "startDate", value: iso.string(from: fetchStart)),
-                URLQueryItem(name: "endDate", value: iso.string(from: fetchEnd)),
-                URLQueryItem(name: "EnableImages", value: "false"),
-                URLQueryItem(name: "EnableUserData", value: "false")
-            ]
-            guard let finalURL = components?.url else {
-                print("GuideView: Invalid programs URL")
-                error = "Invalid programs URL"
-                isLoading = false
-                return
-            }
-            print("GuideView: Fetching programs from \(finalURL)")
-            var programRequest = URLRequest(url: finalURL)
-            programRequest.setValue("application/json", forHTTPHeaderField: "Content-Type")
-            programRequest.setValue(appState.accessToken, forHTTPHeaderField: "X-Emby-Token")
-            let (programData, _) = try await URLSession.shared.data(for: programRequest)
-
-            let decoder = JSONDecoder()
-            decoder.dateDecodingStrategy = .custom { d in
-                let c = try d.singleValueContainer()
-                let s = try c.decode(String.self)
-                let frac = ISO8601DateFormatter(); frac.formatOptions = [.withInternetDateTime, .withFractionalSeconds]
-                if let dt = frac.date(from: s) { return dt }
-                let basic = ISO8601DateFormatter(); if let dt2 = basic.date(from: s) { return dt2 }
-                throw DecodingError.dataCorruptedError(in: c, debugDescription: "Unable to parse date: \(s)")
-            }
-            let programResponse = try decoder.decode(ProgramsResponse.self, from: programData)
-            self.programs = programResponse.items ?? []
-            self.programCache[dayStart] = self.programs
-            let enc = JSONEncoder(); enc.dateEncodingStrategy = .iso8601
-            if let data = try? enc.encode(programResponse) { try? data.write(to: diskURL) }
-            print("GuideView: Loaded programs: \(self.programs.count)")
-            saveProgramsToCache(self.programs)
-        } catch {
-            print("GuideView: Error loading guide data: \(error)")
-            self.error = error.localizedDescription
-        }
-
-        isLoading = false
-        print("GuideView: Finished loadGuideData")
-        Task { await prefetchNextDayIfNeeded() }
-    }
-
-    // Prefetch logic for early next day hours
-    private func prefetchNextDayIfNeeded() async {
-        let cal = Calendar.current
-        let todayStart = dayStart(Date())
-        guard cal.isDate(selectedDate, inSameDayAs: Date()) else { return } // only when viewing today
-        let now = Date()
-        let endOfDay = cal.date(byAdding: .day, value: 1, to: todayStart) ?? todayStart.addingTimeInterval(86400)
-        let remaining = endOfDay.timeIntervalSince(now)
-        let trigger = Double(prefetchTriggerRemainingHours) * 3600.0
-        guard remaining <= trigger else { return }
-        let nextStart = cal.date(byAdding: .day, value: 1, to: todayStart) ?? todayStart.addingTimeInterval(86400)
-        if prefetchedEarly[nextStart] != nil { return } // already prefetched
-        guard let client = appState.client else { return }
-        print("GuideView: Prefetching early hours for next day")
-        let nextEnd = cal.date(byAdding: .hour, value: prefetchHours, to: nextStart) ?? nextStart.addingTimeInterval(Double(prefetchHours) * 3600)
-        let iso = ISO8601DateFormatter()
-        let programURL = client.configuration.url.appendingPathComponent("/LiveTv/Programs")
-        var components = URLComponents(url: programURL, resolvingAgainstBaseURL: false)
-        components?.queryItems = [
-            URLQueryItem(name: "startDate", value: iso.string(from: nextStart)),
-            URLQueryItem(name: "endDate", value: iso.string(from: nextEnd)),
-            URLQueryItem(name: "EnableImages", value: "false"),
-            URLQueryItem(name: "EnableUserData", value: "false")
-        ]
-        guard let url = components?.url else { return }
-        var req = URLRequest(url: url)
-        req.setValue("application/json", forHTTPHeaderField: "Content-Type")
-        req.setValue(appState.accessToken, forHTTPHeaderField: "X-Emby-Token")
-        do {
-            let (data, _) = try await URLSession.shared.data(for: req)
-            let dec = JSONDecoder()
-            dec.dateDecodingStrategy = .custom { d in
-                let c = try d.singleValueContainer(); let s = try c.decode(String.self)
-                let f1 = ISO8601DateFormatter(); f1.formatOptions = [.withInternetDateTime, .withFractionalSeconds]
-                if let dt = f1.date(from: s) { return dt }
-                let f2 = ISO8601DateFormatter(); if let dt2 = f2.date(from: s) { return dt2 }
-                throw DecodingError.dataCorruptedError(in: c, debugDescription: "Cannot parse date: \(s)")
-            }
-            if let resp = try? dec.decode(ProgramsResponse.self, from: data) {
-                DispatchQueue.main.async {
-                    self.prefetchedEarly[nextStart] = resp.items ?? []
-                }
-            }
-        } catch {
-            print("GuideView: Prefetch failed: \(error)")
-        }
-    }
-
-    func shiftDay(_ delta: Int) {
-        let cal = Calendar.current
-        let newDate = cal.date(byAdding: .day, value: delta, to: selectedDate) ?? selectedDate
-        if dateRange.contains(newDate) { selectedDate = newDate }
-    }
-    func canShift(_ delta: Int) -> Bool {
-        let cal = Calendar.current
-        let newDate = cal.date(byAdding: .day, value: delta, to: selectedDate) ?? selectedDate
-        return dateRange.contains(newDate)
-    }
-
-    // MARK: - Day Helpers
-    private func dayStart(_ date: Date) -> Date { Calendar.current.startOfDay(for: date) }
-    private func cacheFileURL(for day: Date) -> URL {
-        let df = DateFormatter(); df.dateFormat = "yyyyMMdd"; df.timeZone = .current
-        return cacheURL.deletingLastPathComponent().appendingPathComponent("cached_programs_\(df.string(from: day)).json")
-    }
-
-    // Determine the timeline's visible start for a given selected date.
-    // For past or future days: start at midnight. For today: start at either midnight
-    // or slightly before 'now' (rounded down to the previous 30‑minute mark) to provide context.
-    private func trimmedStart(for date: Date) -> Date {
-        let cal = Calendar.current
-        let startOfDay = cal.startOfDay(for: date)
-        guard cal.isDateInToday(date) else { return startOfDay }
-        let now = Date()
-        // If we're earlier than startOfDay (unlikely) just return startOfDay
-        guard now > startOfDay else { return startOfDay }
-        // Round down to nearest 30 minutes for a cleaner grid alignment
-        let comps = cal.dateComponents([.year, .month, .day, .hour, .minute], from: now)
-        let minute = comps.minute ?? 0
-        let roundedMinute = minute - (minute % 30)
-        var rounded = DateComponents()
-        rounded.year = comps.year; rounded.month = comps.month; rounded.day = comps.day
-        rounded.hour = comps.hour; rounded.minute = roundedMinute
-        let aligned = cal.date(from: rounded) ?? startOfDay
-        // Provide a small earlier context window (up to 30 min) but never before midnight
-        return max(startOfDay, aligned)
-    }
-
-    private var nextDayEarlyPrograms: [BaseItemDto] {
-        let start = dayStart(selectedDate)
-        let next = Calendar.current.date(byAdding: .day, value: 1, to: start) ?? start
-        return prefetchedEarly[next] ?? []
-    }
-
-    private func initializeForSelectedDate() async {
-        // legacy refresh path retained for pull-to-refresh (full reload)
-        isLoading = true
-        error = nil
-        let cal = Calendar.current
-        let day = cal.startOfDay(for: selectedDate)
-        loadedDayStarts.removeAll()
-        programCache.removeAll()
-        prefetchedEarly.removeAll()
-        timelineStart = trimmedStart(for: selectedDate)
-        timelineEnd = cal.date(byAdding: .day, value: 1, to: day)
-        await ensureChannelsLoaded()
-        await loadDay(day)
-        isLoading = false
-    }
-
-    private func initialGuideLoad() async {
-        isLoading = true
-        error = nil
-        let cal = Calendar.current
-        let day = cal.startOfDay(for: selectedDate)
-        timelineStart = trimmedStart(for: selectedDate)
-        timelineEnd = cal.date(byAdding: .day, value: 1, to: day)
-        await ensureChannelsLoaded()
-        await loadDay(day)
-        didInitialLoad = true
-        isLoading = false
-    }
-
-    private func focusOnSelectedDate(_ date: Date) async {
-        // Do not clear caches, just adjust visible window and load missing day
-        let cal = Calendar.current
-        let day = cal.startOfDay(for: date)
-        if cal.isDateInToday(day) {
-            timelineStart = trimmedStart(for: day)
-        } else {
-            timelineStart = day
-        }
-        timelineEnd = cal.date(byAdding: .day, value: 1, to: day)
-        await loadDay(day)
-    }
-
-    func loadChannelsFromCache() {
-        guard channels.isEmpty else { return }
-        if let data = try? Data(contentsOf: channelCacheURL) {
-            if let decoded = try? JSONDecoder().decode([LiveTvChannelDto].self, from: data) {
-                channels = decoded
-            }
-        }
-    }
-    private func saveChannelsToCache() {
-        guard !channels.isEmpty else { return }
-        if let data = try? JSONEncoder().encode(channels) { try? data.write(to: channelCacheURL, options: [.atomic]) }
-    }
-
-    private func ensureChannelsLoaded() async {
-        if !channels.isEmpty { return }
-        loadChannelsFromCache() // attempt cache first
-        if !channels.isEmpty { return }
-        guard let client = appState.client else { return }
-        let channelURL = client.configuration.url.appendingPathComponent("/LiveTv/Channels")
-        var req = URLRequest(url: channelURL)
-        req.setValue("application/json", forHTTPHeaderField: "Content-Type")
-        req.setValue(appState.accessToken, forHTTPHeaderField: "X-Emby-Token")
-        do {
-            let (data, _) = try await URLSession.shared.data(for: req)
-            let resp = try JSONDecoder().decode(ChannelsResponse.self, from: data)
-            await MainActor.run {
-                self.channels = resp.items ?? []
-                self.saveChannelsToCache()
-            }
-        } catch {
-            await MainActor.run { self.error = error.localizedDescription }
-        }
-    }
-
-    // MARK: - Day Loading
-    private func loadDay(_ dayStart: Date) async {
-        let cal = Calendar.current
-        if loadedDayStarts.contains(dayStart) { return }
-        guard let client = appState.client else { return }
-        let dayEnd = cal.date(byAdding: .day, value: 1, to: dayStart) ?? dayStart.addingTimeInterval(86400)
-        // Memory cache check (already handled but keep structure)
-        if programCache[dayStart] != nil { loadedDayStarts.insert(dayStart); return }
-        // Disk cache
-        let disk = cacheFileURL(for: dayStart)
-        if let data = try? Data(contentsOf: disk) {
-            let dec = JSONDecoder(); dec.dateDecodingStrategy = .iso8601
-            if let resp = try? dec.decode(ProgramsResponse.self, from: data) {
-                programCache[dayStart] = resp.items ?? []
-                loadedDayStarts.insert(dayStart)
-                return
-            }
-        }
-        // Network fetch
-        let iso = ISO8601DateFormatter()
-        let url = client.configuration.url.appendingPathComponent("/LiveTv/Programs")
-        var comp = URLComponents(url: url, resolvingAgainstBaseURL: false)
-        comp?.queryItems = [
-            URLQueryItem(name: "startDate", value: iso.string(from: dayStart)),
-            URLQueryItem(name: "endDate", value: iso.string(from: dayEnd)),
-            URLQueryItem(name: "EnableImages", value: "false"),
-            URLQueryItem(name: "EnableUserData", value: "false")
-        ]
-        guard let final = comp?.url else { return }
-        var req = URLRequest(url: final)
-        req.setValue("application/json", forHTTPHeaderField: "Content-Type")
-        req.setValue(appState.accessToken, forHTTPHeaderField: "X-Emby-Token")
-        do {
-            let (data, _) = try await URLSession.shared.data(for: req)
-            let dec = JSONDecoder()
-            dec.dateDecodingStrategy = .custom { d in
-                let c = try d.singleValueContainer(); let s = try c.decode(String.self)
-                let f1 = ISO8601DateFormatter(); f1.formatOptions = [.withInternetDateTime, .withFractionalSeconds]
-                if let dt = f1.date(from: s) { return dt }
-                let f2 = ISO8601DateFormatter(); if let dt2 = f2.date(from: s) { return dt2 }
-                throw DecodingError.dataCorruptedError(in: c, debugDescription: "Cannot parse date: \(s)")
-            }
-            if let resp = try? dec.decode(ProgramsResponse.self, from: data) {
-                programCache[dayStart] = resp.items ?? []
-                let enc = JSONEncoder(); enc.dateEncodingStrategy = .iso8601
-                if let out = try? enc.encode(resp) { try? out.write(to: disk) }
-            }
-        } catch {
-            print("GuideView: loadDay error: \(error)")
-        }
-        loadedDayStarts.insert(dayStart)
-    }
-
-    private func loadNextDayIfPossible() {
-        Task { await _loadNextDayIfPossible() }
-    }
-    private func _loadNextDayIfPossible() async {
-        guard let tEnd = timelineEnd else { return }
-        let cal = Calendar.current
-        var nextDayStart = cal.startOfDay(for: tEnd)
-        // Try up to 7 days ahead to find a day with data (avoid forcing empty days)
-        for _ in 0..<7 {
-            await loadDay(nextDayStart)
-            let hasData = (programCache[nextDayStart]?.isEmpty == false)
-            if hasData {
-                if let newEnd = cal.date(byAdding: .day, value: 1, to: nextDayStart) {
-                    timelineEnd = newEnd
-                }
-                break
-            } else {
-                // If no data, advance without extending timelineEnd (skip empty day visually)
-                if let candidate = cal.date(byAdding: .day, value: 1, to: nextDayStart) { nextDayStart = candidate } else { break }
-            }
-        }
-    }
-
-    private func handleHorizontalOffset(_ offset: CGFloat) { // added
-        guard let start = timelineStart else { return }
-        // convert horizontal pixel offset to minutes from timelineStart
-        let minutesF = max(0, offset / minutesPerPixel)
-        let minutes = Int(minutesF.rounded(.down))
-        if let scrolledDate = Calendar.current.date(byAdding: .minute, value: minutes, to: start) {
-            let day = Calendar.current.startOfDay(for: scrolledDate)
-            if day != Calendar.current.startOfDay(for: selectedDate) && loadedDayStarts.contains(day) {
-                isProgrammaticDateChange = true
-                selectedDate = day
-            }
-        }
-    }
-
-    // Pull-to-refresh action (re-fetch just current day without clearing other cached days)
-    @MainActor
-    private func refreshEPG() async {
-        let cal = Calendar.current
-        let day = cal.startOfDay(for: selectedDate)
-        // Remove cached data for the current day only then reload that day
-        programCache[day] = nil
-        loadedDayStarts.remove(day)
-        await loadDay(day)
-    }
-}
-
-// Response model for Channels
-struct ChannelsResponse: Codable {
-    let items: [LiveTvChannelDto]?
-
-    enum CodingKeys: String, CodingKey {
-        case items = "Items"
-    }
-}
-
-// MARK: - EPG Grid View (restored simple version)
-struct EPGGridView: View {
-    let channels: [LiveTvChannelDto]
-    let programs: [BaseItemDto]
-    let timelineStart: Date
-    let timelineEnd: Date // exclusive
-    let minutesPerPixel: CGFloat
-    let timeBarHeight: CGFloat
-    let channelColWidth: CGFloat
-    let programRowHeight: CGFloat
-    let requestMore: () -> Void
-    let onHorizontalOffsetChange: (CGFloat) -> Void
-    let refreshAction: () async -> Void // added
-
-    var totalMinutes: CGFloat { CGFloat(Calendar.current.dateComponents([.minute], from: timelineStart, to: timelineEnd).minute ?? 0) }
-    var gridWidth: CGFloat { max(400, totalMinutes * minutesPerPixel) }
-    var programsByChannel: [String: [BaseItemDto]] { Dictionary(grouping: programs, by: { $0.channelId ?? "" }) }
-
-    var body: some View {
-        ScrollView(.vertical, showsIndicators: true) {
-            HStack(alignment: .top, spacing: 0) {
-                // Channel list + header spacer
-                VStack(spacing: 0) {
-                    Rectangle().fill(Color.clear).frame(width: channelColWidth, height: timeBarHeight)
-                    ForEach(channels, id: \.id) { channel in
-                        ChannelCell(channel: channel, width: channelColWidth, height: programRowHeight)
-                    }
-                }
-                .background(Color(UIColor.secondarySystemBackground))
-                .zIndex(2)
-
-                // Timeline scroller
-                ScrollView(.horizontal, showsIndicators: true) {
-                    ZStack(alignment: .topLeading) {
-                        VStack(spacing: 0) {
-                            TimeBarView(start: timelineStart, end: timelineEnd, width: gridWidth, minutesPerPixel: minutesPerPixel, height: timeBarHeight)
-                                .frame(width: gridWidth, height: timeBarHeight)
-                            ForEach(channels, id: \.id) { channel in
-                                ProgramRow(
-                                    channel: channel,
-                                    programs: programsByChannel[channel.id] ?? [],
-                                    start: timelineStart,
-                                    end: timelineEnd,
-                                    rowWidth: gridWidth,
-                                    minutesPerPixel: minutesPerPixel,
-                                    height: programRowHeight
-                                )
-                            }
-                            Color.clear.frame(width: 40, height: 1).onAppear { requestMore() }
-                        }
-                        GridLinesOverlay(start: timelineStart, end: timelineEnd, width: gridWidth, minutesPerPixel: minutesPerPixel, topOffset: 0, rowAreaHeight: CGFloat(channels.count) * programRowHeight + timeBarHeight)
-                            .allowsHitTesting(false)
-                        CurrentTimeIndicator(start: timelineStart, end: timelineEnd, width: gridWidth, minutesPerPixel: minutesPerPixel, channelColWidth: 0)
-                            .zIndex(3)
-                    }
-                }
-                .coordinateSpace(name: "EPGHorizontal")
-                .onPreferenceChange(HorizontalOffsetPreferenceKey.self) { value in
-                    onHorizontalOffsetChange(value)
-                }
-                .background(Color(UIColor.systemBackground))
-            }
-        }
-        .refreshable { await refreshAction() }
-    }
-}
-
-// New overlay for vertical grid lines
-private struct GridLinesOverlay: View {
-    let start: Date
-    let end: Date
-    let width: CGFloat
-    let minutesPerPixel: CGFloat
-    let topOffset: CGFloat
-    let rowAreaHeight: CGFloat
-    let slotMinutes: Int = 30
-    var body: some View {
-        let totalMinutes = end.timeIntervalSince(start) / 60.0
-        let totalSlots = Int(ceil(totalMinutes / Double(slotMinutes))) + 1
-        let slotWidth = CGFloat(slotMinutes) * minutesPerPixel
-        return ZStack(alignment: .topLeading) {
-            ForEach(0..<totalSlots, id: \.self) { i in
-                Rectangle()
-                    .fill(Color(UIColor.separator).opacity(0.18))
-                    .frame(width: 1)
-                    .offset(x: pixelAlign(CGFloat(i) * slotWidth), y: topOffset)
-                    .frame(height: rowAreaHeight, alignment: .top)
-            }
-        }
-    }
-}
-
-struct TimeBarView: View {
-    let start: Date
-    let end: Date
-    let width: CGFloat
-    let minutesPerPixel: CGFloat
-    let height: CGFloat // added
-    var body: some View {
-        let cal = Calendar.current
-        let slotMinutes = 30
-        let totalSlots = Int(width / (CGFloat(slotMinutes) * minutesPerPixel)) + 1
-        let slotWidth = CGFloat(slotMinutes) * minutesPerPixel
-        HStack(spacing: 0) {
-            ForEach(0..<totalSlots, id: \.self) { i in
-                let slotStart = cal.date(byAdding: .minute, value: i * slotMinutes, to: start) ?? start
-                if slotStart <= end {
-                    let label = DateFormatter.localizedString(from: slotStart, dateStyle: .none, timeStyle: .short)
-                    ZStack {
-                        Color(UIColor.tertiarySystemBackground)
-                        Text(label)
-                            .font(.caption2)
-                            .foregroundColor(.primary)
-                    }
-                    .frame(width: slotWidth, height: height)
-                }
-            }
-        }
-        .overlay(alignment: .bottomLeading) { Rectangle().fill(Color(UIColor.separator).opacity(0.4)).frame(height: 1) }
-    }
-}
-
-struct ChannelCell: View {
-    let channel: LiveTvChannelDto
-    let width: CGFloat
-    let height: CGFloat
-    @EnvironmentObject var appState: AppState
-    var body: some View {
-        HStack(spacing: 6) {
-            // Channel number
-            Text((channel.number as? String) ?? "") // safe cast handles optional/non-optional
-                .font(.caption2)
-                .foregroundColor(.secondary)
-                .frame(width: 28, alignment: .trailing)
-            // Logo
-            ChannelImageView(
-                baseUrl: appState.client?.configuration.url.absoluteString ?? "",
-                apiKey: appState.accessToken,
-                channelId: channel.id ?? ""
-            )
-            .frame(width: 36, height: 36)
-            .clipShape(RoundedRectangle(cornerRadius: 6))
-            // Name
-            Text(channel.name ?? "Unnamed")
-                .font(.subheadline)
-                .lineLimit(1)
-                .frame(maxWidth: .infinity, alignment: .leading)
-        }
-        .padding(.horizontal, 6)
-        .frame(width: width, height: height, alignment: .center)
-        .background(Color(UIColor.secondarySystemBackground))
-        .border(Color(UIColor.separator), width: 0.5)
-    }
-}
-
-// Fix ForEach in ProgramRow
-struct ProgramRow: View {
-    let channel: LiveTvChannelDto
-    let programs: [BaseItemDto]
-    let start: Date
-    let end: Date
-    let rowWidth: CGFloat
-    let minutesPerPixel: CGFloat
-    let height: CGFloat
-    @EnvironmentObject private var appState: AppState
-    @EnvironmentObject private var homeVM: HomeViewModel
-
-    var filteredPrograms: [(id: String, value: BaseItemDto, visibleStart: Date, visibleEnd: Date, durationMinutesExact: Double, offsetMinutesExact: Double)] {
-        programs.compactMap { prog in
-            guard let pid = prog.id, let s = prog.startDate, let e = prog.endDate else { return nil }
-            let vs = max(s, start)
-            let ve = min(e, end)
-            let durationSec = ve.timeIntervalSince(vs)
-            if durationSec <= 0 { return nil }
-            let offsetSec = vs.timeIntervalSince(start)
-            let durationMin = durationSec / 60.0
-            let offsetMin = offsetSec / 60.0
-            return (pid, prog, vs, ve, durationMin, offsetMin)
-        }
-    }
-
-    var body: some View {
-        ZStack(alignment: .leading) {
-            Color(UIColor.systemBackground)
-            ForEach(filteredPrograms, id: \.id) { item in
-                let rawWidth = CGFloat(item.durationMinutesExact) * minutesPerPixel
-                let rawOffset = CGFloat(item.offsetMinutesExact) * minutesPerPixel
-                let width = pixelAlign(rawWidth)
-                let offsetX = pixelAlign(rawOffset)
-                if width.isFinite && width > 0.5 {
-                    if let converted = JFProgram(epg: item.value, channelName: channel.name, channelId: channel.id) {
-                        NavigationLink(destination: ProgramView(program: converted)
-                            .environmentObject(appState)
-                            .environmentObject(homeVM)) {
-                                ProgramBlock(program: item.value, width: max(width - 1, 6), height: height)
-                        }
-                        .buttonStyle(.plain)
-                        .offset(x: max(offsetX, 0))
-                    } else {
-                        ProgramBlock(program: item.value, width: max(width - 1, 6), height: height)
-                            .offset(x: max(offsetX, 0))
-                    }
-                }
-            }
-        }
-        .frame(width: rowWidth, height: height)
-    }
-}
-
-struct ProgramBlock: View {
-    let program: BaseItemDto
-    let width: CGFloat
-    let height: CGFloat
-    var body: some View {
-        VStack(alignment: .leading, spacing: 2) {
-            Text(program.name ?? "Untitled")
-                .font(.caption)
-                .bold()
-                .lineLimit(1)
-            if let start = program.startDate, let end = program.endDate {
-                Text("\(start.formatted(date: .omitted, time: .shortened)) - \(end.formatted(date: .omitted, time: .shortened))")
-                    .font(.caption2)
-                    .foregroundColor(.gray)
-            }
-        }
-        .padding(.horizontal, 6)
-        .padding(.vertical, 4)
-        .frame(width: width, height: height - 4, alignment: .topLeading)
-        .background(Color.accentColor.opacity(0.14))
-        .clipShape(RoundedRectangle(cornerRadius: 6))
-        .overlay(
-            RoundedRectangle(cornerRadius: 6)
-                .stroke(Color.accentColor.opacity(0.22), lineWidth: 1)
-        )
-    }
-}
-
-struct CurrentTimeIndicator: View {
-    let start: Date
-    let end: Date
-    let width: CGFloat
-    let minutesPerPixel: CGFloat
-    let channelColWidth: CGFloat
-    var body: some View {
-        let now = Date()
-        return ZStack(alignment: .leading) {
-            if now >= start && now <= end {
-                let offsetMinutes = now.timeIntervalSince(start) / 60.0
-                let offset = pixelAlign(CGFloat(offsetMinutes) * minutesPerPixel)
-                Rectangle()
-                    .fill(Color.red)
-                    .frame(width: 2, height: .infinity)
-                    .offset(x: channelColWidth + offset)
-            }
-        }
-    }
-}
-
-struct HorizontalOffsetPreferenceKey: PreferenceKey { static var defaultValue: CGFloat = 0; static func reduce(value: inout CGFloat, nextValue: () -> CGFloat) { value = nextValue() } }
-
-// MARK: - Pixel alignment helper
-private func pixelAlign(_ value: CGFloat) -> CGFloat {
-    let scale = UIScreen.main.scale
-    return (value * scale).rounded() / scale
-}
diff --git a/LiveFin/App Specific/HomeView.swift b/LiveFin/App Specific/HomeView.swift
deleted file mode 100644
index 0608c7f..0000000
--- a/LiveFin/App Specific/HomeView.swift	
+++ /dev/null
@@ -1,834 +0,0 @@
-//
-//  HomeView.swift
-//  LiveFin
-//
-//  Created by KPGamingz on 9/12/25.
-//
-
-import SwiftUI
-
-struct HomeView: View {
-    @EnvironmentObject var appState: AppState
-    @StateObject private var vm = HomeViewModel()
-    @State private var nowTimer = Timer.publish(every: 600, on: .main, in: .common).autoconnect()
-
-    var body: some View {
-        NavigationStack {
-            ScrollView {
-                VStack(alignment: .leading, spacing: 24) {
-                    // Greeting
-                    Text("Hi, \(appState.username)")
-                        .font(.largeTitle).bold()
-                        .frame(maxWidth: .infinity, alignment: .leading)
-                        .padding(.horizontal)
-                        .padding(.top, 8)
-
-                    // On Now
-                    SectionHeader("On Now")
-                    if !vm.onNow.isEmpty {
-                        HorizontalProgramsRow(programs: vm.onNow, style: .landscape)
-                            .environmentObject(vm)
-                    } else {
-                        EmptySectionPlaceholder()
-                    }
-
-                    // Channels (logos only)
-                    SectionHeader("Channels")
-                    if !vm.channels.isEmpty {
-                        HorizontalChannelsRow(channels: vm.channels)
-                            .environmentObject(appState)
-                    } else {
-                        EmptySectionPlaceholder()
-                    }
-
-                    // Shows
-                    SectionHeader("Shows")
-                    if !vm.shows.isEmpty {
-                        HorizontalProgramsRow(programs: vm.shows, style: .landscape)
-                            .environmentObject(vm)
-                    } else {
-                        EmptySectionPlaceholder()
-                    }
-
-                    // Movies (portrait posters)
-                    SectionHeader("Movies")
-                    if !vm.movies.isEmpty {
-                        HorizontalProgramsRow(programs: vm.movies, style: .portrait)
-                            .environmentObject(vm)
-                    } else {
-                        EmptySectionPlaceholder()
-                    }
-
-                    // News
-                    SectionHeader("News")
-                    if !vm.news.isEmpty {
-                        HorizontalProgramsRow(programs: vm.news, style: .landscape)
-                            .environmentObject(vm)
-                    } else {
-                        EmptySectionPlaceholder()
-                    }
-
-                    // Sports
-                    SectionHeader("Sports")
-                    if !vm.sports.isEmpty {
-                        HorizontalProgramsRow(programs: vm.sports, style: .landscape)
-                            .environmentObject(vm)
-                    } else {
-                        EmptySectionPlaceholder()
-                    }
-
-                    // Kids
-                    SectionHeader("Kids")
-                    if !vm.kids.isEmpty {
-                        HorizontalProgramsRow(programs: vm.kids, style: .landscape)
-                            .environmentObject(vm)
-                    } else {
-                        EmptySectionPlaceholder()
-                    }
-                }
-                .padding(.bottom, 24)
-            }
-            .refreshable { await vm.refresh(appState: appState) }
-            .onAppear { if !appState.serverURL.isEmpty { Task { await vm.refresh(appState: appState) } } }
-            .onChange(of: appState.serverURL) { _ in Task { await vm.refresh(appState: appState) } }
-            .onChange(of: appState.isLoggedIn) { loggedIn in if loggedIn { Task { await vm.refresh(appState: appState) } } }
-            .onReceive(nowTimer) { _ in Task { await vm.refresh(appState: appState) } }
-            .toolbar { ToolbarView() }
-        }
-    }
-}
-
-// MARK: - ViewModel
-final class HomeViewModel: ObservableObject {
-    @Published var onNow: [JFProgram] = []
-    @Published var shows: [JFProgram] = []
-    @Published var movies: [JFProgram] = []
-    @Published var news: [JFProgram] = []
-    @Published var sports: [JFProgram] = []
-    @Published var kids: [JFProgram] = []
-    @Published var channelNames: [String: String] = [:]
-    @Published var channels: [JFChannel] = [] // New: full channel list
-
-    private let iso = ISO8601DateFormatter()
-    private let debugLog = false
-
-    init() {
-        iso.formatOptions = [.withInternetDateTime]
-    }
-
-    func channelName(for id: String?) -> String? {
-        guard let id = id else { return nil }
-        return channelNames[id]
-    }
-
-    enum FilterKind { case shows, movies, news, sports, kids }
-
-    @MainActor
-    func refresh(appState: AppState) async {
-        guard !appState.serverURL.isEmpty else { return }
-        async let namesA = fetchChannelNames(appState: appState)
-        async let channelsA = fetchChannels(appState: appState)
-        async let onNowA = fetchOnNow(appState: appState)
-        async let showsA = fetchCategory(appState: appState, filter: .shows)
-        async let moviesA = fetchCategory(appState: appState, filter: .movies)
-        async let newsA = fetchCategory(appState: appState, filter: .news)
-        async let sportsA = fetchCategory(appState: appState, filter: .sports)
-        async let kidsA = fetchCategory(appState: appState, filter: .kids)
-
-        var (names, channels, onNow, shows, movies, news, sports, kids) = await (namesA, channelsA, onNowA, showsA, moviesA, newsA, sportsA, kidsA)
-        self.channelNames = names
-        self.channels = channels
-
-        // If Shows or Kids are empty, derive from a general window as a safety net
-        if shows.isEmpty || kids.isEmpty {
-            let window = await fetchProgramsWindow(appState: appState)
-            if debugLog { print("HomeView: Fallback window count: \(window.count)") }
-            if !window.isEmpty {
-                let now = Date()
-                func notPast(_ p: JFProgram) -> Bool {
-                    guard let s = p.startDate else { return false }
-                    if let e = p.endDate { return e >= now }
-                    if let t = p.runTimeTicks { return s.addingTimeInterval(TimeInterval(Double(t)/10_000_000.0)) >= now }
-                    return s >= now.addingTimeInterval(-2 * 3600)
-                }
-                let base = window.filter(notPast)
-                if debugLog { print("HomeView: Fallback base (time-filtered) count: \(base.count)") }
-                if shows.isEmpty {
-                    let derived = base.filter { p in
-                        let notOther = !p.isMovie && !p.isNews && !p.isSports && !p.isKids
-                        let seriesCue = p.isSeries || ((p.seriesName?.isEmpty == false) || (p.episodeTitle?.isEmpty == false))
-                        return notOther || seriesCue
-                    }
-                    shows = Array(derived.sorted(by: { ($0.startDate ?? .distantFuture) < ($1.startDate ?? .distantFuture) }).prefix(60))
-                    if debugLog { print("HomeView: Fallback derived Shows count: \(shows.count)") }
-                }
-                if kids.isEmpty {
-                    let derived = base.filter { p in
-                        if p.isKids { return true }
-                        let hasKidsGenre = (p.genres?.contains(where: { g in
-                            let gl = g.lowercased()
-                            return gl.contains("kids") || gl.contains("children") || gl.contains("child") || gl.contains("family") || gl.contains("cartoon") || gl.contains("animation")
-                        }) ?? false)
-                        let rating = (p.officialRating ?? "").uppercased()
-                        let kidRatings = ["TV-Y", "TV-Y7", "TV-G", "G", "Y7"]
-                        let hasKidRating = kidRatings.contains(where: { rating.hasPrefix($0) })
-                        return hasKidsGenre || hasKidRating
-                    }
-                    kids = Array(derived.sorted(by: { ($0.startDate ?? .distantFuture) < ($1.startDate ?? .distantFuture) }).prefix(60))
-                    if debugLog { print("HomeView: Fallback derived Kids count: \(kids.count)") }
-                }
-            }
-            if (shows.isEmpty || kids.isEmpty) && !onNow.isEmpty {
-                if shows.isEmpty {
-                    let derived = onNow.filter { p in
-                        let notOther = !p.isMovie && !p.isNews && !p.isSports && !p.isKids
-                        let seriesCue = p.isSeries || ((p.seriesName?.isEmpty == false) || (p.episodeTitle?.isEmpty == false))
-                        return notOther || seriesCue
-                    }
-                    shows = Array(derived.prefix(60))
-                    if debugLog { print("HomeView: OnNow-derived Shows count: \(shows.count)") }
-                }
-                if kids.isEmpty {
-                    let derived = onNow.filter { p in
-                        if p.isKids { return true }
-                        let hasKidsGenre = (p.genres?.contains(where: { g in
-                            let gl = g.lowercased()
-                            return gl.contains("kids") || gl.contains("children") || gl.contains("child") || gl.contains("family") || gl.contains("cartoon") || gl.contains("animation")
-                        }) ?? false)
-                        let rating = (p.officialRating ?? "").uppercased()
-                        let kidRatings = ["TV-Y", "TV-Y7", "TV-G", "G", "Y7"]
-                        let hasKidRating = kidRatings.contains(where: { rating.hasPrefix($0) })
-                        return hasKidsGenre || hasKidRating
-                    }
-                    kids = Array(derived.prefix(60))
-                    if debugLog { print("HomeView: OnNow-derived Kids count: \(kids.count)") }
-                }
-            }
-        }
-
-        self.onNow = onNow
-        self.shows = shows
-        self.movies = movies
-        self.news = news
-        self.sports = sports
-        self.kids = kids
-
-        if debugLog { print("HomeView: Section counts -> OnNow: \(self.onNow.count), Shows: \(self.shows.count), Movies: \(self.movies.count), News: \(self.news.count), Sports: \(self.sports.count), Kids: \(self.kids.count), Channels: \(self.channels.count)") }
-    }
-
-    // MARK: - Per-section fetches
-    private func fetchOnNow(appState: AppState) async -> [JFProgram] {
-        do {
-            guard let base = URL(string: appState.serverURL)?.appendingPathComponent("/LiveTv/Programs") else { return [] }
-            var comps = URLComponents(url: base, resolvingAgainstBaseURL: false)
-            var q: [URLQueryItem] = [
-                URLQueryItem(name: "IsAiring", value: "true"),
-                URLQueryItem(name: "Limit", value: "120"),
-                URLQueryItem(name: "fields", value: "Overview,OfficialRating,Genres,SeriesName,EpisodeTitle,RunTimeTicks,ParentIndexNumber,IndexNumber")
-            ]
-            if let uid = appState.user?.id { q.append(URLQueryItem(name: "userId", value: uid)) }
-            comps?.queryItems = q
-            guard let url = comps?.url else { return [] }
-            var req = URLRequest(url: url)
-            req.httpMethod = "GET"
-            if !appState.accessToken.isEmpty { req.setValue(appState.accessToken, forHTTPHeaderField: "X-Emby-Token") }
-            let (data, resp) = try await URLSession.shared.data(for: req)
-            guard let http = resp as? HTTPURLResponse, http.statusCode == 200 else { return [] }
-            let items = parsePrograms(data: data)
-            // Sort by start time then channel
-            return items.sorted { a, b in
-                let la = a.startDate ?? Date.distantFuture
-                let lb = b.startDate ?? Date.distantFuture
-                if la == lb {
-                    let ca = a.channelName ?? channelName(for: a.channelId) ?? ""
-                    let cb = b.channelName ?? channelName(for: b.channelId) ?? ""
-                    return ca.localizedCaseInsensitiveCompare(cb) == .orderedAscending
-                }
-                return la < lb
-            }
-        } catch {
-            print("HomeView: fetchOnNow error: \(error.localizedDescription)")
-            return []
-        }
-    }
-
-    private func fetchCategory(appState: AppState, filter: FilterKind) async -> [JFProgram] {
-        do {
-            guard let base = URL(string: appState.serverURL)?.appendingPathComponent("/LiveTv/Programs") else { return [] }
-            var comps = URLComponents(url: base, resolvingAgainstBaseURL: false)
-            let now = Date()
-            let start = now.addingTimeInterval(-6 * 3600)
-            let end = now.addingTimeInterval(24 * 3600)
-            var q: [URLQueryItem] = [
-                URLQueryItem(name: "startDate", value: iso.string(from: start)),
-                URLQueryItem(name: "endDate", value: iso.string(from: end)),
-                URLQueryItem(name: "Limit", value: "250"),
-                URLQueryItem(name: "fields", value: "Overview,OfficialRating,Genres,SeriesName,EpisodeTitle,RunTimeTicks,ParentIndexNumber,IndexNumber")
-            ]
-            switch filter {
-            case .shows:
-                q.append(URLQueryItem(name: "IsSeries", value: "true"))
-            case .movies:
-                q.append(URLQueryItem(name: "IsMovie", value: "true"))
-            case .news:
-                q.append(URLQueryItem(name: "IsNews", value: "true"))
-            case .sports:
-                q.append(URLQueryItem(name: "IsSports", value: "true"))
-            case .kids:
-                q.append(URLQueryItem(name: "IsKids", value: "true"))
-            }
-            if let uid = appState.user?.id { q.append(URLQueryItem(name: "userId", value: uid)) }
-            comps?.queryItems = q
-            guard let url = comps?.url else { return [] }
-            var req = URLRequest(url: url)
-            req.httpMethod = "GET"
-            if !appState.accessToken.isEmpty { req.setValue(appState.accessToken, forHTTPHeaderField: "X-Emby-Token") }
-            let (data, resp) = try await URLSession.shared.data(for: req)
-            guard let http = resp as? HTTPURLResponse, http.statusCode == 200 else { return [] }
-            var items = parsePrograms(data: data)
-            if debugLog { print("HomeView: fetchCategory raw count (\(filter)): \(items.count)") }
-
-            // If empty, try StartDateUtc/EndDateUtc variant
-            if items.isEmpty {
-                var compsUtc = URLComponents(url: base, resolvingAgainstBaseURL: false)
-                var qUtc: [URLQueryItem] = [
-                    URLQueryItem(name: "StartDateUtc", value: iso.string(from: start)),
-                    URLQueryItem(name: "EndDateUtc", value: iso.string(from: end)),
-                    URLQueryItem(name: "Limit", value: "250"),
-                    URLQueryItem(name: "fields", value: "Overview,OfficialRating,Genres,SeriesName,EpisodeTitle,RunTimeTicks,ParentIndexNumber,IndexNumber")
-                ]
-                switch filter {
-                case .shows: qUtc.append(URLQueryItem(name: "IsSeries", value: "true"))
-                case .movies: qUtc.append(URLQueryItem(name: "IsMovie", value: "true"))
-                case .news: qUtc.append(URLQueryItem(name: "IsNews", value: "true"))
-                case .sports: qUtc.append(URLQueryItem(name: "IsSports", value: "true"))
-                case .kids: qUtc.append(URLQueryItem(name: "IsKids", value: "true"))
-                }
-                if let uid = appState.user?.id { qUtc.append(URLQueryItem(name: "userId", value: uid)) }
-                compsUtc?.queryItems = qUtc
-                if let urlUtc = compsUtc?.url {
-                    var reqUtc = URLRequest(url: urlUtc)
-                    reqUtc.httpMethod = "GET"
-                    if !appState.accessToken.isEmpty { reqUtc.setValue(appState.accessToken, forHTTPHeaderField: "X-Emby-Token") }
-                    let (dataUtc, respUtc) = try await URLSession.shared.data(for: reqUtc)
-                    if let httpUtc = respUtc as? HTTPURLResponse, httpUtc.statusCode == 200 {
-                        items = parsePrograms(data: dataUtc)
-                        if debugLog { print("HomeView: fetchCategory UTC count (\(filter)): \(items.count)") }
-                    }
-                }
-            }
-
-            // Filter: keep ongoing or upcoming (with 2h fallback when no end/runtime)
-            let nowT = Date()
-            items = items.filter { p in
-                if let e = p.endDate { return e >= nowT }
-                if let s = p.startDate, let t = p.runTimeTicks { return s.addingTimeInterval(TimeInterval(Double(t)/10_000_000.0)) >= nowT }
-                if let s = p.startDate { return s >= nowT.addingTimeInterval(-2 * 3600) }
-                return false
-            }
-            if debugLog { print("HomeView: fetchCategory post-time-filter count (\(filter)): \(items.count)") }
-
-            // Fallback to unfiltered window when empty
-            if items.isEmpty {
-                if filter == .shows || filter == .kids {
-                    var compsFB = URLComponents(url: base, resolvingAgainstBaseURL: false)
-                    var qFB: [URLQueryItem] = [
-                        URLQueryItem(name: "startDate", value: iso.string(from: start)),
-                        URLQueryItem(name: "endDate", value: iso.string(from: end)),
-                        URLQueryItem(name: "Limit", value: "500"),
-                        URLQueryItem(name: "fields", value: "Overview,OfficialRating,Genres,SeriesName,EpisodeTitle,RunTimeTicks,ParentIndexNumber,IndexNumber")
-                    ]
-                    if let uid = appState.user?.id { qFB.append(URLQueryItem(name: "userId", value: uid)) }
-                    compsFB?.queryItems = qFB
-                    if let urlFB = compsFB?.url {
-                        var reqFB = URLRequest(url: urlFB)
-                        reqFB.httpMethod = "GET"
-                        if !appState.accessToken.isEmpty { reqFB.setValue(appState.accessToken, forHTTPHeaderField: "X-Emby-Token") }
-                        let (dataFB, respFB) = try await URLSession.shared.data(for: reqFB)
-                        if let httpFB = respFB as? HTTPURLResponse, httpFB.statusCode == 200 {
-                            var all = parsePrograms(data: dataFB)
-                            // Apply same relaxed time filter
-                            all = all.filter { p in
-                                if let e = p.endDate { return e >= nowT }
-                                if let s = p.startDate, let t = p.runTimeTicks { return s.addingTimeInterval(TimeInterval(Double(t)/10_000_000.0)) >= nowT }
-                                if let s = p.startDate { return s >= nowT.addingTimeInterval(-2 * 3600) }
-                                return false
-                            }
-                            if debugLog { print("HomeView: fetchCategory fallback window count (\(filter)): \(all.count)") }
-                            if filter == .shows {
-                                items = all.filter { !$0.isMovie && !$0.isNews && !$0.isSports && !$0.isKids }
-                            } else if filter == .kids {
-                                items = all.filter { $0.isKids }
-                            }
-                        }
-                    }
-                }
-            }
-
-            // Sort and cap
-            return items.sorted { a, b in
-                let la = a.startDate ?? Date.distantFuture
-                let lb = b.startDate ?? Date.distantFuture
-                if la == lb {
-                    let ca = a.channelName ?? channelName(for: a.channelId) ?? ""
-                    let cb = b.channelName ?? channelName(for: b.channelId) ?? ""
-                    return ca.localizedCaseInsensitiveCompare(cb) == .orderedAscending
-                }
-                return la < lb
-            }.prefixed(40)
-        } catch {
-            print("HomeView: fetchCategory error: \(error.localizedDescription)")
-            return []
-        }
-    }
-
-    private func fetchChannelNames(appState: AppState) async -> [String: String] {
-        do {
-            guard let base = URL(string: appState.serverURL)?.appendingPathComponent("/LiveTv/Channels") else { return [:] }
-            var comps = URLComponents(url: base, resolvingAgainstBaseURL: false)
-            comps?.queryItems = [URLQueryItem(name: "Limit", value: "500"), URLQueryItem(name: "StartIndex", value: "0")]
-            var req = URLRequest(url: comps?.url ?? base)
-            req.httpMethod = "GET"
-            if !appState.accessToken.isEmpty { req.setValue(appState.accessToken, forHTTPHeaderField: "X-Emby-Token") }
-            let (data, resp) = try await URLSession.shared.data(for: req)
-            guard let http = resp as? HTTPURLResponse, http.statusCode == 200 else { return [:] }
-            var map: [String: String] = [:]
-            if let obj = try? JSONSerialization.jsonObject(with: data) as? [String: Any],
-               let items = obj["Items"] as? [[String: Any]] {
-                for item in items {
-                    if let id = item["Id"] as? String, let name = item["Name"] as? String { map[id] = name }
-                }
-            } else if let arr = try? JSONSerialization.jsonObject(with: data) as? [[String: Any]] {
-                for item in arr {
-                    if let id = item["Id"] as? String, let name = item["Name"] as? String { map[id] = name }
-                }
-            }
-            return map
-        } catch {
-            print("HomeView: fetchChannelNames error: \(error.localizedDescription)")
-            return [:]
-        }
-    }
-
-    private func parsePrograms(data: Data) -> [JFProgram] {
-        // Jellyfin can return an array or an object with Items. Handle both.
-        if let arr = try? JSONSerialization.jsonObject(with: data) as? [[String: Any]] {
-            return arr.compactMap { JFProgram(json: $0) }
-        }
-        if let obj = try? JSONSerialization.jsonObject(with: data) as? [String: Any] {
-            if let arr = obj["Items"] as? [[String: Any]] { return arr.compactMap { JFProgram(json: $0) } }
-        }
-        return []
-    }
-
-    // Fetch a general window to derive sections as fallback
-    private func fetchProgramsWindow(appState: AppState) async -> [JFProgram] {
-        func attempt(params: [URLQueryItem]) async -> [JFProgram] {
-            do {
-                guard let base = URL(string: appState.serverURL)?.appendingPathComponent("/LiveTv/Programs") else { return [] }
-                var comps = URLComponents(url: base, resolvingAgainstBaseURL: false)
-                comps?.queryItems = params
-                guard let url = comps?.url else { return [] }
-                var req = URLRequest(url: url)
-                req.httpMethod = "GET"
-                if !appState.accessToken.isEmpty { req.setValue(appState.accessToken, forHTTPHeaderField: "X-Emby-Token") }
-                let (data, resp) = try await URLSession.shared.data(for: req)
-                guard let http = resp as? HTTPURLResponse, http.statusCode == 200 else { return [] }
-                let parsed = parsePrograms(data: data)
-                return parsed
-            } catch {
-                print("HomeView: fetchProgramsWindow error: \(error.localizedDescription)")
-                return []
-            }
-        }
-        let now = Date()
-        let start = now.addingTimeInterval(-6 * 3600)
-        let end = now.addingTimeInterval(24 * 3600)
-        var base: [URLQueryItem] = [
-            URLQueryItem(name: "Limit", value: "600")
-        ]
-        if let uid = appState.user?.id { base.append(URLQueryItem(name: "userId", value: uid)) }
-
-        // Try startDate/endDate with and without extra flags
-        var q1 = base
-        q1.append(contentsOf: [
-            URLQueryItem(name: "startDate", value: iso.string(from: start)),
-            URLQueryItem(name: "endDate", value: iso.string(from: end)),
-            URLQueryItem(name: "fields", value: "Overview,OfficialRating,Genres,SeriesName,EpisodeTitle,RunTimeTicks,ParentIndexNumber,IndexNumber")
-        ])
-        var r1 = await attempt(params: q1)
-        if !r1.isEmpty { return r1 }
-        var q1b = q1
-        q1b.append(contentsOf: [URLQueryItem(name: "EnableImages", value: "false"), URLQueryItem(name: "EnableUserData", value: "false")])
-        r1 = await attempt(params: q1b)
-        if !r1.isEmpty { return r1 }
-
-        // Try StartDateUtc/EndDateUtc
-        var q2 = base
-        q2.append(contentsOf: [
-            URLQueryItem(name: "StartDateUtc", value: iso.string(from: start)),
-            URLQueryItem(name: "EndDateUtc", value: iso.string(from: end)),
-            URLQueryItem(name: "fields", value: "Overview,OfficialRating,Genres,SeriesName,EpisodeTitle,RunTimeTicks,ParentIndexNumber,IndexNumber")
-        ])
-        var r2 = await attempt(params: q2)
-        if !r2.isEmpty { return r2 }
-        var q2b = q2
-        q2b.append(contentsOf: [URLQueryItem(name: "EnableImages", value: "false"), URLQueryItem(name: "EnableUserData", value: "false")])
-        r2 = await attempt(params: q2b)
-        return r2
-    }
-
-    private func fetchChannels(appState: AppState) async -> [JFChannel] {
-        do {
-            guard let base = URL(string: appState.serverURL)?.appendingPathComponent("/LiveTv/Channels") else { return [] }
-            var comps = URLComponents(url: base, resolvingAgainstBaseURL: false)
-            comps?.queryItems = [URLQueryItem(name: "Limit", value: "500"), URLQueryItem(name: "StartIndex", value: "0")]
-            var req = URLRequest(url: comps?.url ?? base)
-            req.httpMethod = "GET"
-            if !appState.accessToken.isEmpty { req.setValue(appState.accessToken, forHTTPHeaderField: "X-Emby-Token") }
-            let (data, resp) = try await URLSession.shared.data(for: req)
-            guard let http = resp as? HTTPURLResponse, http.statusCode == 200 else { return [] }
-            var list: [JFChannel] = []
-            if let obj = try? JSONSerialization.jsonObject(with: data) as? [String: Any], let items = obj["Items"] as? [[String: Any]] {
-                for item in items { if let ch = JFChannel(json: item) { list.append(ch) } }
-            } else if let arr = try? JSONSerialization.jsonObject(with: data) as? [[String: Any]] {
-                for item in arr { if let ch = JFChannel(json: item) { list.append(ch) } }
-            }
-            return list.sorted { $0.name.localizedCaseInsensitiveCompare($1.name) == .orderedAscending }
-        } catch {
-            print("HomeView: fetchChannels error: \(error.localizedDescription)")
-            return []
-        }
-    }
-}
-
-// MARK: - Models
-struct JFProgram: Identifiable, Hashable {
-    let id: String
-    let name: String
-    let overview: String?
-    let startDate: Date?
-    let endDate: Date?
-    let channelId: String?
-    let channelName: String?
-    let isMovie: Bool
-    let isSeries: Bool
-    let isNews: Bool
-    let isSports: Bool
-    let isKids: Bool
-    // New optional subtitle-related fields
-    let episodeTitle: String?
-    let seriesName: String?
-    // New optional runtime ticks to compute missing end time
-    let runTimeTicks: Int64?
-    // New fields for fallback categorization
-    let officialRating: String?
-    let genres: [String]?
-    // New: season/episode index numbers
-    let parentIndexNumber: Int?
-    let indexNumber: Int?
-
-    init?(json: [String: Any]) {
-        guard let id = json["Id"] as? String ?? json["ProgramId"] as? String else { return nil }
-        self.id = id
-        self.name = json["Name"] as? String ?? ""
-        self.overview = json["Overview"] as? String
-        // Robust ISO8601 parsing: try with fractional seconds, without, then trim to 3 fractional digits and retry
-        let isoFrac = ISO8601DateFormatter(); isoFrac.formatOptions = [.withInternetDateTime, .withFractionalSeconds]
-        let isoPlain = ISO8601DateFormatter(); isoPlain.formatOptions = [.withInternetDateTime]
-        func trimFractionTo3(_ s: String) -> String {
-            guard let dot = s.firstIndex(of: ".") else { return s }
-            let afterDot = s.index(after: dot)
-            let tzIndex: String.Index = {
-                if let z = s[afterDot...].firstIndex(of: "Z") { return z }
-                if let plus = s[afterDot...].firstIndex(of: "+") { return plus }
-                if let minus = s[afterDot...].firstIndex(of: "-") { return minus }
-                return s.endIndex
-            }()
-            let prefix = String(s[..<dot])
-            let fracDigits = String(s[afterDot..<tzIndex])
-            let first3 = String(fracDigits.prefix(3))
-            let suffix = String(s[tzIndex...])
-            return prefix + "." + first3 + suffix
-        }
-        func parse(_ sIn: String) -> Date? {
-            if let d = isoFrac.date(from: sIn) ?? isoPlain.date(from: sIn) { return d }
-            let trimmed = trimFractionTo3(sIn)
-            if trimmed != sIn { return isoFrac.date(from: trimmed) ?? isoPlain.date(from: trimmed) }
-            return nil
-        }
-        if let s = (json["StartDate"] as? String) ?? (json["StartDateUtc"] as? String) { self.startDate = parse(s) } else { self.startDate = nil }
-        if let e = (json["EndDate"] as? String) ?? (json["EndDateUtc"] as? String) { self.endDate = parse(e) } else { self.endDate = nil }
-        self.channelId = json["ChannelId"] as? String
-        self.channelName = json["ChannelName"] as? String
-        self.isMovie = (json["IsMovie"] as? Bool) ?? false
-        self.isSeries = (json["IsSeries"] as? Bool) ?? false
-        self.isNews = (json["IsNews"] as? Bool) ?? false
-        self.isSports = (json["IsSports"] as? Bool) ?? false
-        self.isKids = (json["IsKids"] as? Bool) ?? false
-        // Parse optional subtitle fields if present
-        self.episodeTitle = json["EpisodeTitle"] as? String ?? json["Subtitle"] as? String
-        self.seriesName = json["SeriesName"] as? String
-        // Parse runtime ticks if present
-        if let ticks = json["RunTimeTicks"] as? Int64 {
-            self.runTimeTicks = ticks
-        } else if let ticksNum = json["RunTimeTicks"] as? NSNumber {
-            self.runTimeTicks = ticksNum.int64Value
-        } else {
-            self.runTimeTicks = nil
-        }
-        // Additional fields
-        self.officialRating = json["OfficialRating"] as? String
-        if let gs = json["Genres"] as? [String] {
-            self.genres = gs
-        } else if let any = json["Genres"] as? [Any] {
-            self.genres = any.compactMap { $0 as? String }
-        } else {
-            self.genres = nil
-        }
-        // New parsing for season/episode
-        if let s = json["ParentIndexNumber"] as? Int { self.parentIndexNumber = s } else if let ns = json["ParentIndexNumber"] as? NSNumber { self.parentIndexNumber = ns.intValue } else { self.parentIndexNumber = nil }
-        if let e = json["IndexNumber"] as? Int { self.indexNumber = e } else if let ne = json["IndexNumber"] as? NSNumber { self.indexNumber = ne.intValue } else { self.indexNumber = nil }
-    }
-}
-
-// New Channel model
-struct JFChannel: Identifiable, Hashable {
-    let id: String
-    let name: String
-    init?(json: [String: Any]) {
-        guard let id = json["Id"] as? String else { return nil }
-        self.id = id
-        self.name = (json["Name"] as? String) ?? "Channel"
-    }
-}
-// Helper to convert lightweight JFChannel to LiveTvChannelDto used by detail view
-private extension JFChannel {
-    func asLiveDto(baseURL: String) -> LiveTvChannelDto {
-        LiveTvChannelDto(id: id, name: name, number: nil, startDate: nil, endDate: nil, baseURL: baseURL)
-    }
-}
-
-// MARK: - Views
-private struct SectionHeader: View {
-    let title: String
-    init(_ title: String) { self.title = title }
-    var body: some View {
-        Text(title)
-            .font(.title2).bold()
-            .frame(maxWidth: .infinity, alignment: .leading)
-            .padding(.horizontal)
-    }
-}
-
-private enum RowStyle { case landscape, portrait }
-
-private struct HorizontalProgramsRow: View {
-    let programs: [JFProgram]
-    let style: RowStyle
-    @EnvironmentObject private var appState: AppState
-    @EnvironmentObject private var vm: HomeViewModel
-
-    var body: some View {
-        ScrollView(.horizontal, showsIndicators: false) {
-            LazyHStack(alignment: .top, spacing: 12) {
-                ForEach(programs) { program in
-                    NavigationLink(destination: ProgramView(program: program)
-                        .environmentObject(appState)
-                        .environmentObject(vm)) {
-                        ProgramCard(program: program, style: style)
-                            .environmentObject(appState)
-                            .environmentObject(vm)
-                    }
-                    .buttonStyle(.plain)
-                }
-            }
-            .padding(.horizontal)
-        }
-    }
-}
-
-// New horizontal row for channels
-private struct HorizontalChannelsRow: View {
-    let channels: [JFChannel]
-    @EnvironmentObject private var appState: AppState
-    var body: some View {
-        ScrollView(.horizontal, showsIndicators: false) {
-            LazyHStack(alignment: .center, spacing: 16) {
-                ForEach(channels) { channel in
-                    NavigationLink(destination: ChannelDetailView(channel: channel.asLiveDto(baseURL: appState.serverURL))) {
-                        ZStack {
-                            // Enlarged tile background
-                            RoundedRectangle(cornerRadius: 16, style: .continuous)
-                                .fill(Color(UIColor.secondarySystemBackground))
-                                .overlay(
-                                    RoundedRectangle(cornerRadius: 16, style: .continuous)
-                                        .stroke(Color.secondary.opacity(0.15), lineWidth: 1)
-                                )
-                            // Smaller logo centered
-                            ChannelImageView(baseUrl: appState.serverURL, apiKey: appState.apiKey, channelId: channel.id)
-                                .frame(width: 67, height: 67) // shrunk image
-                                .shadow(radius: 1, y: 1)
-                        }
-                        .frame(width: 84, height: 84) // enlarged box
-                        .contentShape(RoundedRectangle(cornerRadius: 16, style: .continuous))
-                        .accessibilityLabel(Text(channel.name))
-                    }
-                    .buttonStyle(.plain)
-                }
-            }
-            .padding(.horizontal)
-            .frame(minHeight: 110)
-        }
-    }
-}
-
-private struct ProgramCard: View {
-    let program: JFProgram
-    let style: RowStyle
-    @EnvironmentObject private var appState: AppState
-    @EnvironmentObject private var vm: HomeViewModel
-
-    var body: some View {
-        VStack(alignment: .leading, spacing: 6) {
-            ZStack(alignment: .bottomLeading) {
-                ProgramImage(program: program)
-                    .frame(width: imageWidth, height: imageHeight)
-                    .background(Color(UIColor.secondarySystemBackground))
-                    .clipShape(RoundedRectangle(cornerRadius: 8, style: .continuous))
-                if let progress = progressRatio, progress > 0, progress < 1 {
-                    ZStack(alignment: .leading) {
-                        Rectangle().fill(Color.black.opacity(0.35)).frame(height: 4)
-                        Rectangle().fill(Color.white).frame(width: imageWidth * progress, height: 4)
-                    }
-                    .clipShape(Capsule())
-                    .padding(6)
-                }
-            }
-            Text(program.name)
-                .font(.headline)
-                .lineLimit(1)
-                .frame(maxWidth: imageWidth, alignment: .leading)
-            if let subtitle = programSubtitle {
-                Text(subtitle)
-                    .font(.subheadline)
-                    .foregroundColor(.secondary)
-                    .lineLimit(1)
-                    .frame(maxWidth: imageWidth, alignment: .leading)
-            }
-            if let timeLine = timeLine {
-                Text(timeLine)
-                    .font(.caption)
-                    .foregroundColor(.secondary)
-                    .lineLimit(1)
-                    .frame(maxWidth: imageWidth, alignment: .leading)
-            }
-            Text(channelLine)
-                .font(.subheadline)
-                .foregroundColor(.secondary)
-                .lineLimit(1)
-                .frame(maxWidth: imageWidth, alignment: .leading)
-        }
-    }
-
-    private var imageWidth: CGFloat { style == .portrait ? 120 : 220 }
-    private var imageHeight: CGFloat { style == .portrait ? 180 : 124 }
-
-    // Added subtitle (Season/Episode + EpisodeTitle) logic back
-    private var programSubtitle: String? {
-        let seasonEpisode: String? = {
-            if let s = program.parentIndexNumber, let e = program.indexNumber { return String(format: "S%02dE%02d", s, e) }
-            return nil
-        }()
-        if let ep = program.episodeTitle, !ep.isEmpty {
-            if let seasonEpisode { return "\(seasonEpisode) • \(ep)" } else { return ep }
-        } else {
-            return seasonEpisode
-        }
-    }
-
-    // Added time range back (no description)
-    private var timeLine: String? {
-        guard let start = program.startDate else { return nil }
-        let end: Date? = {
-            if let e = program.endDate { return e }
-            if let t = program.runTimeTicks { return start.addingTimeInterval(TimeInterval(Double(t)/10_000_000.0)) }
-            return nil
-        }()
-        guard let end = end else { return nil }
-        return "\(start.formatted(date: .omitted, time: .shortened)) - \(end.formatted(date: .omitted, time: .shortened))"
-    }
-
-    private var channelLine: String {
-        (program.channelName ?? vm.channelName(for: program.channelId)) ?? "Unknown channel"
-    }
-
-    private var progressRatio: Double? {
-        guard let start = program.startDate else { return nil }
-        let end: Date? = {
-            if let e = program.endDate { return e }
-            if let t = program.runTimeTicks { return start.addingTimeInterval(TimeInterval(Double(t) / 10_000_000.0)) }
-            return nil
-        }()
-        guard let end = end else { return nil }
-        let now = Date()
-        guard start <= now && now <= end else { return nil }
-        let total = end.timeIntervalSince(start)
-        guard total > 1 else { return nil }
-        let elapsed = now.timeIntervalSince(start)
-        return min(max(elapsed / total, 0), 1)
-    }
-}
-
-private struct ProgramImage: View {
-    let program: JFProgram
-    @EnvironmentObject private var appState: AppState
-    @EnvironmentObject private var vm: HomeViewModel
-
-    var body: some View {
-        if let url = imageURL() {
-            AsyncImage(url: url) { phase in
-                switch phase {
-                case .empty:
-                    ZStack { ProgressView() }
-                case .success(let img):
-                    img.resizable().scaledToFill()
-                case .failure:
-                    ZStack { Image(systemName: "film").imageScale(.large).foregroundColor(.secondary) }
-                @unknown default:
-                    EmptyView()
-                }
-            }
-        } else {
-            ZStack { Image(systemName: "film").imageScale(.large).foregroundColor(.secondary) }
-        }
-    }
-
-    private func imageURL() -> URL? {
-        // Defer images until we know we can authenticate and have a server URL
-        guard !appState.serverURL.isEmpty, !appState.apiKey.isEmpty else { return nil }
-        let base = appState.serverURL.trimmingCharacters(in: CharacterSet(charactersIn: "/"))
-        let path = "/Items/\(program.id)/Images/Primary"
-        var comps = URLComponents(string: base + path)
-        comps?.queryItems = [
-            URLQueryItem(name: "maxWidth", value: "600"),
-            URLQueryItem(name: "api_key", value: appState.apiKey),
-            URLQueryItem(name: "t", value: String(Int(Date().timeIntervalSince1970)))
-        ]
-        return comps?.url
-    }
-}
-
-private struct EmptySectionPlaceholder: View {
-    var body: some View {
-        ScrollView(.horizontal, showsIndicators: false) {
-            HStack { Text("No items right now").foregroundColor(.secondary) }
-                .frame(height: 20)
-                .padding(.horizontal)
-        }
-    }
-}
-
-private extension Array where Element == JFProgram {
-    func prefixed(_ n: Int) -> [JFProgram] { Array(self.prefix(n)) }
-}
diff --git a/LiveFin/App Specific/LiveFinApp.swift b/LiveFin/App Specific/LiveFinApp.swift
index 338f09c..2ce33f3 100644
--- a/LiveFin/App Specific/LiveFinApp.swift	
+++ b/LiveFin/App Specific/LiveFinApp.swift	
@@ -1,13 +1,108 @@
 import SwiftUI
+import BackgroundTasks
 
 @main
 struct LiveFinApp: App {
     @StateObject private var appState = AppState()
+    // Background refresh identifier - must also be added to Info.plist
+    private static let bgRefreshIdentifier = "com.livefin.app.epgrefresh"
+    private static var bgRegistered = false
+
+    @Environment(\.scenePhase) private var scenePhase
 
     var body: some Scene {
         WindowGroup {
             RootView()
                 .environmentObject(appState)
+                .onAppear {
+                    // One-time BGTask registration; do not capture `self` in init to avoid escaping capture of a mutating self
+                    if !Self.bgRegistered {
+                        Self.bgRegistered = true
+                        BGTaskScheduler.shared.register(forTaskWithIdentifier: Self.bgRefreshIdentifier, using: nil) { rawTask in
+                            guard let task = rawTask as? BGAppRefreshTask else {
+                                rawTask.setTaskCompleted(success: false)
+                                return
+                            }
+
+                            // Schedule next
+                            let req = BGAppRefreshTaskRequest(identifier: Self.bgRefreshIdentifier)
+                            req.earliestBeginDate = Date(timeIntervalSinceNow: 15 * 60)
+                            do { try BGTaskScheduler.shared.submit(req) } catch { print("Failed to resubmit BGAppRefresh: \(error)") }
+
+                            // Perform the refresh using AppState (captured reference type)
+                            let queue = OperationQueue()
+                            queue.maxConcurrentOperationCount = 1
+                            let op = BlockOperation {
+                                let sem = DispatchSemaphore(value: 0)
+                                Task {
+                                    await appState.performBackgroundEPGRefresh()
+                                    sem.signal()
+                                }
+                                _ = sem.wait(timeout: .now() + 25)
+                            }
+
+                            task.expirationHandler = { queue.cancelAllOperations() }
+                            op.completionBlock = { task.setTaskCompleted(success: !op.isCancelled) }
+                            queue.addOperation(op)
+                        }
+                    }
+
+                    // Schedule first background refresh attempt when the app appears
+                    scheduleBackgroundRefresh()
+                }
+                .onChange(of: scenePhase) { newPhase in
+                    if newPhase == .background {
+                        scheduleBackgroundRefresh()
+                        // Fallback: perform an immediate background refresh while we have execution time
+                        Task {
+                            await appState.performBackgroundEPGRefresh()
+                        }
+                    }
+                }
+        }
+    }
+
+    private func scheduleBackgroundRefresh() {
+        let request = BGAppRefreshTaskRequest(identifier: Self.bgRefreshIdentifier)
+        // Prefer a refresh no earlier than 15 minutes from now; let the system optimize
+        request.earliestBeginDate = Date(timeIntervalSinceNow: 15 * 60)
+        do {
+            try BGTaskScheduler.shared.submit(request)
+            print("BGAppRefresh scheduled: \(Self.bgRefreshIdentifier)")
+        } catch {
+            print("Failed to schedule BGAppRefresh: \(error)")
+        }
+    }
+
+    // keep handleAppRefresh if needed elsewhere, but not required for registration now
+    private func handleAppRefresh(task: BGAppRefreshTask) {
+        // Schedule the next refresh
+        scheduleBackgroundRefresh()
+
+        // Create an operation or Task to perform refresh and notify completion
+        let queue = OperationQueue()
+        queue.maxConcurrentOperationCount = 1
+
+        let op = BlockOperation {
+            // Call AppState's background refresh synchronously (Task) and wait up to a timeout
+            let sem = DispatchSemaphore(value: 0)
+            Task {
+                await appState.performBackgroundEPGRefresh()
+                sem.signal()
+            }
+            // Wait but respect system time constraints (cap here 25s)
+            let _ = sem.wait(timeout: .now() + 25)
         }
+
+        task.expirationHandler = {
+            // Cancel the operation if task expired
+            queue.cancelAllOperations()
+        }
+
+        op.completionBlock = {
+            task.setTaskCompleted(success: !op.isCancelled)
+        }
+
+        queue.addOperation(op)
     }
 }
diff --git a/LiveFin/App Specific/LoginView.swift b/LiveFin/App Specific/LoginView.swift
deleted file mode 100644
index 3f1da1d..0000000
--- a/LiveFin/App Specific/LoginView.swift	
+++ /dev/null
@@ -1,121 +0,0 @@
-//
-//  LoginView.swift
-//  LiveFin
-//
-//  Created by KPGamingz on 4/9/25.
-//
-
-import SwiftUI
-import JellyfinAPI
-import Foundation
-import Get
-
-extension Color {
-    init(hex: String) {
-        let scanner = Scanner(string: hex)
-        _ = scanner.scanString("#")
-        var rgb: UInt64 = 0
-        scanner.scanHexInt64(&rgb)
-        let r = Double((rgb >> 16) & 0xFF) / 255
-        let g = Double((rgb >> 8) & 0xFF) / 255
-        let b = Double(rgb & 0xFF) / 255
-        self.init(red: r, green: g, blue: b)
-    }
-}
-
-struct LoginView: View {
-    @EnvironmentObject var appState: AppState
-    @State private var server = "http://localhost:8096"
-    @State private var username = ""
-    @State private var password = ""
-    @State private var error: String?
-    @State private var isLoggingIn = false
-    
-    var body: some View {
-        NavigationStack {
-            ZStack {
-                VStack(alignment: .leading, spacing: 16) {
-                    Text("Login to your Jellyfin server")
-                        .font(.title2.bold())
-                        .foregroundStyle(
-                            LinearGradient(
-                                colors: [Color(hex: "#AA5CC3"), Color(hex: "#00A4DC")],
-                                startPoint: .leading,
-                                endPoint: .trailing
-                            )
-                        )
-                        .frame(maxWidth: .infinity, alignment: .leading)
-                        .padding(.horizontal)
-                        .padding(.top)
-                    
-                    Form {
-                        TextField("Server", text: $server)
-                            .autocapitalization(.none)
-                            .disableAutocorrection(true)
-                        TextField("Username", text: $username)
-                            .autocapitalization(.none)
-                            .disableAutocorrection(true)
-                        SecureField("Password", text: $password)
-                            .onSubmit {
-                                performLogin()
-                            }
-                        
-                        if let error = error {
-                            Text(error)
-                                .foregroundColor(.red)
-                        }
-                        
-                        Button("Login") {
-                            performLogin()
-                        }
-                    }
-                    
-                    if isLoggingIn {
-                        ProgressView("Logging in to your Jellyfin server")
-                            .padding()
-                            .frame(maxWidth: .infinity, alignment: .center)
-                            .tint(.red)
-                            .foregroundColor(.red)
-                    }
-                }
-            }
-        }
-        .onAppear {
-            // Generate a new API key every time the app is opened
-            let newApiKey = UUID().uuidString
-            
-            // Save the new API key to Keychain
-            KeychainHelper.save(key: "apiKey", value: newApiKey)
-            
-            appState.restoreLogin()  // Restore login state and API key
-        }
-        .navigationDestination(isPresented: Binding<Bool>(
-            get: { appState.isLoggedIn },
-            set: { appState.isLoggedIn = $0 }
-        )) {
-            if appState.isDemoMode {
-                DemoChannelsView()
-                    .environmentObject(appState)
-            } else {
-                ChannelsView()
-                    .environmentObject(appState)
-            }
-        }
-    }
-    
-    // MARK: - Login logic
-    private func performLogin() {
-        Task {
-            isLoggingIn = true
-            defer { isLoggingIn = false }
-            if let url = URL(string: server) {
-                await appState.login(server: url, username: username, password: password)
-                if let loginError = appState.loginError {
-                    error = loginError
-                }
-            } else {
-                error = "Invalid server URL"
-            }
-        }
-    }
-}
diff --git a/LiveFin/App Specific/ProgramView.swift b/LiveFin/App Specific/ProgramView.swift
deleted file mode 100644
index bb14172..0000000
--- a/LiveFin/App Specific/ProgramView.swift	
+++ /dev/null
@@ -1,678 +0,0 @@
-import SwiftUI
-
-struct ProgramView: View {
-    let program: JFProgram
-    @EnvironmentObject private var appState: AppState
-    @EnvironmentObject private var vm: HomeViewModel
-    @State private var posterRefreshTick = 0
-    @State private var streamURL: URL? // For playback
-    @State private var extendedUpcoming: [JFProgram] = [] // multi-channel upcoming fetched from server
-    @State private var channelSchedule: [JFProgram] = [] // single-channel schedule enrichment
-    @State private var relatedServer: [JFProgram] = [] // server-derived related items
-
-    // Adjusted movie poster target size
-    private let moviePosterWidth: CGFloat = 200
-    private var moviePosterHeight: CGFloat { moviePosterWidth * 1.5 } // 2:3
-
-    private var channelName: String {
-        program.channelName ?? vm.channelName(for: program.channelId) ?? "Unknown Channel"
-    }
-
-    private var timeLine: String {
-        guard let start = program.startDate else { return "" }
-        let end: Date? = {
-            if let e = program.endDate { return e }
-            if let ticks = program.runTimeTicks { return start.addingTimeInterval(TimeInterval(Double(ticks)/10_000_000.0)) }
-            return nil
-        }()
-        let dateFmt = DateFormatter(); dateFmt.dateStyle = .medium; dateFmt.timeStyle = .none
-        let timeFmt = DateFormatter(); timeFmt.dateStyle = .none; timeFmt.timeStyle = .short
-        if let end {
-            if Calendar.current.isDate(start, inSameDayAs: end) {
-                return "\(dateFmt.string(from: start)) • \(timeFmt.string(from: start)) - \(timeFmt.string(from: end))"
-            } else {
-                return "\(dateFmt.string(from: start)) \(timeFmt.string(from: start)) - \(dateFmt.string(from: end)) \(timeFmt.string(from: end))"
-            }
-        } else {
-            return "\(dateFmt.string(from: start)) • \(timeFmt.string(from: start))"
-        }
-    }
-
-    private var progressRatio: Double? {
-        guard let start = program.startDate else { return nil }
-        let end: Date? = program.endDate ?? (program.runTimeTicks.map { start.addingTimeInterval(TimeInterval(Double($0)/10_000_000.0)) })
-        guard let end, start <= Date(), Date() <= end else { return nil }
-        let total = end.timeIntervalSince(start); guard total > 1 else { return nil }
-        let elapsed = Date().timeIntervalSince(start); return min(max(elapsed/total,0),1)
-    }
-
-    private var isLive: Bool { progressRatio != nil }
-
-    // Related programs logic
-    private var relatedPrograms: [JFProgram] {
-        // Prefer server-derived related; fallback to vm only if empty
-        if !relatedServer.isEmpty { return relatedServer }
-        // Fallback (legacy) if server fetch not populated yet
-        var pool: [JFProgram] = []
-        pool.append(contentsOf: vm.onNow + vm.shows + vm.movies + vm.news + vm.sports + vm.kids)
-        let unique = Dictionary(grouping: pool, by: { $0.id }).compactMap { $0.value.first }
-        // Exclude same name (case-insensitive)
-        let filteredOutSameName = unique.filter { $0.name.caseInsensitiveCompare(program.name) != .orderedSame }
-        // Same series first
-        if let series = program.seriesName, !series.isEmpty {
-            let matches = filteredOutSameName.filter { $0.seriesName == series }
-            if !matches.isEmpty { return Array(matches.prefix(12)) }
-        }
-        if let cid = program.channelId {
-            let matches = filteredOutSameName.filter { $0.channelId == cid }
-                .sorted { ($0.startDate ?? .distantFuture) < ($1.startDate ?? .distantFuture) }
-            if !matches.isEmpty { return Array(matches.prefix(12)) }
-        }
-        let baseGenres = Set(program.genres ?? [])
-        if !baseGenres.isEmpty {
-            let matches = filteredOutSameName.filter { !(Set($0.genres ?? []).isDisjoint(with: baseGenres)) }
-            if !matches.isEmpty { return Array(matches.prefix(12)) }
-        }
-        return []
-    }
-
-    // Upcoming episodes/programs with same series or name (future only)
-    private var upcomingPrograms: [JFProgram] { upcomingLocal }
-    private var upcomingLocal: [JFProgram] {
-        // Only use channelSchedule now (no HomeView VM arrays) to avoid reliance on HomeView
-        let now = Date()
-        let targetSeries = program.seriesName?.isEmpty == false ? program.seriesName : nil
-        let baseName = program.name
-        let pool = channelSchedule
-        let filtered = pool.filter { p in
-            guard p.id != program.id, let start = p.startDate, start > now else { return false }
-            if let ts = targetSeries { return p.seriesName == ts }
-            return p.name.caseInsensitiveCompare(baseName) == .orderedSame
-        }
-        return filtered.sorted { ($0.startDate ?? .distantFuture) < ($1.startDate ?? .distantFuture) }
-    }
-    private var combinedUpcoming: [JFProgram] {
-        // Merge local and extended, allowing multiple occurrences of same ProgramId at different times or channels.
-        let displayLimit = 15
-        let all = upcomingLocal + extendedUpcoming
-        guard !all.isEmpty else { return [] }
-        var seen: Set<String> = []
-        var result: [JFProgram] = []
-        for p in all.sorted(by: { ($0.startDate ?? .distantFuture) < ($1.startDate ?? .distantFuture) }) {
-            guard let start = p.startDate else { continue }
-            // Composite key includes id, channel, and start to keep simultaneous multi-channel airings distinct
-            let key = (p.id) + "|" + (p.channelId ?? "") + "|" + String(Int(start.timeIntervalSince1970))
-            if !seen.contains(key) {
-                seen.insert(key)
-                result.append(p)
-                if result.count >= displayLimit { break }
-            }
-        }
-        return result
-    }
-
-    var body: some View {
-        ScrollView {
-            VStack(alignment: .leading, spacing: 20) {
-                // IMAGE AREA
-                if program.isMovie {
-                    VStack { // Center vertically inside available width
-                        ProgramDetailImage(program: program, refreshSeed: posterRefreshTick)
-                            .frame(width: moviePosterWidth, height: moviePosterHeight)
-                            .clipShape(RoundedRectangle(cornerRadius: 20, style: .continuous))
-                            .overlay(alignment: .bottomLeading) { progressOverlay }
-                            .shadow(radius: 8, y: 4)
-                    }
-                    .frame(maxWidth: .infinity)
-                    .padding(.top)
-                } else {
-                    ProgramDetailImage(program: program, refreshSeed: posterRefreshTick)
-                        .frame(maxWidth: .infinity)
-                        .frame(height: 220)
-                        .clipShape(RoundedRectangle(cornerRadius: 16, style: .continuous))
-                        .overlay(alignment: .bottomLeading) { progressOverlay }
-                        .padding(.top)
-                }
-
-                // Title & subtitle with SxxEyy prefix
-                VStack(alignment: .leading, spacing: 8) {
-                    Text(program.name)
-                        .font(.title.bold())
-                        .fixedSize(horizontal: false, vertical: true)
-                    if let subtitleLine = primarySubtitleLine() {
-                        Text(subtitleLine)
-                            .font(.headline)
-                            .foregroundColor(.secondary)
-                    }
-                }
-
-                // Rating row (consistent styling capsule)
-                if let rating = program.officialRating, !rating.isEmpty {
-                    HStack {
-                        Text(rating)
-                            .font(.subheadline)
-                            .padding(.horizontal, 10)
-                            .padding(.vertical, 6)
-                            .background(Color.gray.opacity(0.2))
-                            .clipShape(Capsule())
-                        Spacer()
-                    }
-                }
-
-                // Channel row
-                if let channelId = program.channelId {
-                    HStack(alignment: .center, spacing: 12) {
-                        ChannelImageView(baseUrl: appState.serverURL, apiKey: appState.apiKey, channelId: channelId)
-                            .frame(width: 54, height: 54)
-                            .clipShape(RoundedRectangle(cornerRadius: 10, style: .continuous))
-                        VStack(alignment: .leading, spacing: 4) {
-                            if !timeLine.isEmpty { Text(timeLine).font(.subheadline).foregroundColor(.secondary) }
-                            Text(channelName).font(.subheadline)
-                            if isLive { LiveBadge() }
-                        }
-                        Spacer()
-                    }
-                } else if !timeLine.isEmpty || !channelName.isEmpty {
-                    VStack(alignment: .leading, spacing: 4) {
-                        if !timeLine.isEmpty { Text(timeLine).font(.subheadline).foregroundColor(.secondary) }
-                        Text(channelName).font(.subheadline)
-                        if isLive { LiveBadge() }
-                    }
-                }
-
-                // Chips
-                metaChips
-
-                // Overview
-                if let ov = program.overview, !ov.trimmingCharacters(in: .whitespacesAndNewlines).isEmpty {
-                    Text(ov).font(.body).foregroundColor(.primary).fixedSize(horizontal: false, vertical: true)
-                } else {
-                    Text("No description available.").font(.body).foregroundColor(.secondary)
-                }
-
-                // Related
-                if !relatedPrograms.isEmpty {
-                    VStack(alignment: .leading, spacing: 12) {
-                        Text("Related")
-                            .font(.title2).bold()
-                        ScrollView(.horizontal, showsIndicators: false) {
-                            HStack(spacing: 12) {
-                                ForEach(relatedPrograms) { rel in
-                                    NavigationLink(
-                                        destination: ProgramView(program: rel)
-                                            .environmentObject(appState)
-                                            .environmentObject(vm)
-                                    ) {
-                                        RelatedProgramCard(program: rel)
-                                    }
-                                    .buttonStyle(.plain)
-                                }
-                            }.padding(.horizontal, 2)
-                        }
-                    }
-                }
-
-                // Upcoming section
-                if !combinedUpcoming.isEmpty {
-                    VStack(alignment: .leading, spacing: 12) {
-                        Text("Upcoming")
-                            .font(.title2).bold()
-                        VStack(spacing: 0) {
-                            ForEach(combinedUpcoming) { up in
-                                NavigationLink(destination: ProgramView(program: up).environmentObject(appState).environmentObject(vm)) {
-                                    UpcomingProgramRow(program: up, referenceName: program.name)
-                                }
-                                .buttonStyle(.plain)
-                                Divider().padding(.leading, 8)
-                            }
-                        }
-                        .background(RoundedRectangle(cornerRadius: 12).fill(Color(UIColor.secondarySystemBackground)))
-                    }
-                }
-
-                Spacer(minLength: 32)
-            }
-            .padding(.horizontal)
-        }
-        .navigationTitle("")
-        .navigationBarTitleDisplayMode(.inline)
-        .toolbar { toolbarContent }
-        .fullScreenCover(item: $streamURL) { url in
-            VideoPlayerView(
-                streamURL: url,
-                channel: buildChannel(),
-                nextProgramTitle: program.name,
-                nextProgramSubtitle: program.episodeTitle ?? program.overview
-            )
-            .environmentObject(appState)
-        }
-        .task {
-            async let _ = fetchExtendedUpcoming()
-            async let _ = fetchChannelSchedule()
-            async let _ = fetchRelatedPrograms()
-            _ = await ((),(),())
-        }
-    }
-
-    // Progress overlay extracted
-    @ViewBuilder private var progressOverlay: some View {
-        if let p = progressRatio, p > 0, p < 1 {
-            ProgressView(value: p)
-                .progressViewStyle(.linear)
-                .tint(.white)
-                .background(Color.black.opacity(0.3))
-                .frame(width: 160)
-                .padding(8)
-        }
-    }
-
-    @ToolbarContentBuilder private var toolbarContent: some ToolbarContent {
-        if isLive, program.channelId != nil {
-            ToolbarItem(placement: .navigationBarTrailing) {
-                Button { Task { await startPlayback() } } label: {
-                    Image(systemName: "play.circle.fill")
-                        .resizable()
-                        .frame(width: 28, height: 28)
-                        .foregroundColor(.accentColor)
-                }
-                .accessibilityLabel("Play Live")
-            }
-        }
-    }
-
-    private func startPlayback() async {
-        guard let cid = program.channelId else { return }
-        if let stream = await LiveTVStreamService.fetchStreamURL(appState: appState, channelId: cid), let url = URL(string: stream) {
-            await MainActor.run { self.streamURL = url }
-        }
-    }
-
-    private func buildChannel() -> LiveTvChannelDto? {
-        guard let cid = program.channelId else { return nil }
-        return LiveTvChannelDto(
-            id: cid,
-            name: channelName,
-            number: nil,
-            startDate: program.startDate,
-            endDate: program.endDate,
-            baseURL: appState.serverURL
-        )
-    }
-
-    @ViewBuilder private var metaChips: some View {
-        let chips = buildChips()
-        if !chips.isEmpty {
-            ScrollView(.horizontal, showsIndicators: false) {
-                HStack(spacing: 8) {
-                    ForEach(chips, id: \.self) { chip in
-                        Text(chip)
-                            .font(.caption)
-                            .padding(.horizontal, 10)
-                            .padding(.vertical, 6)
-                            .background(Color.accentColor.opacity(0.15))
-                            .foregroundColor(.accentColor)
-                            .clipShape(Capsule())
-                    }
-                }
-                .padding(.vertical, 2)
-            }
-        }
-    }
-
-    private func buildChips() -> [String] {
-        var arr: [String] = []
-        // Hide duplicate Movie/Series chips per request
-        if program.isNews { arr.append("News") }
-        if program.isSports { arr.append("Sports") }
-        if program.isKids { arr.append("Kids") }
-        // Rating removed (separate row)
-        if let genres = program.genres, !genres.isEmpty { arr.append(contentsOf: genres.prefix(3)) }
-        return arr
-    }
-
-    // Helper to build subtitle with SxxEyy prefix
-    private func primarySubtitleLine() -> String? {
-        var parts: [String] = []
-        if let s = program.parentIndexNumber, let e = program.indexNumber {
-            parts.append(String(format: "S%02dE%02d", s, e))
-        }
-        if let ep = program.episodeTitle, !ep.isEmpty { parts.append(ep) }
-        else if let series = program.seriesName, !series.isEmpty, series != program.name { parts.append(series) }
-        guard !parts.isEmpty else { return nil }
-        return parts.joined(separator: " • ")
-    }
-}
-
-// Upcoming row view
-private struct UpcomingProgramRow: View {
-    let program: JFProgram
-    let referenceName: String
-    @EnvironmentObject private var vm: HomeViewModel
-    private var showName: Bool { program.name.caseInsensitiveCompare(referenceName) != .orderedSame }
-    private var seasonEpisode: String? {
-        if let s = program.parentIndexNumber, let e = program.indexNumber { return String(format: "S%02dE%02d", s, e) }
-        return nil
-    }
-    private var subtitle: String? {
-        if let ep = program.episodeTitle, !ep.isEmpty { return ep }
-        if let series = program.seriesName, !series.isEmpty, series != program.name { return series }
-        return nil
-    }
-    private var channelName: String? { program.channelName ?? vm.channelName(for: program.channelId) }
-    var body: some View {
-        HStack(alignment: .top, spacing: 12) {
-            VStack(alignment: .leading, spacing: 4) {
-                if showName { Text(program.name).font(.headline).lineLimit(2) }
-                let lineParts = [seasonEpisode, subtitle].compactMap { $0 }
-                if !lineParts.isEmpty {
-                    Text(lineParts.joined(separator: " • "))
-                        .font(.subheadline)
-                        .foregroundColor(.secondary)
-                        .lineLimit(2)
-                }
-                if let s = program.startDate, let e = program.endDate ?? program.startDate?.addingTimeInterval(program.runTimeSeconds) {
-                    Text("\(s.formatted(date: .omitted, time: .shortened)) - \(e.formatted(date: .omitted, time: .shortened))")
-                        .font(.caption)
-                        .foregroundColor(.secondary)
-                } else if let s = program.startDate {
-                    Text(s.formatted(date: .omitted, time: .shortened))
-                        .font(.caption)
-                        .foregroundColor(.secondary)
-                }
-                if let cn = channelName, !cn.isEmpty { Text(cn).font(.caption2).foregroundColor(.secondary) }
-            }
-            Spacer()
-            if isFuture(program: program) { Image(systemName: "chevron.right").font(.caption).foregroundColor(.secondary) }
-        }
-        .padding(.horizontal, 12)
-        .padding(.vertical, 8)
-    }
-    private func isFuture(program: JFProgram) -> Bool { (program.startDate ?? Date.distantPast) > Date() }
-}
-
-// Helpers
-private extension JFProgram {
-    var runTimeSeconds: TimeInterval { if let t = runTimeTicks { return Double(t)/10_000_000.0 } else { return 0 } }
-}
-
-// Collection helpers used for upcoming/related selections
-private extension Array where Element: Hashable {
-    func uniqued() -> [Element] {
-        // NOTE: Keep existing helper (used elsewhere) but no longer used for upcoming airings to allow duplicates per time/channel.
-        var seen: Set<Element> = []
-        return self.filter { seen.insert($0).inserted }
-    }
-}
-private extension Array {
-    func prefixArray(_ n: Int) -> [Element] { Array(prefix(n)) }
-}
-
-// Supporting small views
-private struct LiveBadge: View {
-    var body: some View {
-        HStack(spacing: 4) {
-            Circle().fill(Color.red).frame(width: 10, height: 10)
-            Text("LIVE").font(.caption).foregroundColor(.red).bold()
-        }
-    }
-}
-
-private struct RelatedProgramCard: View {
-    let program: JFProgram
-    @EnvironmentObject private var appState: AppState
-    // Dynamic sizing: movies use portrait poster like HomeView (120x180), others keep existing landscape style
-    private var isMovie: Bool { program.isMovie }
-    private var imageWidth: CGFloat { isMovie ? 120 : 140 }
-    private var imageHeight: CGFloat { isMovie ? 180 : 80 }
-    var body: some View {
-        VStack(alignment: .leading, spacing: 6) {
-            ZStack {
-                if let url = imageURL() {
-                    AsyncImage(url: url) { phase in
-                        switch phase {
-                        case .empty: ZStack { Color(UIColor.secondarySystemBackground); ProgressView() }
-                        case .success(let img): img.resizable().scaledToFill()
-                        case .failure: placeholder
-                        @unknown default: placeholder
-                        }
-                    }
-                } else { placeholder }
-            }
-            .frame(width: imageWidth, height: imageHeight)
-            .clipShape(RoundedRectangle(cornerRadius: 8, style: .continuous))
-            Text(program.name)
-                .font(.caption)
-                .lineLimit(2)
-                .frame(width: imageWidth, alignment: .leading)
-            if let ep = program.episodeTitle, !ep.isEmpty, !isMovie { // keep episode subtitle only for non-movie items
-                Text(ep).font(.caption2).foregroundColor(.secondary).lineLimit(1)
-            }
-        }
-        .frame(width: imageWidth)
-    }
-    private var placeholder: some View { ZStack { Color(UIColor.secondarySystemBackground); Image(systemName: "film").foregroundColor(.secondary) } }
-    private func imageURL() -> URL? {
-        guard !appState.serverURL.isEmpty, !appState.apiKey.isEmpty else { return nil }
-        let base = appState.serverURL.trimmingCharacters(in: CharacterSet(charactersIn: "/"))
-        var comps = URLComponents(string: base + "/Items/\(program.id)/Images/Primary")
-        // Increase requested size slightly for portraits to retain quality
-        let maxWidth = isMovie ? "600" : "400"
-        comps?.queryItems = [URLQueryItem(name: "maxWidth", value: maxWidth), URLQueryItem(name: "api_key", value: appState.apiKey)]
-        return comps?.url
-    }
-}
-
-// Existing ProgramDetailImage below (unchanged except kept at bottom)
-private struct ProgramDetailImage: View {
-    let program: JFProgram
-    let refreshSeed: Int
-    @EnvironmentObject private var appState: AppState
-    var body: some View {
-        GeometryReader { geo in
-            if let url = imageURL(maxWidth: Int(geo.size.width * 2)) {
-                AsyncImage(url: url) { phase in
-                    switch phase {
-                    case .empty:
-                        ZStack { ProgressView() }
-                            .frame(width: geo.size.width, height: geo.size.height)
-                            .background(Color(UIColor.secondarySystemBackground))
-                    case .success(let img): img.resizable().scaledToFill()
-                    case .failure: placeholder
-                    @unknown default: placeholder
-                    }
-                }
-            } else { placeholder }
-        }
-    }
-    private var placeholder: some View { ZStack { Image(systemName: "film").imageScale(.large).foregroundColor(.secondary) }.background(Color(UIColor.secondarySystemBackground)) }
-    private func imageURL(maxWidth: Int = 800) -> URL? {
-        guard !appState.serverURL.isEmpty, !appState.apiKey.isEmpty else { return nil }
-        let base = appState.serverURL.trimmingCharacters(in: CharacterSet(charactersIn: "/"))
-        let path = "/Items/\(program.id)/Images/Primary"
-        var comps = URLComponents(string: base + path)
-        comps?.queryItems = [
-            URLQueryItem(name: "maxWidth", value: String(maxWidth)),
-            URLQueryItem(name: "api_key", value: appState.apiKey),
-            URLQueryItem(name: "t", value: String(Int(Date().timeIntervalSince1970))),
-            URLQueryItem(name: "seed", value: String(refreshSeed))
-        ]
-        return comps?.url
-    }
-}
-
-private extension ProgramView {
-    func fetchExtendedUpcoming() async {
-        if !extendedUpcoming.isEmpty { return }
-        guard !appState.serverURL.isEmpty else { return }
-        let targetSeries = program.seriesName?.trimmingCharacters(in: .whitespacesAndNewlines)
-        let baseName = program.name.trimmingCharacters(in: .whitespacesAndNewlines)
-        if (targetSeries == nil || targetSeries?.isEmpty == true) && baseName.isEmpty { return }
-        let now = Date()
-        // Extend search window to 7 days to increase the number of future airings collected
-        guard let end = Calendar.current.date(byAdding: .day, value: 7, to: now) else { return }
-        let iso = ISO8601DateFormatter(); iso.formatOptions = [.withInternetDateTime]
-        let startStr = iso.string(from: now)
-        let endStr = iso.string(from: end)
-        guard let base = URL(string: appState.serverURL)?.appendingPathComponent("/LiveTv/Programs") else { return }
-        var comps = URLComponents(url: base, resolvingAgainstBaseURL: false)
-        comps?.queryItems = [
-            URLQueryItem(name: "startDate", value: startStr),
-            URLQueryItem(name: "endDate", value: endStr),
-            URLQueryItem(name: "Limit", value: "1500"), // bump slightly for broader window
-            URLQueryItem(name: "Fields", value: "Overview,OfficialRating,Genres,SeriesName,EpisodeTitle,RunTimeTicks,ParentIndexNumber,IndexNumber,ChannelId,ChannelName,IsRepeat")
-        ]
-        if let uid = appState.user?.id { comps?.queryItems?.append(URLQueryItem(name: "userId", value: uid)) }
-        guard let url = comps?.url else { return }
-        var req = URLRequest(url: url); req.httpMethod = "GET"
-        if !appState.accessToken.isEmpty { req.setValue(appState.accessToken, forHTTPHeaderField: "X-Emby-Token") }
-        do {
-            let (data, resp) = try await URLSession.shared.data(for: req)
-            guard let http = resp as? HTTPURLResponse, http.statusCode == 200 else { return }
-            var parsed: [JFProgram] = []
-            if let arr = try? JSONSerialization.jsonObject(with: data) as? [[String: Any]] {
-                parsed = arr.compactMap { JFProgram(json: $0) }
-            } else if let obj = try? JSONSerialization.jsonObject(with: data) as? [String: Any], let arr = obj["Items"] as? [[String: Any]] {
-                parsed = arr.compactMap { JFProgram(json: $0) }
-            }
-            let filtered = parsed.filter { p in
-                guard p.id != program.id, let start = p.startDate, start > now else { return false }
-                if let ts = targetSeries, let sName = p.seriesName, !ts.isEmpty { return sName == ts }
-                return p.name.caseInsensitiveCompare(baseName) == .orderedSame
-            }
-            var seen: Set<String> = []
-            let deduped = filtered.filter { p in
-                let key = p.id + "|" + (p.channelId ?? "") + "|" + String(Int(p.startDate?.timeIntervalSince1970 ?? 0))
-                if seen.contains(key) { return false }
-                seen.insert(key); return true
-            }
-            let hydrated: [JFProgram] = deduped.map { p in
-                if p.channelName == nil, let cid = p.channelId, let _ = vm.channelName(for: cid) { return p }
-                return p
-            }
-            await MainActor.run { self.extendedUpcoming = hydrated.sorted { ($0.startDate ?? .distantFuture) < ($1.startDate ?? .distantFuture) } }
-        } catch { }
-    }
-    
-    func fetchChannelSchedule() async {
-        // Fetch only this channel's schedule to enrich upcoming (similar to ChannelDetailView)
-        guard channelSchedule.isEmpty else { return }
-        guard let cid = program.channelId, !appState.serverURL.isEmpty else { return }
-        let now = Date()
-        guard let end = Calendar.current.date(byAdding: .day, value: 1, to: now) else { return }
-        let iso = ISO8601DateFormatter(); iso.formatOptions = [.withInternetDateTime]
-        guard let base = URL(string: appState.serverURL)?.appendingPathComponent("/LiveTv/Programs") else { return }
-        var comps = URLComponents(url: base, resolvingAgainstBaseURL: false)
-        comps?.queryItems = [
-            URLQueryItem(name: "channelIds", value: cid),
-            URLQueryItem(name: "startDate", value: iso.string(from: now)),
-            URLQueryItem(name: "endDate", value: iso.string(from: end)),
-            URLQueryItem(name: "Limit", value: "1000"),
-            URLQueryItem(name: "Fields", value: "Overview,OfficialRating,Genres,SeriesName,EpisodeTitle,RunTimeTicks,ParentIndexNumber,IndexNumber,ChannelId,ChannelName,IsRepeat")
-        ]
-        if let uid = appState.user?.id { comps?.queryItems?.append(URLQueryItem(name: "userId", value: uid)) }
-        guard let url = comps?.url else { return }
-        var req = URLRequest(url: url); req.httpMethod = "GET"
-        if !appState.accessToken.isEmpty { req.setValue(appState.accessToken, forHTTPHeaderField: "X-Emby-Token") }
-        do {
-            let (data, resp) = try await URLSession.shared.data(for: req)
-            guard let http = resp as? HTTPURLResponse, http.statusCode == 200 else { return }
-            var parsed: [JFProgram] = []
-            if let arr = try? JSONSerialization.jsonObject(with: data) as? [[String: Any]] {
-                parsed = arr.compactMap { JFProgram(json: $0) }
-            } else if let obj = try? JSONSerialization.jsonObject(with: data) as? [String: Any], let arr = obj["Items"] as? [[String: Any]] {
-                parsed = arr.compactMap { JFProgram(json: $0) }
-            }
-            // Keep only those with future start for upcoming context
-            let now = Date()
-            let future = parsed.filter { ($0.startDate ?? .distantPast) > now }
-            await MainActor.run { self.channelSchedule = future }
-        } catch { }
-    }
-    
-    func fetchRelatedPrograms() async {
-        // Build a broader related set from server (genres, same channel timeframe overlap, same series) independent of HomeView
-        guard relatedServer.isEmpty else { return }
-        guard !appState.serverURL.isEmpty else { return }
-        let now = Date()
-        // Fetch small window: past 6h to next 48h for context
-        guard let past = Calendar.current.date(byAdding: .hour, value: -6, to: now),
-              let future = Calendar.current.date(byAdding: .hour, value: 48, to: now) else { return }
-        let iso = ISO8601DateFormatter(); iso.formatOptions = [.withInternetDateTime]
-        guard let base = URL(string: appState.serverURL)?.appendingPathComponent("/LiveTv/Programs") else { return }
-        var comps = URLComponents(url: base, resolvingAgainstBaseURL: false)
-        comps?.queryItems = [
-            URLQueryItem(name: "startDate", value: iso.string(from: past)),
-            URLQueryItem(name: "endDate", value: iso.string(from: future)),
-            URLQueryItem(name: "Limit", value: "2000"),
-            URLQueryItem(name: "Fields", value: "Overview,OfficialRating,Genres,SeriesName,EpisodeTitle,RunTimeTicks,ParentIndexNumber,IndexNumber,ChannelId,ChannelName,IsRepeat")
-        ]
-        if let uid = appState.user?.id { comps?.queryItems?.append(URLQueryItem(name: "userId", value: uid)) }
-        guard let url = comps?.url else { return }
-        var req = URLRequest(url: url); req.httpMethod = "GET"
-        if !appState.accessToken.isEmpty { req.setValue(appState.accessToken, forHTTPHeaderField: "X-Emby-Token") }
-        do {
-            let (data, resp) = try await URLSession.shared.data(for: req)
-            guard let http = resp as? HTTPURLResponse, http.statusCode == 200 else { return }
-            var parsed: [JFProgram] = []
-            if let arr = try? JSONSerialization.jsonObject(with: data) as? [[String: Any]] {
-                parsed = arr.compactMap { JFProgram(json: $0) }
-            } else if let obj = try? JSONSerialization.jsonObject(with: data) as? [String: Any], let arr = obj["Items"] as? [[String: Any]] {
-                parsed = arr.compactMap { JFProgram(json: $0) }
-            }
-            // Filtering logic: exclude current program, exclude identical name unless different series/episode (avoid duplicates), then rank.
-            let baseGenres = Set(program.genres ?? [])
-            let seriesName = program.seriesName
-            let sameChannel = program.channelId
-            var scored: [(JFProgram, Int)] = []
-            for p in parsed where p.id != program.id {
-                var score = 0
-                if let s = seriesName, !s.isEmpty, p.seriesName == s { score += 50 }
-                if let cid = sameChannel, let pcid = p.channelId, cid == pcid { score += 15 }
-                let overlap = baseGenres.intersection(Set(p.genres ?? []))
-                if !overlap.isEmpty { score += overlap.count * 5 }
-                if p.name.caseInsensitiveCompare(program.name) == .orderedSame { score += 5 } // same base title
-                // Deprioritize if entirely unrelated
-                if score > 0 { scored.append((p, score)) }
-            }
-            let sorted = scored.sorted { lhs, rhs in
-                if lhs.1 == rhs.1 {
-                    return (lhs.0.startDate ?? .distantFuture) < (rhs.0.startDate ?? .distantFuture)
-                }
-                return lhs.1 > rhs.1
-            }.map { $0.0 }
-            // Deduplicate by (id|channel|start)
-            var seen: Set<String> = []
-            var final: [JFProgram] = []
-            for p in sorted {
-                let key = p.id + "|" + (p.channelId ?? "") + "|" + String(Int(p.startDate?.timeIntervalSince1970 ?? 0))
-                if !seen.contains(key) {
-                    seen.insert(key)
-                    final.append(p)
-                }
-                if final.count >= 20 { break }
-            }
-            await MainActor.run { self.relatedServer = final }
-        } catch { }
-    }
-}
-
-#if DEBUG
-struct ProgramView_Previews: PreviewProvider {
-    static var previews: some View {
-        let sample = JFProgram(json: [
-            "Id": "demo1",
-            "Name": "Sample Program",
-            "Overview": "Demo overview for ProgramView detail screen.",
-            "StartDate": ISO8601DateFormatter().string(from: Date().addingTimeInterval(-1200)),
-            "EndDate": ISO8601DateFormatter().string(from: Date().addingTimeInterval(1800)),
-            "IsSeries": true,
-            "OfficialRating": "TV-PG",
-            "Genres": ["Drama", "Adventure"],
-            "ChannelId": "chan1",
-            "SeriesName": "Sample Series"
-        ])!
-        let vm = HomeViewModel()
-        NavigationStack { ProgramView(program: sample).environmentObject(AppState()).environmentObject(vm) }
-    }
-}
-#endif
diff --git a/LiveFin/App Specific/RootView.swift b/LiveFin/App Specific/RootView.swift
index 131c425..6936cb0 100644
--- a/LiveFin/App Specific/RootView.swift	
+++ b/LiveFin/App Specific/RootView.swift	
@@ -10,28 +10,40 @@ import SwiftUI
 
 struct RootView: View {
     @EnvironmentObject var appState: AppState
+    @State private var searchQuery: String = ""
 
     var body: some View {
         if appState.isLoggedIn {
             if appState.isDemoMode {
-                DemoChannelsView()
-            } else {
-                TabView {
-                    HomeView()
-                        .tabItem {
-                            Label("Home", systemImage: "house.fill")
-                        }
-
-                    ChannelsView()
-                        .tabItem {
-                            Label("Channels", systemImage: "tv.fill")
-                        }
-
-                    GuideView()
-                        .tabItem {
-                            Label("Guide", systemImage: "list.bullet.rectangle")
-                        }
+                VStack(spacing: 0) {
+                    TabView {
+                        DemoHomeView()
+                            .tabItem { Label("Home", systemImage: "house.fill") }
+
+                        DemoChannelsView()
+                            .tabItem { Label("Channels", systemImage: "tv.fill") }
+
+                        DemoGuideView()
+                            .tabItem { Label("Guide", systemImage: "list.bullet.rectangle") }
+                    }
                 }
+            } else {
+                    TabView {
+                        HomeView()
+                            .tabItem {
+                                Label("Home", systemImage: "house.fill")
+                            }
+
+                        ChannelsView()
+                            .tabItem {
+                                Label("Channels", systemImage: "tv.fill")
+                            }
+
+                        GuideView()
+                            .tabItem {
+                                Label("Guide", systemImage: "list.bullet.rectangle")
+                            }
+                    }
             }
         } else {
             LoginView()
diff --git a/LiveFin/App Specific/ToolbarView.swift b/LiveFin/App Specific/ToolbarView.swift
deleted file mode 100644
index 06cd1bc..0000000
--- a/LiveFin/App Specific/ToolbarView.swift	
+++ /dev/null
@@ -1,42 +0,0 @@
-import SwiftUI
-
-struct ToolbarView: View {
-    @EnvironmentObject var appState: AppState
-    @State private var showAbout = false
-    var body: some View {
-        Menu {
-            // About Button
-            Button(action: { showAbout = true }) {
-                Label("About", systemImage: "info.circle")
-            }
-            // Discord Button
-            Button(action: {
-                if let url = URL(string: "https://discord.gg/xGdey3dxQN") {
-                    UIApplication.shared.open(url)
-                }
-            }) {
-                Label {
-                    Text("Discord")
-                } icon: {
-                    Image("Discord Logo")
-                        .resizable()
-                        .scaledToFit()
-                        .frame(width: 18, height: 18)
-                }
-            }
-
-            // Logout Button
-            Button(role: .destructive, action: {
-                appState.logout()
-            }) {
-                Label("Logout", systemImage: "person.crop.circle.badge.minus")
-            }
-        } label: {
-            Image(systemName: "person.crop.circle")
-                .font(.title2)
-        }
-        .sheet(isPresented: $showAbout) {
-            AboutView()
-        }
-    }
-}
diff --git a/LiveFin/App Specific/VideoPlayerView.swift b/LiveFin/App Specific/VideoPlayerView.swift
deleted file mode 100644
index 5977b47..0000000
--- a/LiveFin/App Specific/VideoPlayerView.swift	
+++ /dev/null
@@ -1,372 +0,0 @@
-//
-//  VideoPlayerView.swift
-//  LiveFin
-//
-//  Created by KPGamingz on 5/6/25.
-//
-
-import SwiftUI
-import AVKit
-import UIKit
-import MediaPlayer
-
-struct VideoPlayerView: View {
-    let streamURL: URL
-    let channel: LiveTvChannelDto?
-    let nextProgramTitle: String?
-    let nextProgramSubtitle: String?
-
-    @EnvironmentObject var appState: AppState
-    @Environment(\.dismiss) private var dismiss
-
-    var body: some View {
-        ZStack(alignment: .topLeading) {
-            PlayerViewControllerRepresentable(
-                streamURL: streamURL,
-                channel: channel,
-                nextProgramTitle: nextProgramTitle,
-                nextProgramSubtitle: nextProgramSubtitle,
-                appState: appState,
-                onClose: { dismiss() }
-            )
-            .ignoresSafeArea()
-        }
-        .onAppear {
-            if let windowScene = UIApplication.shared.connectedScenes.first as? UIWindowScene {
-                let geometryPreferences = UIWindowScene.GeometryPreferences.iOS(interfaceOrientations: .landscapeRight)
-                windowScene.requestGeometryUpdate(geometryPreferences)
-            }
-            AppDelegate.orientationLock = .landscapeRight
-        }
-        .onDisappear {
-            if let windowScene = UIApplication.shared.connectedScenes.first as? UIWindowScene {
-                let geometryPreferences = UIWindowScene.GeometryPreferences.iOS(interfaceOrientations: .portrait)
-                windowScene.requestGeometryUpdate(geometryPreferences)
-            }
-            AppDelegate.orientationLock = .portrait
-        }
-    }
-}
-
-// MARK: - UIKit AVPlayerViewController wrapper
-struct PlayerViewControllerRepresentable: UIViewControllerRepresentable {
-    let streamURL: URL
-    let channel: LiveTvChannelDto?
-    let nextProgramTitle: String?
-    let nextProgramSubtitle: String?
-    let appState: AppState
-    let onClose: () -> Void
-
-    func makeCoordinator() -> Coordinator { Coordinator(self) }
-
-    func makeUIViewController(context: Context) -> AVPlayerViewController {
-        let controller = AVPlayerViewController()
-        controller.delegate = context.coordinator
-        controller.allowsPictureInPicturePlayback = true
-        if #available(iOS 14.2, *) {
-            controller.canStartPictureInPictureAutomaticallyFromInline = true
-        }
-        controller.exitsFullScreenWhenPlaybackEnds = false
-        controller.showsPlaybackControls = true
-
-        // Build AVPlayer with headers
-        var headers: [String: String] = ["User-Agent": "LiveFin iOS"]
-        headers["Accept"] = "application/vnd.apple.mpegurl, application/x-mpegURL, */*"
-        if !appState.accessToken.isEmpty { headers["X-Emby-Token"] = appState.accessToken }
-        // Include X-Emby-Authorization so server applies correct Device Profile
-        let authHeader = "MediaBrowser Client=\"LiveFin\", Device=\"\(appState.clientDevice)\", DeviceId=\"\(appState.deviceId)\", Version=\"\(appState.clientVersion)\", Token=\"\(appState.accessToken)\""
-        headers["X-Emby-Authorization"] = authHeader
-        // Redacted stream URL log for debugging
-        let redactedURL = appState.accessToken.isEmpty ? streamURL.absoluteString : streamURL.absoluteString.replacingOccurrences(of: appState.accessToken, with: "<redacted>")
-        print("[Player] Using stream URL: \(redactedURL)")
-        let headerOptions = ["AVURLAssetHTTPHeaderFieldsKey": headers]
-        let asset = AVURLAsset(url: streamURL, options: headerOptions)
-        let item = AVPlayerItem(asset: asset)
-        let player = AVPlayer(playerItem: item)
-        controller.player = player
-
-        // Store references
-        context.coordinator.controller = controller
-        context.coordinator.player = player
-        context.coordinator.appState = appState
-        context.coordinator.channelId = channel?.id
-        context.coordinator.currentItemId = channel?.id
-        context.coordinator.onClose = onClose
-        context.coordinator.titleOverride = nextProgramTitle
-        context.coordinator.subtitleOverride = nextProgramSubtitle
-
-        // Inject metadata so AVPlayerViewController shows it in controls
-        context.coordinator.refreshMetadata()
-
-        // Start playback and observers
-        context.coordinator.startPlayback()
-
-        return controller
-    }
-
-    func updateUIViewController(_ controller: AVPlayerViewController, context: Context) {
-        // Update metadata if app state or overrides changed
-        context.coordinator.appState = appState
-        context.coordinator.titleOverride = nextProgramTitle
-        context.coordinator.subtitleOverride = nextProgramSubtitle
-        context.coordinator.refreshMetadata()
-    }
-
-    static func dismantleUIViewController(_ controller: AVPlayerViewController, coordinator: Coordinator) {
-        coordinator.cleanupOnExit()
-    }
-
-    // MARK: - Coordinator
-    final class Coordinator: NSObject, AVPlayerViewControllerDelegate {
-        var parent: PlayerViewControllerRepresentable
-        weak var controller: AVPlayerViewController?
-        var player: AVPlayer?
-        var appState: AppState?
-        var channelId: String?
-        var currentItemId: String?
-        var progressObserver: Any?
-        var pauseKVO: NSKeyValueObservation?
-        var tcsKVO: NSKeyValueObservation?
-        // Removed custom overlays and close button
-        var onClose: (() -> Void)?
-
-        // Display overrides from SwiftUI
-        var titleOverride: String?
-        var subtitleOverride: String?
-
-        // New: error/status logging
-        var itemStatusKVO: NSKeyValueObservation?
-        var itemFailedObserver: NSObjectProtocol?
-        var itemEndObserver: NSObjectProtocol?
-        var itemErrorLogObserver: NSObjectProtocol?
-
-        init(_ parent: PlayerViewControllerRepresentable) { self.parent = parent }
-
-        func startPlayback() {
-            guard let player = player else { return }
-            // Report start on main actor
-            if let itemId = currentItemId, let app = appState {
-                Task { @MainActor in
-                    app.reportPlaybackStart(itemId: itemId, canSeek: false)
-                }
-            }
-            // Start EPG polling on main actor
-            if let ch = channelId, let app = appState {
-                Task { @MainActor in
-                    app.startEPGPolling(for: ch)
-                }
-            }
-            // Observers
-            addObservers()
-            observeItem(player.currentItem)
-            player.play()
-        }
-
-        func addObservers() {
-            guard let player = player else { return }
-            if let itemId = currentItemId, let appState = appState {
-                let interval = CMTime(seconds: 10, preferredTimescale: CMTimeScale(NSEC_PER_SEC))
-                progressObserver = player.addPeriodicTimeObserver(forInterval: interval, queue: .main) { time in
-                    let seconds = CMTimeGetSeconds(time)
-                    let ticks = Int64(seconds * 10_000_000)
-                    Task { @MainActor in
-                        appState.reportPlaybackProgress(itemId: itemId, positionTicks: ticks, canSeek: false)
-                    }
-                }
-            }
-            pauseKVO = player.observe(\.rate, options: [.initial, .new]) { [weak self] player, _ in
-                guard let self = self, let itemId = self.currentItemId, let app = self.appState else { return }
-                let paused = player.rate == 0
-                let seconds = player.currentTime().seconds
-                let ticks = Int64(seconds * 10_000_000)
-                Task { @MainActor in
-                    app.reportPlaybackProgress(itemId: itemId, positionTicks: ticks, canSeek: false, isPaused: paused)
-                }
-            }
-            if #available(iOS 10.0, *) {
-                tcsKVO = player.observe(\.timeControlStatus, options: [.initial, .new]) { [weak self] player, _ in
-                    guard let self = self else { return }
-                    switch player.timeControlStatus {
-                    case .waitingToPlayAtSpecifiedRate:
-                        if let reason = player.reasonForWaitingToPlay {
-                            print("[Player] Waiting to play: \(reason)")
-                        } else {
-                            print("[Player] Waiting to play at specified rate")
-                        }
-                    case .playing:
-                        print("[Player] Playing")
-                    case .paused:
-                        print("[Player] Paused")
-                    @unknown default:
-                        print("[Player] Unknown time control status")
-                    }
-                }
-            }
-        }
-
-        private func observeItem(_ item: AVPlayerItem?) {
-            guard let item = item else { return }
-            itemStatusKVO = item.observe(\.status, options: [.initial, .new]) { item, _ in
-                switch item.status {
-                case .readyToPlay:
-                    print("[Item] Ready to play")
-                case .failed:
-                    print("[Item] Failed: \(String(describing: item.error))")
-                    if let logs = item.errorLog()?.events {
-                        for e in logs { print("[Item] ErrorLog: \(e.errorComment ?? "<no comment>")") }
-                    }
-                case .unknown:
-                    print("[Item] Status unknown")
-                @unknown default:
-                    print("[Item] Status unknown (future)")
-                }
-            }
-            let center = NotificationCenter.default
-            itemFailedObserver = center.addObserver(forName: .AVPlayerItemFailedToPlayToEndTime, object: item, queue: .main) { note in
-                let err = (note.userInfo?[AVPlayerItemFailedToPlayToEndTimeErrorKey] as? NSError)
-                print("[Item] FailedToPlayToEnd: \(String(describing: err))")
-            }
-            itemEndObserver = center.addObserver(forName: .AVPlayerItemDidPlayToEndTime, object: item, queue: .main) { _ in
-                print("[Item] DidPlayToEndTime")
-            }
-            itemErrorLogObserver = center.addObserver(forName: .AVPlayerItemNewErrorLogEntry, object: item, queue: .main) { _ in
-                if let logs = item.errorLog()?.events {
-                    for e in logs { print("[Item] NewErrorLogEntry: \(e.errorStatusCode) \(e.errorDomain) \(e.errorComment ?? "")") }
-                }
-            }
-        }
-
-        func cleanupOnExit() {
-            if let token = progressObserver, let player = player {
-                player.removeTimeObserver(token)
-                progressObserver = nil
-            }
-            pauseKVO?.invalidate(); pauseKVO = nil
-            tcsKVO?.invalidate(); tcsKVO = nil
-            itemStatusKVO?.invalidate(); itemStatusKVO = nil
-            let center = NotificationCenter.default
-            if let obs = itemFailedObserver { center.removeObserver(obs) }
-            if let obs = itemEndObserver { center.removeObserver(obs) }
-            if let obs = itemErrorLogObserver { center.removeObserver(obs) }
-            itemFailedObserver = nil; itemEndObserver = nil; itemErrorLogObserver = nil
-            // Report stop if still active
-            if let itemId = currentItemId, let app = appState, let player = player {
-                let seconds = player.currentTime().seconds
-                let ticks = Int64(seconds * 10_000_000)
-                Task { @MainActor in app.reportPlaybackStopped(itemId: itemId, positionTicks: ticks) }
-            }
-            if let _ = channelId, let app = appState {
-                Task { @MainActor in app.stopEPGPolling() }
-            }
-            player?.pause()
-            // Clear Now Playing
-            MPNowPlayingInfoCenter.default().nowPlayingInfo = nil
-        }
-
-        // MARK: - Metadata integration
-        func refreshMetadata() {
-            guard let item = player?.currentItem else { return }
-            let title = computeDisplayTitle()
-            let subtitle = computeDisplaySubtitle()
-
-            var metadataItems: [AVMutableMetadataItem] = []
-
-            let titleItem = AVMutableMetadataItem()
-            titleItem.identifier = .commonIdentifierTitle
-            titleItem.keySpace = .common
-            titleItem.value = title as NSString
-            metadataItems.append(titleItem)
-
-            if !subtitle.isEmpty {
-                let subItem = AVMutableMetadataItem()
-                subItem.identifier = .commonIdentifierDescription
-                subItem.keySpace = .common
-                subItem.value = subtitle as NSString
-                metadataItems.append(subItem)
-            }
-
-            item.externalMetadata = metadataItems
-
-            // Update Now Playing basic info immediately
-            var nowPlaying: [String: Any] = [
-                MPMediaItemPropertyTitle: title
-            ]
-            if !subtitle.isEmpty { nowPlaying[MPMediaItemPropertyArtist] = subtitle }
-            MPNowPlayingInfoCenter.default().nowPlayingInfo = nowPlaying
-
-            // Fetch and apply artwork asynchronously
-            fetchArtworkData { data in
-                guard let data = data else { return }
-                // Update AVPlayerItem artwork
-                let art = AVMutableMetadataItem()
-                art.identifier = .commonIdentifierArtwork
-                art.keySpace = .common
-                art.value = data as NSData
-
-                var meta = item.externalMetadata.filter { $0.identifier != .commonIdentifierArtwork }
-                meta.append(art)
-                item.externalMetadata = meta
-
-                // Update Now Playing artwork
-                if let image = UIImage(data: data) {
-                    var np = MPNowPlayingInfoCenter.default().nowPlayingInfo ?? [:]
-                    np[MPMediaItemPropertyArtwork] = MPMediaItemArtwork(boundsSize: image.size) { _ in image }
-                    MPNowPlayingInfoCenter.default().nowPlayingInfo = np
-                }
-            }
-        }
-
-        private func computeDisplayTitle() -> String {
-            if let t = titleOverride, !t.isEmpty { return t }
-            if let ch = appStateChannel(), let t = ch.currentProgram?.name, !t.isEmpty { return t }
-            if let t = appState?.currentProgramTitle, !t.isEmpty { return t }
-            return appStateChannel()?.name ?? ""
-        }
-
-        private func computeDisplaySubtitle() -> String {
-            if let s = subtitleOverride, !s.isEmpty { return s }
-            if let ch = appStateChannel(), let s = ch.currentProgram?.episodeTitle, !s.isEmpty { return s }
-            if let s = appState?.currentProgramSubtitle, !s.isEmpty { return s }
-            if let n = appStateChannel()?.number { return "Channel \(n)" }
-            return ""
-        }
-
-        private func appStateChannel() -> LiveTvChannelDto? {
-            // We only store channelId; rely on parent struct's channel for names
-            return parent.channel
-        }
-
-        private func fetchArtworkData(completion: @escaping (Data?) -> Void) {
-            guard let chId = channelId,
-                  let server = appState?.serverURL, !server.isEmpty,
-                  let token = appState?.accessToken, !token.isEmpty else {
-                completion(nil); return
-            }
-            let trimmed = server.trimmingCharacters(in: CharacterSet(charactersIn: "/"))
-            let path = "/Items/\(chId)/Images/Primary"
-            var comps = URLComponents(string: "\(trimmed)\(path)")
-            var q: [URLQueryItem] = [
-                URLQueryItem(name: "maxWidth", value: "512"),
-                URLQueryItem(name: "api_key", value: token)
-            ]
-            // Add cache buster
-            q.append(URLQueryItem(name: "t", value: String(Int(Date().timeIntervalSince1970))))
-            comps?.queryItems = q
-            guard let url = comps?.url else { completion(nil); return }
-
-            let req = URLRequest(url: url)
-            URLSession.shared.dataTask(with: req) { data, resp, err in
-                if let _ = err { completion(nil); return }
-                guard let http = resp as? HTTPURLResponse, (200...299).contains(http.statusCode) else { completion(nil); return }
-                completion(data)
-            }.resume()
-        }
-
-        // PiP lifecycle hooks (optional)
-        func playerViewControllerWillStartPictureInPicture(_ playerViewController: AVPlayerViewController) {}
-        func playerViewControllerDidStopPictureInPicture(_ playerViewController: AVPlayerViewController) {}
-    }
-}
-
-// MARK: - Bottom-left info overlay
-// Removed custom InfoOverlayView overlay; metadata is now provided to AVPlayerViewController controls and Now Playing.
diff --git a/LiveFin/Assets.xcassets/AppIcon.appiconset/Contents.json b/LiveFin/Assets.xcassets/AppIcon.appiconset/Contents.json
deleted file mode 100644
index 2305880..0000000
--- a/LiveFin/Assets.xcassets/AppIcon.appiconset/Contents.json
+++ /dev/null
@@ -1,35 +0,0 @@
-{
-  "images" : [
-    {
-      "idiom" : "universal",
-      "platform" : "ios",
-      "size" : "1024x1024"
-    },
-    {
-      "appearances" : [
-        {
-          "appearance" : "luminosity",
-          "value" : "dark"
-        }
-      ],
-      "idiom" : "universal",
-      "platform" : "ios",
-      "size" : "1024x1024"
-    },
-    {
-      "appearances" : [
-        {
-          "appearance" : "luminosity",
-          "value" : "tinted"
-        }
-      ],
-      "idiom" : "universal",
-      "platform" : "ios",
-      "size" : "1024x1024"
-    }
-  ],
-  "info" : {
-    "author" : "xcode",
-    "version" : 1
-  }
-}
diff --git a/LiveFin/Assets.xcassets/Discord Logo.imageset/Contents.json b/LiveFin/Assets.xcassets/Discord Logo.imageset/Contents.json
index dbb0dfa..873074d 100644
--- a/LiveFin/Assets.xcassets/Discord Logo.imageset/Contents.json	
+++ b/LiveFin/Assets.xcassets/Discord Logo.imageset/Contents.json	
@@ -2,16 +2,7 @@
   "images" : [
     {
       "filename" : "discord-logo.png",
-      "idiom" : "universal",
-      "scale" : "1x"
-    },
-    {
-      "idiom" : "universal",
-      "scale" : "2x"
-    },
-    {
-      "idiom" : "universal",
-      "scale" : "3x"
+      "idiom" : "universal"
     }
   ],
   "info" : {
diff --git a/LiveFin/Assets.xcassets/Logo.imageset/Contents.json b/LiveFin/Assets.xcassets/Logo.imageset/Contents.json
index c26ad04..0fe4d2c 100644
--- a/LiveFin/Assets.xcassets/Logo.imageset/Contents.json
+++ b/LiveFin/Assets.xcassets/Logo.imageset/Contents.json
@@ -2,16 +2,7 @@
   "images" : [
     {
       "filename" : "LiveFin Logo No Background.png",
-      "idiom" : "universal",
-      "scale" : "1x"
-    },
-    {
-      "idiom" : "universal",
-      "scale" : "2x"
-    },
-    {
-      "idiom" : "universal",
-      "scale" : "3x"
+      "idiom" : "universal"
     }
   ],
   "info" : {
diff --git a/LiveFin/Demo Mode/DemoChannelDetailView.swift b/LiveFin/Demo Mode/DemoChannelDetailView.swift
index 32a869f..0ebbef1 100644
--- a/LiveFin/Demo Mode/DemoChannelDetailView.swift	
+++ b/LiveFin/Demo Mode/DemoChannelDetailView.swift	
@@ -11,6 +11,8 @@ struct DemoChannelDetailView: View {
     let channel: DemoChannel
     @State private var showPlayer = false
     @State private var streamURL: URL? = nil
+    // Playback error presented when demo player reports a failure
+    @State private var playbackErrorMessage: String? = nil
     @EnvironmentObject var appState: AppState
     
     // Demo stream URLs for each channel (replace with real demo streams if available)
@@ -80,11 +82,22 @@ struct DemoChannelDetailView: View {
                     streamURL: url,
                     channel: nil,
                     nextProgramTitle: channel.name,
-                    nextProgramSubtitle: channel.description
+                    // Avoid using the full channel description as the Now Playing subtitle — leave subtitle nil for demos.
+                    nextProgramSubtitle: nil,
+                    onPlaybackError: { msg in
+                        // Dismiss the player and show the alert
+                        showPlayer = false
+                        playbackErrorMessage = msg
+                    }
                 )
                 .environmentObject(appState)
             }
         }
+        .alert("Playback Error", isPresented: Binding(get: { playbackErrorMessage != nil }, set: { if !$0 { playbackErrorMessage = nil } })) {
+            Button("OK", role: .cancel) { playbackErrorMessage = nil }
+        } message: {
+            Text(playbackErrorMessage ?? "An unknown error occurred while trying to play the demo stream.")
+        }
     }
 }
 
diff --git a/LiveFin/Info.plist b/LiveFin/Info.plist
index 4cc0ef0..dbe790b 100644
--- a/LiveFin/Info.plist
+++ b/LiveFin/Info.plist
@@ -2,21 +2,89 @@
 <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
 <plist version="1.0">
 <dict>
-	<key>ITSAppUsesNonExemptEncryption</key>
-	<false/>
-	<key>CFBundleURLTypes</key>
+	<key>BGTaskSchedulerPermittedIdentifiers</key>
 	<array>
+		<string>com.livefin.app.epgrefresh</string>
+	</array>
+	<key>CFBundleAlternateIcons</key>
+	<dict>
+		<key>Aura</key>
 		<dict>
-			<key>CFBundleTypeRole</key>
-			<string>Editor</string>
-			<key>CFBundleURLName</key>
-			<string>NSLocalNetworkUsageDescription</string>
+			<key>CFBundleIconFiles</key>
+			<array>
+				<string>LiveFin Logo Aura</string>
+			</array>
+			<key>UIPrerenderedIcon</key>
+			<false/>
 		</dict>
-		<dict/>
-		<dict/>
-	</array>
+		<key>Burple</key>
+		<dict>
+			<key>CFBundleIconFiles</key>
+			<array>
+				<string>LiveFin Logo Burple</string>
+			</array>
+			<key>UIPrerenderedIcon</key>
+			<false/>
+		</dict>
+		<key>Gree</key>
+		<dict>
+			<key>CFBundleIconFiles</key>
+			<array>
+				<string>LiveFin Logo Gree</string>
+			</array>
+			<key>UIPrerenderedIcon</key>
+			<false/>
+		</dict>
+		<key>Jelly</key>
+		<dict>
+			<key>CFBundleIconFiles</key>
+			<array>
+				<string>LiveFin Logo Jelly</string>
+			</array>
+			<key>UIPrerenderedIcon</key>
+			<false/>
+		</dict>
+		<key>Purple</key>
+		<dict>
+			<key>CFBundleIconFiles</key>
+			<array>
+				<string>LiveFin Logo Purple</string>
+			</array>
+			<key>UIPrerenderedIcon</key>
+			<false/>
+		</dict>
+		<key>Real</key>
+		<dict>
+			<key>CFBundleIconFiles</key>
+			<array>
+				<string>LiveFin Logo Real</string>
+			</array>
+			<key>UIPrerenderedIcon</key>
+			<false/>
+		</dict>
+		<key>Roho</key>
+		<dict>
+			<key>CFBundleIconFiles</key>
+			<array>
+				<string>LiveFin Logo Roho</string>
+			</array>
+			<key>UIPrerenderedIcon</key>
+			<false/>
+		</dict>
+		<key>Yolo</key>
+		<dict>
+			<key>CFBundleIconFiles</key>
+			<array>
+				<string>LiveFin Logo Yolo</string>
+			</array>
+			<key>UIPrerenderedIcon</key>
+			<false/>
+		</dict>
+	</dict>
+	<key>ITSAppUsesNonExemptEncryption</key>
+	<false/>
 	<key>NSAllowsLocalNetworking</key>
-	<string></string>
+	<true/>
 	<key>NSAppTransportSecurity</key>
 	<dict>
 		<key>NSAllowsArbitraryLoads</key>
@@ -30,6 +98,8 @@
 			</dict>
 		</dict>
 	</dict>
+	<key>NSLocalNetworkUsageDescription</key>
+	<string>This app needs access to devices on your local network to discover and connect to media servers.</string>
 	<key>UIApplicationSceneManifest</key>
 	<dict>
 		<key>UIApplicationSupportsMultipleScenes</key>
@@ -39,9 +109,10 @@
 		<key>UISceneConfigurations</key>
 		<dict/>
 	</dict>
-	<key>UTImportedTypeDeclarations</key>
+	<key>UIBackgroundModes</key>
 	<array>
-		<dict/>
+		<string>processing</string>
+		<string>audio</string>
 	</array>
 	<key>UISupportedInterfaceOrientations</key>
 	<array>
@@ -49,5 +120,9 @@
 		<string>UIInterfaceOrientationLandscapeLeft</string>
 		<string>UIInterfaceOrientationLandscapeRight</string>
 	</array>
+	<key>UTImportedTypeDeclarations</key>
+	<array>
+		<dict/>
+	</array>
 </dict>
 </plist>
diff --git a/LiveFin/Jellyfin Specific/ChannelImageView.swift b/LiveFin/Jellyfin Specific/ChannelImageView.swift
index 45df9f2..f4a173e 100644
--- a/LiveFin/Jellyfin Specific/ChannelImageView.swift	
+++ b/LiveFin/Jellyfin Specific/ChannelImageView.swift	
@@ -7,13 +7,17 @@
 
 import SwiftUI
 import JellyfinAPI
+import UIKit
 
 struct ChannelImageView: View {
     let baseUrl: String
     let apiKey: String
     let channelId: String
+    // Optional binding the caller can use to know whether an image is available
+    var hasImage: Binding<Bool>? = nil
 
     @State private var imageUrl: URL?
+    @State private var uiImage: UIImage?
     @Environment(\.colorScheme) private var colorScheme
 
     func buildImageUrl() -> URL? {
@@ -24,37 +28,59 @@ struct ChannelImageView: View {
         return URL(string: urlString)
     }
 
+    private func loadImageIfNeeded(from url: URL) {
+        // First, check our shared cache (memory+disk)
+        if let cached = ImageCacheManager.shared.imageIfCached(for: url) {
+            self.uiImage = cached
+            return
+        }
+        // Not cached: fetch and cache via manager
+        ImageCacheManager.shared.load(url) { image in
+            guard let image else { return }
+            self.uiImage = image
+        }
+    }
+
     var body: some View {
-        AsyncImage(url: imageUrl) { phase in
-            switch phase {
-            case .empty:
-                ProgressView()
-            case .success(let image):
-                Group {
-                    if colorScheme == .light {
-                        image
-                            .resizable()
-                            .aspectRatio(contentMode: .fit)
-                            .shadow(color: Color.black.opacity(1.0), radius: 12, x: 0, y: 0)
-                    } else {
-                        image
-                            .resizable()
-                            .aspectRatio(contentMode: .fit)
-                    }
-                }
-            case .failure:
-                Image(systemName: "photo")
+        Group {
+            if let img = uiImage {
+                Image(uiImage: img)
                     .resizable()
                     .aspectRatio(contentMode: .fit)
-                    .foregroundColor(.gray)
-            @unknown default:
-                EmptyView()
+                    .modifier(ShadowIfLight(colorScheme: colorScheme))
+            } else if let url = imageUrl {
+                // Show lightweight placeholder while loading from cache/network
+                ZStack {
+                    Color(UIColor.secondarySystemBackground)
+                    ProgressView()
+                }
+                .onAppear { loadImageIfNeeded(from: url) }
+            } else {
+                ProgressView()
             }
         }
         .onAppear {
-            // Build a stable URL without cache-busting so URLCache can reuse the response
-            if imageUrl == nil {
-                imageUrl = buildImageUrl()
+            if imageUrl == nil { imageUrl = buildImageUrl() }
+            // Try to populate instantly from cache to avoid flicker
+            if let url = imageUrl, uiImage == nil, let cached = ImageCacheManager.shared.imageIfCached(for: url) {
+                self.uiImage = cached
+            }
+        }
+        .onChange(of: uiImage) { new in
+            // notify caller when an image becomes available
+            if new != nil { hasImage?.wrappedValue = true }
+        }
+    }
+}
+
+private struct ShadowIfLight: ViewModifier {
+    let colorScheme: ColorScheme
+    func body(content: Content) -> some View {
+        Group {
+            if colorScheme == .light {
+                content.shadow(color: Color.black.opacity(1.0), radius: 12, x: 0, y: 0)
+            } else {
+                content
             }
         }
     }
diff --git a/LiveFin/Jellyfin Specific/GetLiveStream.swift b/LiveFin/Jellyfin Specific/GetLiveStream.swift
index d2d4787..7dcb1e8 100644
--- a/LiveFin/Jellyfin Specific/GetLiveStream.swift	
+++ b/LiveFin/Jellyfin Specific/GetLiveStream.swift	
@@ -7,22 +7,21 @@
 
 import Foundation
 import JellyfinAPI
-import UIKit
 
-// Compatibility wrapper matching older signature used by legacy code/snippet.
+// Minimal wrapper to align with the call-site used below
 struct PlaybackInfoService {
     static func fetchPlaybackInfo(
         itemId: String,
         userId: String,
         serverURL: String,
         accessToken: String,
-        mediaSourceId: String?,
+        mediaSourceId: String?, // ignored here
         deviceId: String,
-        playSessionId: String,
-        deviceName: String = UIDevice.current.name,
-        clientVersion: String = (Bundle.main.infoDictionary?["CFBundleShortVersionString"] as? String) ?? "1.0"
+        playSessionId: String,  // ignored here
+        deviceName: String = "LiveFin",
+        clientVersion: String = "1.0"
     ) async throws -> PlaybackInfoResponse {
-        return try await JFPlaybackInfoService.fetchPlaybackInfo(
+        try await JFPlaybackInfoService.fetchPlaybackInfo(
             itemId: itemId,
             userId: userId,
             serverURL: serverURL,
@@ -32,64 +31,124 @@ struct PlaybackInfoService {
             clientVersion: clientVersion
         )
     }
+    static func fetchPlaybackInfo(
+        appState: AppState,
+        itemId: String
+    ) async throws -> PlaybackInfoResponse {
+        try await JFPlaybackInfoService.fetchPlaybackInfo(
+            appState: appState,
+            itemId: itemId
+        )
+    }
 }
 
-// MARK: - OpenLiveStream DTO / Response (subset of fields used)
-struct OpenLiveStreamDto: Codable {
-    var channelId: String?
-    var userId: String?
-    var deviceProfile: DeviceProfileLite?
-    var playSessionId: String?
-    var maxStreamingBitrate: Int?
-    var enableDirectPlay: Bool? = true
-    var enableDirectStream: Bool? = true
-    var enableTranscoding: Bool? = true
-    var allowVideoStreamCopy: Bool? = true
-    var allowAudioStreamCopy: Bool? = true
-    var deviceId: String?
-
-    struct DeviceProfileLite: Codable {
-        var name: String? = "LiveFin"
+struct LiveTVStreamService {
+    private static func normalizeURL(_ urlOrPath: String, base: String) -> String {
+        var corrected = urlOrPath
+        if corrected.contains("localhost") || corrected.contains("127.0.0.1") {
+            if let host = URL(string: base)?.host {
+                corrected = corrected.replacingOccurrences(of: "localhost", with: host)
+                corrected = corrected.replacingOccurrences(of: "127.0.0.1", with: host)
+            }
+        }
+        // If it's absolute (http/https), rebase to the AppState base (scheme/host/port and include base path if present)
+        if corrected.hasPrefix("http://") || corrected.hasPrefix("https://") {
+            guard let absURL = URL(string: corrected), let baseURL = URL(string: base) else { return corrected }
+            var comps = URLComponents(url: absURL, resolvingAgainstBaseURL: false)
+            let baseComps = URLComponents(url: baseURL, resolvingAgainstBaseURL: false)
+            comps?.scheme = baseComps?.scheme
+            comps?.host = baseComps?.host
+            comps?.port = baseComps?.port
+            if let bPath = baseComps?.path, !bPath.isEmpty, bPath != "/" {
+                let normalizedBase = bPath.hasSuffix("/") ? String(bPath.dropLast()) : bPath
+                let currentPath = comps?.path ?? absURL.path
+                if currentPath.hasPrefix(normalizedBase) {
+                    comps?.path = currentPath
+                } else {
+                    let curr = currentPath.hasPrefix("/") ? currentPath : "/" + currentPath
+                    comps?.path = normalizedBase + curr
+                }
+            }
+            return comps?.url?.absoluteString ?? corrected
+        }
+        // Otherwise treat as relative and join to base
+        let baseFixed = base.hasSuffix("/") ? String(base.dropLast()) : base
+        return corrected.hasPrefix("/") ? baseFixed + corrected : baseFixed + "/" + corrected
     }
 
-    enum CodingKeys: String, CodingKey {
-        case channelId = "ChannelId"
-        case userId = "UserId"
-        case deviceProfile = "DeviceProfile"
-        case playSessionId = "PlaySessionId"
-        case maxStreamingBitrate = "MaxStreamingBitrate"
-        case enableDirectPlay = "EnableDirectPlay"
-        case enableDirectStream = "EnableDirectStream"
-        case enableTranscoding = "EnableTranscoding"
-        case allowVideoStreamCopy = "AllowVideoStreamCopy"
-        case allowAudioStreamCopy = "AllowAudioStreamCopy"
-        case deviceId = "DeviceId"
+    private static func replaceLocalhostWithServerHostNoPort(in urlString: String, serverBase: String) -> String {
+        guard let abs = URL(string: urlString), let base = URL(string: serverBase) else { return urlString }
+        guard let host = abs.host?.lowercased(), host == "localhost" || host == "127.0.0.1" else { return urlString }
+        var comps = URLComponents(url: abs, resolvingAgainstBaseURL: false)
+        let baseComps = URLComponents(url: base, resolvingAgainstBaseURL: false)
+        comps?.host = baseComps?.host
+        // Prefer the original path's port when present; otherwise use the server's port
+        let pathPort = comps?.port
+        let serverPort = baseComps?.port
+        comps?.port = pathPort ?? serverPort
+        // Scheme: keep the path's scheme if pathPort exists; otherwise use server's scheme
+        if pathPort != nil {
+            if comps?.scheme == nil {
+                comps?.scheme = baseComps?.scheme
+            }
+        } else {
+            comps?.scheme = baseComps?.scheme
+        }
+        return comps?.url?.absoluteString ?? urlString
     }
-}
 
-struct OpenLiveStreamResponse: Codable {
-    let mediaSourceId: String?
-    let liveStreamId: String?
-    let path: String?
-    let mediaSources: [PlaybackMediaSource]?
+    // Prefer the host/scheme/port from MediaSource.Path so HLS segments resolve correctly
+    private static func preferredBase(from playback: PlaybackInfoResponse) -> String? {
+        guard let sources = playback.mediaSources, !sources.isEmpty else { return nil }
+        for p in sources.compactMap({ $0.path }) {
+            if let u = URL(string: p), let scheme = u.scheme, (scheme == "http" || scheme == "https"), let host = u.host {
+                var comps = URLComponents()
+                comps.scheme = scheme
+                comps.host = host
+                comps.port = u.port
+                if let s = comps.url?.absoluteString { return s.hasSuffix("/") ? String(s.dropLast()) : s }
+            }
+        }
+        return nil
+    }
 
-    struct PlaybackMediaSource: Codable { let id: String?; let path: String?; enum CodingKeys: String, CodingKey { case id = "Id"; case path = "Path" } }
-    enum CodingKeys: String, CodingKey { case mediaSourceId = "MediaSourceId"; case liveStreamId = "LiveStreamId"; case path = "Path"; case mediaSources = "MediaSources" }
-}
+    // If urlStr is absolute, swap its scheme/host/port to match baseAbs; otherwise, join relative to baseAbs
+    private static func rebaseAbsolute(_ urlStr: String, toBase baseAbs: String) -> String {
+        guard let baseURL = URL(string: baseAbs) else { return urlStr }
+        if urlStr.hasPrefix("http://") || urlStr.hasPrefix("https://") {
+            guard let absURL = URL(string: urlStr) else { return urlStr }
+            var comps = URLComponents(url: absURL, resolvingAgainstBaseURL: false)
+            let baseComps = URLComponents(url: baseURL, resolvingAgainstBaseURL: false)
+            comps?.scheme = baseComps?.scheme
+            comps?.host = baseComps?.host
+            comps?.port = baseComps?.port
+            return comps?.url?.absoluteString ?? urlStr
+        } else {
+            let baseFixed = baseAbs.hasSuffix("/") ? String(baseAbs.dropLast()) : baseAbs
+            return urlStr.hasPrefix("/") ? baseFixed + urlStr : baseFixed + "/" + urlStr
+        }
+    }
 
-struct LiveTVStreamService {
-    // Convenience overload using AppState
-    static func fetchStreamURL(appState: AppState, channelId: String) async -> String? {
-        await fetchStreamURL(
-            channelId: channelId,
-            userId: appState.userID,
-            serverURL: appState.serverURL,
-            accessToken: appState.accessToken,
-            deviceId: appState.deviceId,
-            playSessionId: "",
-            deviceName: appState.clientDevice,
-            clientVersion: appState.clientVersion
-        )
+    private static func debugPrintAvailable() -> Bool {
+        #if DEBUG
+        return true
+        #else
+        return false
+        #endif
+    }
+
+    private static func ensureApiKeyParam(in urlString: String, token: String) -> String {
+        guard !token.isEmpty else { return urlString }
+        guard var comps = URLComponents(string: urlString) else { return urlString }
+        let hasApiKey = (comps.queryItems ?? []).contains { qi in
+            let n = qi.name.lowercased()
+            return n == "apikey" || n == "api_key"
+        }
+        if hasApiKey { return urlString }
+        var q = comps.queryItems ?? []
+        q.append(URLQueryItem(name: "ApiKey", value: token))
+        comps.queryItems = q
+        return comps.url?.absoluteString ?? urlString
     }
 
     static func fetchStreamURL(
@@ -99,43 +158,30 @@ struct LiveTVStreamService {
         accessToken: String,
         deviceId: String,
         playSessionId: String,
-        deviceName: String = UIDevice.current.name,
-        clientVersion: String = (Bundle.main.infoDictionary?["CFBundleShortVersionString"] as? String) ?? "1.0"
+        deviceName: String = "LiveFin",
+        clientVersion: String = "1.0"
     ) async -> String? {
-        func buildPath(from path: String?) -> String? {
-            guard let path else { return nil }
-            var corrected = path
-            // Normalize host reference if server returned localhost
-            if corrected.contains("localhost") || corrected.contains("127.0.0.1") {
-                if let host = URL(string: serverURL)?.host {
-                    corrected = corrected.replacingOccurrences(of: "localhost", with: host)
-                        .replacingOccurrences(of: "127.0.0.1", with: host)
-                }
-            } else if !corrected.lowercased().hasPrefix("http") {
-                // Prepend base server URL (ensure no double slash)
-                let base = serverURL.hasSuffix("/") ? String(serverURL.dropLast()) : serverURL
-                corrected = corrected.hasPrefix("/") ? base + corrected : base + "/" + corrected
-            }
-            // Strip api_key query parameter if present (token header already provided elsewhere)
-            if var comps = URLComponents(string: corrected), let items = comps.queryItems {
-                let filtered = items.filter { $0.name.lowercased() != "api_key" }
-                if filtered.count != items.count { comps.queryItems = filtered.isEmpty ? nil : filtered; corrected = comps.string ?? corrected }
-            }
-            return corrected
-        }
-
-        func isTSPath(_ path: String?) -> Bool {
-            guard let p = path?.lowercased() else { return false }
-            return p.hasSuffix(".ts") || p.contains("/stream.ts")
-        }
-        func isLiveTvLiveStreamFilesTs(_ path: String?) -> Bool {
-            guard let p = path?.lowercased() else { return false }
-            return p.contains("/livetv/livestreamfiles/") && p.hasSuffix("stream.ts")
+        // Prefer the resolver that can return TranscodingUrl for TS sources
+        if let url = await JFOpenLiveStreamService.resolveStreamURL(
+            channelId: channelId,
+            userId: userId,
+            serverURL: serverURL,
+            accessToken: accessToken,
+            deviceId: deviceId,
+            deviceName: deviceName,
+            clientVersion: clientVersion,
+            debug: true
+        ) {
+            // Resolver returns TS URLs with api_key and non-TS direct paths unmodified; do not modify further
+            print("[LiveTVStreamService] Resolved via resolver: \(url)")
+            return url
+        } else {
+            print("[LiveTVStreamService] Resolver returned nil, falling back to PlaybackInfo")
         }
 
-        // First attempt: playback info route
+        // Fallback: query PlaybackInfo and prefer TranscodingUrl when present; never return TS direct path
         do {
-            let (playbackInfo, rawTranscodingUrl, initialMediaSourceId, _) = try await JFPlaybackInfoService.fetchPlaybackInfoWithTranscodingUrl(
+            let (playback, transcodingUrl, mediaSourceId, _) = try await JFPlaybackInfoService.fetchPlaybackInfoWithTranscodingUrl(
                 itemId: channelId,
                 userId: userId,
                 serverURL: serverURL,
@@ -143,93 +189,203 @@ struct LiveTVStreamService {
                 deviceId: deviceId,
                 deviceName: deviceName,
                 clientVersion: clientVersion,
-                enableDirectPlay: true,
-                enableDirectStream: true,
-                enableTranscoding: true
+                debug: true
             )
-            if let t = buildPath(from: rawTranscodingUrl) { return t }
-            if let mediaSource = playbackInfo.mediaSources?.first { // fallback to direct path
-                let direct = buildPath(from: mediaSource.path)
-                if let direct, !isTSPath(direct) { return direct }
-                if let direct, isLiveTvLiveStreamFilesTs(direct) { // force transcode once
-                    if let (_, forcedUrl, _, _) = try? await JFPlaybackInfoService.fetchPlaybackInfoWithTranscodingUrl(
-                        itemId: channelId,
-                        userId: userId,
-                        serverURL: serverURL,
-                        accessToken: accessToken,
-                        deviceId: deviceId,
-                        deviceName: deviceName,
-                        clientVersion: clientVersion,
-                        enableDirectPlay: false,
-                        enableDirectStream: false,
-                        enableTranscoding: true,
-                        requireAvc: true,
-                        audioCodec: "aac",
-                        videoCodec: "h264",
-                        transcodingProtocol: "hls",
-                        transcodingContainer: "ts",
-                        allowVideoStreamCopy: false,
-                        allowAudioStreamCopy: false,
-                        mediaSourceId: initialMediaSourceId
-                    ), let built = buildPath(from: forcedUrl) { return built }
-                    return direct
+            let container = playback.mediaSources?.first?.container?.lowercased()
+            let path = playback.mediaSources?.first?.path
+            print("[LiveTVStreamService] PlaybackInfo container=\(container ?? "<nil>") path=\(path ?? "<nil>") transcodingUrl=\(transcodingUrl ?? "<nil>") mediaSourceId=\(mediaSourceId ?? "<nil>")")
+
+            let containerIsTs = (container == "ts")
+            let pathLower = path?.lowercased()
+            let pathLooksTs = pathLower?.hasSuffix("/stream.ts") == true || pathLower?.hasSuffix(".ts") == true
+
+            if containerIsTs || pathLooksTs {
+                // TS: use transcodingUrl path; never return TS direct; append api_key
+                if let t = transcodingUrl {
+                    let finalUrl = ensureApiKeyParam(in: normalizeURL(t, base: serverURL), token: accessToken)
+                    print("[LiveTVStreamService] Using transcodingUrl (TS) -> \(finalUrl)")
+                    return finalUrl
                 }
-                if let direct { // generic TS attempt force
-                    if let (_, forcedUrl, _, _) = try? await JFPlaybackInfoService.fetchPlaybackInfoWithTranscodingUrl(
-                        itemId: channelId,
-                        userId: userId,
-                        serverURL: serverURL,
-                        accessToken: accessToken,
-                        deviceId: deviceId,
-                        deviceName: deviceName,
-                        clientVersion: clientVersion,
-                        enableDirectPlay: false,
-                        enableDirectStream: false,
-                        enableTranscoding: true,
-                        requireAvc: true,
-                        audioCodec: "aac",
-                        videoCodec: "h264",
-                        transcodingProtocol: "hls",
-                        transcodingContainer: "ts",
-                        allowVideoStreamCopy: false,
-                        allowAudioStreamCopy: false
-                    ), let built = buildPath(from: forcedUrl) { return built }
-                    return direct
+                print("[LiveTVStreamService] TS detected; forcing transcoding with mediaSourceId=\(mediaSourceId ?? "<nil>")")
+                // Try forced PlaybackInfo calls to obtain server-provided TranscodingUrl
+                let (_, forcedTranscodeUrl, _, _) = try await JFPlaybackInfoService.fetchPlaybackInfoWithTranscodingUrl(
+                    itemId: channelId,
+                    userId: userId,
+                    serverURL: serverURL,
+                    accessToken: accessToken,
+                    deviceId: deviceId,
+                    deviceName: deviceName,
+                    clientVersion: clientVersion,
+                    maxBitrate: 8_000_000,
+                    enableDirectPlay: false,
+                    enableDirectStream: false,
+                    enableTranscoding: true,
+                    requireAvc: true,
+                    audioCodec: "aac",
+                    videoCodec: "h264",
+                    transcodingProtocol: "hls",
+                    transcodingContainer: nil,
+                    allowVideoStreamCopy: true,
+                    allowAudioStreamCopy: true,
+                    mediaSourceId: mediaSourceId,
+                    debug: true
+                )
+                if let t = forcedTranscodeUrl {
+                    let finalUrl = ensureApiKeyParam(in: normalizeURL(t, base: serverURL), token: accessToken)
+                    print("[LiveTVStreamService] Using forced transcodingUrl -> \(finalUrl)")
+                    return finalUrl
                 }
+                // Retry with forced re-encode
+                print("[LiveTVStreamService] No transcodingUrl from first attempt; retrying with forced re-encode")
+                let (_, forcedTranscodeUrl2, _, _) = try await JFPlaybackInfoService.fetchPlaybackInfoWithTranscodingUrl(
+                    itemId: channelId,
+                    userId: userId,
+                    serverURL: serverURL,
+                    accessToken: accessToken,
+                    deviceId: deviceId,
+                    deviceName: deviceName,
+                    clientVersion: clientVersion,
+                    maxBitrate: 8_000_000,
+                    enableDirectPlay: false,
+                    enableDirectStream: false,
+                    enableTranscoding: true,
+                    requireAvc: true,
+                    audioCodec: "aac",
+                    videoCodec: "h264",
+                    transcodingProtocol: "hls",
+                    transcodingContainer: nil,
+                    allowVideoStreamCopy: false,
+                    allowAudioStreamCopy: false,
+                    mediaSourceId: mediaSourceId,
+                    debug: true
+                )
+                if let t = forcedTranscodeUrl2 {
+                    let finalUrl = ensureApiKeyParam(in: normalizeURL(t, base: serverURL), token: accessToken)
+                    print("[LiveTVStreamService] Using forced transcodingUrl (forced re-encode) -> \(finalUrl)")
+                    return finalUrl
+                }
+                print("[LiveTVStreamService] No transcodingUrl after forcing; aborting TS direct return")
+                return nil
             }
-        } catch {
-            print("[LiveTVStreamService] Playback info error: \(error)")
-        }
 
-        // Second attempt: Explicit open live stream (some servers prefer this path)
-        if let openPath = await openLiveStream(channelId: channelId, serverURL: serverURL, accessToken: accessToken, userId: userId, deviceId: deviceId, playSessionId: playSessionId) {
-            return buildPath(from: openPath)
+            // Non-TS: prefer direct path unmodified; do NOT append api_key; only fall back to transcodingUrl if no direct path exists (also no api_key)
+            if let path = path {
+                let adjusted = replaceLocalhostWithServerHostNoPort(in: path, serverBase: serverURL)
+                print("[LiveTVStreamService] Using direct path (non-TS) -> \(adjusted)")
+                return adjusted
+            }
+            if let t = transcodingUrl {
+                print("[LiveTVStreamService] No direct path; falling back to transcodingUrl (non-TS, no api_key) -> \(t)")
+                return t
+            }
+            print("[LiveTVStreamService] No path available in PlaybackInfo")
+        } catch {
+            print("[LiveTVStreamService] Error fetching playback info: \(error)")
         }
-
         return nil
     }
-
-    // MARK: - Open Live Stream Endpoint
-    private static func openLiveStream(channelId: String, serverURL: String, accessToken: String, userId: String, deviceId: String, playSessionId: String) async -> String? {
-        guard !serverURL.isEmpty, !accessToken.isEmpty else { return nil }
-        guard let base = URL(string: serverURL)?.appendingPathComponent("/LiveTv/LiveStreamFiles/open") else { return nil }
-        var req = URLRequest(url: base)
-        req.httpMethod = "POST"
-        req.setValue("application/json", forHTTPHeaderField: "Content-Type")
-        req.setValue(accessToken, forHTTPHeaderField: "X-Emby-Token")
-        let dto = OpenLiveStreamDto(channelId: channelId, userId: userId, deviceProfile: .init(), playSessionId: playSessionId.isEmpty ? UUID().uuidString : playSessionId, maxStreamingBitrate: 12_000_000, enableDirectPlay: true, enableDirectStream: true, enableTranscoding: true, allowVideoStreamCopy: true, allowAudioStreamCopy: true, deviceId: deviceId)
+    static func fetchStreamURL(
+        appState: AppState,
+        channelId: String
+    ) async -> String? {
+        if let url = await JFOpenLiveStreamService.resolveStreamURL(appState: appState, channelId: channelId, debug: true) {
+            // Resolver returns TS URLs with api_key and non-TS direct paths unmodified; do not modify further
+            print("[LiveTVStreamService] Resolved via resolver(appState): \(url)")
+            return url
+        }
+        // Fallback: query PlaybackInfo and prefer TranscodingUrl when present; never return TS direct path
         do {
-            req.httpBody = try JSONEncoder().encode(dto)
-            let (data, response) = try await URLSession.shared.data(for: req)
-            guard let http = response as? HTTPURLResponse else { return nil }
-            if http.statusCode != 200 { print("[LiveTVStreamService] openLiveStream status: \(http.statusCode)"); return nil }
-            let decoded = try JSONDecoder().decode(OpenLiveStreamResponse.self, from: data)
-            if let direct = decoded.path { return direct }
-            if let ms = decoded.mediaSources?.first?.path { return ms }
-            return nil
+            let (playback, transcodingUrl, mediaSourceId, _) = try await JFPlaybackInfoService.fetchPlaybackInfoWithTranscodingUrl(appState: appState, itemId: channelId, debug: true)
+            let container = playback.mediaSources?.first?.container?.lowercased()
+            let path = playback.mediaSources?.first?.path
+            print("[LiveTVStreamService] (appState) container=\(container ?? "<nil>") path=\(path ?? "<nil>") transcodingUrl=\(transcodingUrl ?? "<nil>") mediaSourceId=\(mediaSourceId ?? "<nil>")")
+
+            let containerIsTs = (container == "ts")
+            let pathLower = path?.lowercased()
+            let pathLooksTs = pathLower?.hasSuffix("/stream.ts") == true || pathLower?.hasSuffix(".ts") == true
+
+            if containerIsTs || pathLooksTs {
+                if let t = transcodingUrl {
+                    let n = ensureApiKeyParam(in: normalizeURL(t, base: appState.serverURL), token: appState.accessToken)
+                    print("[LiveTVStreamService] (appState) Using transcodingUrl (TS) -> \(n)")
+                    return n
+                }
+                print("[LiveTVStreamService] (appState) TS detected; forcing transcoding with mediaSourceId=\(mediaSourceId ?? "<nil>")")
+                // First attempt: allow stream copy so server can return a TranscodingUrl without re-encoding when possible
+                let (_, forcedTranscodeUrl, _, _) = try await JFPlaybackInfoService.fetchPlaybackInfoWithTranscodingUrl(
+                    itemId: channelId,
+                    userId: appState.userID,
+                    serverURL: appState.serverURL,
+                    accessToken: appState.accessToken,
+                    deviceId: appState.deviceId,
+                    deviceName: appState.clientDevice,
+                    clientVersion: appState.clientVersion,
+                    maxBitrate: 8_000_000,
+                    enableDirectPlay: false,
+                    enableDirectStream: false,
+                    enableTranscoding: true,
+                    requireAvc: true,
+                    audioCodec: "aac",
+                    videoCodec: "h264",
+                    transcodingProtocol: "hls",
+                    transcodingContainer: nil,        // request default HLS container
+                    allowVideoStreamCopy: true,
+                    allowAudioStreamCopy: true,
+                    mediaSourceId: mediaSourceId,
+                    debug: true
+                )
+                if let t = forcedTranscodeUrl {
+                    let n = ensureApiKeyParam(in: normalizeURL(t, base: appState.serverURL), token: appState.accessToken)
+                    print("[LiveTVStreamService] (appState) Using forced transcodingUrl -> \(n)")
+                    return n
+                }
+                // Retry with forced re-encode
+                print("[LiveTVStreamService] (appState) No transcodingUrl from first attempt; retrying with forced re-encode")
+                let (_, forcedTranscodeUrl2, _, _) = try await JFPlaybackInfoService.fetchPlaybackInfoWithTranscodingUrl(
+                    itemId: channelId,
+                    userId: appState.userID,
+                    serverURL: appState.serverURL,
+                    accessToken: appState.accessToken,
+                    deviceId: appState.deviceId,
+                    deviceName: appState.clientDevice,
+                    clientVersion: appState.clientVersion,
+                    maxBitrate: 8_000_000,
+                    enableDirectPlay: false,
+                    enableDirectStream: false,
+                    enableTranscoding: true,
+                    requireAvc: true,
+                    audioCodec: "aac",
+                    videoCodec: "h264",
+                    transcodingProtocol: "hls",
+                    transcodingContainer: nil,
+                    allowVideoStreamCopy: false,
+                    allowAudioStreamCopy: false,
+                    mediaSourceId: mediaSourceId,
+                    debug: true
+                )
+                if let t = forcedTranscodeUrl2 {
+                    let n = ensureApiKeyParam(in: normalizeURL(t, base: appState.serverURL), token: appState.accessToken)
+                    print("[LiveTVStreamService] (appState) Using forced transcodingUrl (forced re-encode) -> \(n)")
+                    return n
+                }
+                print("[LiveTVStreamService] (appState) No transcodingUrl after forcing; aborting TS direct return")
+                return nil
+            }
+
+            // Non-TS: prefer direct path (no modification); fall back to transcodingUrl if needed (no api_key)
+            if let path = path {
+                let adjusted = replaceLocalhostWithServerHostNoPort(in: path, serverBase: appState.serverURL)
+                print("[LiveTVStreamService] (appState) Using direct path (non-TS) -> \(adjusted)")
+                return adjusted
+            }
+            if let t = transcodingUrl {
+                print("[LiveTVStreamService] (appState) No direct path; falling back to transcodingUrl (non-TS, no api_key) -> \(t)")
+                return t
+            }
+            print("[LiveTVStreamService] (appState) No path available in PlaybackInfo")
         } catch {
-            print("[LiveTVStreamService] openLiveStream error: \(error)")
-            return nil
+            print("[LiveTVStreamService] (appState) Error fetching playback info: \(error)")
         }
+        return nil
     }
 }
diff --git a/LiveFin/Jellyfin Specific/OpenLiveStreamService.swift b/LiveFin/Jellyfin Specific/OpenLiveStreamService.swift
index 37c1334..8201493 100644
--- a/LiveFin/Jellyfin Specific/OpenLiveStreamService.swift	
+++ b/LiveFin/Jellyfin Specific/OpenLiveStreamService.swift	
@@ -2,30 +2,170 @@
 //  OpenLiveStreamService.swift
 //  LiveFin
 //
-//  Created by GitHub Copilot on 9/14/25.
+//  Created by KPGamingz on 9/14/25.
 //
 
 import Foundation
 import UIKit
+import JellyfinAPI
 
-/// Service to explicitly open a live TV stream for a channel and return the LiveStreamId.
+/// Service to resolve a live TV stream URL for a channel by consulting PlaybackInfo (choosing TranscodingUrl if needed).
 struct JFOpenLiveStreamService {
+    // MARK: - Public API (preferred)
+    static func resolveStreamURL(
+        channelId: String,
+        userId: String,
+        serverURL: String,
+        accessToken: String,
+        deviceId: String,
+        deviceName: String? = nil,
+        clientVersion: String = (Bundle.main.infoDictionary?["CFBundleShortVersionString"] as? String) ?? "1.0",
+        debug: Bool = false
+    ) async -> String? {
+        let base = trimTrailingSlash(serverURL)
+        do {
+            let (playbackInitial, transcodingUrlInitial, mediaSourceIdInitial, _) = try await JFPlaybackInfoService.fetchPlaybackInfoWithTranscodingUrl(
+                itemId: channelId,
+                userId: userId,
+                serverURL: base,
+                accessToken: accessToken,
+                deviceId: deviceId,
+                deviceName: deviceName,
+                clientVersion: clientVersion,
+                debug: debug
+            )
+
+            // Decide based on the direct media source whether we need transcoding (only for TS)
+            let primary = playbackInitial.mediaSources?.first
+            let container = primary?.container?.lowercased()
+            let directPath = primary?.path
+            let isTS = (container == "ts") || needsTranscodingForDirectPath(directPath)
+
+            if isTS {
+                if let turl = transcodingUrlInitial, let final = buildAbsoluteURL(base: base, pathOrUrl: turl) {
+                    let authed = ensureApiKeyParam(in: final, token: accessToken)
+                    if debug { print("[ResolveStream] Using TranscodingUrl for TS source -> \(authed)") }
+                    return authed
+                }
+
+                // Second attempt: ask server again with forced transcoding flags to elicit a TranscodingUrl
+                if debug { print("[ResolveStream] TS source with no TranscodingUrl; retrying PlaybackInfo with forced HLS params") }
+                let (_, forcedTranscodeUrl, _, _) = try await JFPlaybackInfoService.fetchPlaybackInfoWithTranscodingUrl(
+                    itemId: channelId,
+                    userId: userId,
+                    serverURL: base,
+                    accessToken: accessToken,
+                    deviceId: deviceId,
+                    deviceName: deviceName,
+                    clientVersion: clientVersion,
+                    maxBitrate: 8_000_000,
+                    enableDirectPlay: false,
+                    enableDirectStream: false,
+                    enableTranscoding: true,
+                    requireAvc: true,
+                    audioCodec: "aac",
+                    videoCodec: "h264",
+                    transcodingProtocol: "hls",
+                    transcodingContainer: "ts",
+                    allowVideoStreamCopy: true,
+                    allowAudioStreamCopy: true,
+                    mediaSourceId: mediaSourceIdInitial,
+                    debug: debug
+                )
+                if let turl = forcedTranscodeUrl, let final = buildAbsoluteURL(base: base, pathOrUrl: turl) {
+                    let authed = ensureApiKeyParam(in: final, token: accessToken)
+                    if debug { print("[ResolveStream] Using server-provided TranscodingUrl (forced) -> \(authed)") }
+                    return authed
+                }
+                // Retry with forced re-encode (disable stream copy)
+                if debug { print("[ResolveStream] Forced attempt returned no TranscodingUrl; retrying with stream copy disabled") }
+                let (_, forcedTranscodeUrl2, _, _) = try await JFPlaybackInfoService.fetchPlaybackInfoWithTranscodingUrl(
+                    itemId: channelId,
+                    userId: userId,
+                    serverURL: base,
+                    accessToken: accessToken,
+                    deviceId: deviceId,
+                    deviceName: deviceName,
+                    clientVersion: clientVersion,
+                    maxBitrate: 8_000_000,
+                    enableDirectPlay: false,
+                    enableDirectStream: false,
+                    enableTranscoding: true,
+                    requireAvc: true,
+                    audioCodec: "aac",
+                    videoCodec: "h264",
+                    transcodingProtocol: "hls",
+                    transcodingContainer: "ts",
+                    allowVideoStreamCopy: false,
+                    allowAudioStreamCopy: false,
+                    mediaSourceId: mediaSourceIdInitial,
+                    debug: debug
+                )
+                if let turl2 = forcedTranscodeUrl2, let final2 = buildAbsoluteURL(base: base, pathOrUrl: turl2) {
+                    let authed2 = ensureApiKeyParam(in: final2, token: accessToken)
+                    if debug { print("[ResolveStream] Using server-provided TranscodingUrl (forced re-encode) -> \(authed2)") }
+                    return authed2
+                }
+
+                if debug { print("[ResolveStream] No TranscodingUrl available from MediaSourceInfo for TS; not returning TS direct path") }
+                return nil
+            } else {
+                // Not TS: prefer direct path, and DO NOT append api_key
+                if let p = directPath {
+                    // If localhost/127.0.0.1, replace with server host (preserve server port)
+                    let adjusted = replaceLocalhostWithServerHostAndPort(in: p, serverBase: base)
+                    if debug { print("[ResolveStream] Using direct path (non-TS) -> \(adjusted)") }
+                    return adjusted
+                }
+                // Fallback: if no direct path, allow using transcoding url if available, but DO NOT append api_key for non-TS
+                if let turl = transcodingUrlInitial, let final = buildAbsoluteURL(base: base, pathOrUrl: turl) {
+                    if debug { print("[ResolveStream] No direct path; falling back to TranscodingUrl (non-TS, no api_key) -> \(final)") }
+                    return final
+                }
+                if debug { print("[ResolveStream] No path available to play (non-TS)") }
+            }
+
+        } catch {
+            if debug { print("[ResolveStream] Error resolving stream: \(error)") }
+        }
+        return nil
+    }
+
+    static func resolveStreamURL(appState: AppState, channelId: String, debug: Bool = false) async -> String? {
+        await resolveStreamURL(
+            channelId: channelId,
+            userId: appState.userID,
+            serverURL: appState.serverURL,
+            accessToken: appState.accessToken,
+            deviceId: appState.deviceId,
+            deviceName: appState.clientDevice,
+            clientVersion: appState.clientVersion,
+            debug: debug
+        )
+    }
+
+    // MARK: - Legacy explicit open (kept as fallback)
     static func openLiveStream(
         channelId: String,
         userId: String,
         serverURL: String,
         accessToken: String,
         deviceId: String,
-        deviceName: String = UIDevice.current.name,
+        deviceName: String? = nil,
         clientVersion: String = (Bundle.main.infoDictionary?["CFBundleShortVersionString"] as? String) ?? "1.0"
     ) async throws -> String {
-        let base = serverURL.hasSuffix("/") ? String(serverURL.dropLast()) : serverURL
-        // Try LiveStreamFiles/Open first
-        if let id = try? await postOpen(base: base, path: "/LiveTv/LiveStreamFiles/Open", channelId: channelId, userId: userId, accessToken: accessToken, deviceId: deviceId, deviceName: deviceName, clientVersion: clientVersion) {
+        // No longer used by resolver; keeping for potential manual callers
+        let base = trimTrailingSlash(serverURL)
+        let resolvedDeviceName: String
+        if let dn = deviceName {
+            resolvedDeviceName = dn
+        } else {
+            resolvedDeviceName = await MainActor.run { UIDevice.current.name }
+        }
+        if let id = try? await postOpen(base: base, path: "/LiveTv/LiveStreamFiles/Open", channelId: channelId, userId: userId, accessToken: accessToken, deviceId: deviceId, deviceName: resolvedDeviceName, clientVersion: clientVersion) {
             return id
         }
-        // Fallback: LiveStreams/Open (some servers expose this variant)
-        return try await postOpen(base: base, path: "/LiveTv/LiveStreams/Open", channelId: channelId, userId: userId, accessToken: accessToken, deviceId: deviceId, deviceName: deviceName, clientVersion: clientVersion)
+        return try await postOpen(base: base, path: "/LiveTv/LiveStreams/Open", channelId: channelId, userId: userId, accessToken: accessToken, deviceId: deviceId, deviceName: resolvedDeviceName, clientVersion: clientVersion)
     }
 
     private static func postOpen(
@@ -38,42 +178,136 @@ struct JFOpenLiveStreamService {
         deviceName: String,
         clientVersion: String
     ) async throws -> String {
-        let url = URL(string: base + path)!
-        var request = URLRequest(url: url)
-        request.httpMethod = "POST"
-        request.setValue("application/json", forHTTPHeaderField: "Content-Type")
-        request.setValue(accessToken, forHTTPHeaderField: "X-Emby-Token")
-        let authHeader = "MediaBrowser Client=\"LiveFin\", Device=\"\(deviceName)\", DeviceId=\"\(deviceId)\", Version=\"\(clientVersion)\", Token=\"\(accessToken)\""
-        request.setValue(authHeader, forHTTPHeaderField: "X-Emby-Authorization")
-        // Include common fields Jellyfin may inspect
-        let body: [String: Any] = [
-            "ChannelId": channelId,
-            "UserId": userId,
-            "AllowAudioStreamCopy": true,
-            "AllowVideoStreamCopy": true,
-            "MaxStreamingBitrate": 8_000_000,
-            "RequireAvc": true,
-            "AudioCodec": "aac",
-            "VideoCodec": "h264"
-        ]
-        request.httpBody = try JSONSerialization.data(withJSONObject: body)
+        return try await withCheckedThrowingContinuation { continuation in
+            Task {
+                let url = URL(string: base + path)!
+                var request = URLRequest(url: url)
+                request.httpMethod = "POST"
+                request.setValue("application/json", forHTTPHeaderField: "Content-Type")
+                request.setValue("application/json", forHTTPHeaderField: "Accept")
+                request.setValue(accessToken, forHTTPHeaderField: "X-Emby-Token")
+                let authHeader = "MediaBrowser Client=\"LiveFin\", Device=\"\(deviceName)\", DeviceId=\"\(deviceId)\", Version=\"\(clientVersion)\""
+                request.setValue(authHeader, forHTTPHeaderField: "X-Emby-Authorization")
+                request.setValue(userId, forHTTPHeaderField: "X-Emby-User-Id")
+                request.setValue("LiveFin", forHTTPHeaderField: "X-Emby-Client")
+                request.setValue(deviceName, forHTTPHeaderField: "X-Emby-Device-Name")
+                request.setValue(deviceId, forHTTPHeaderField: "X-Emby-Device-Id")
+                request.setValue(clientVersion, forHTTPHeaderField: "X-Emby-Client-Version")
+                let userAgent = "LiveFin/\(clientVersion) (iOS; \(deviceName))"
+                request.setValue(userAgent, forHTTPHeaderField: "User-Agent")
+                let body: [String: Any] = [
+                    "ChannelId": channelId,
+                    "UserId": userId,
+                    "AllowAudioStreamCopy": true,
+                    "AllowVideoStreamCopy": true,
+                    "MaxStreamingBitrate": 8_000_000,
+                    "RequireAvc": true,
+                    "AudioCodec": "aac",
+                    "VideoCodec": "h264"
+                ]
+                request.httpBody = try? JSONSerialization.data(withJSONObject: body)
+                URLSession.shared.dataTask(with: request) { data, response, _ in
+                    guard let http = response as? HTTPURLResponse, let data = data, (200...299).contains(http.statusCode) else {
+                        continuation.resume(throwing: URLError(.badServerResponse)); return
+                    }
+                    if let obj = try? JSONSerialization.jsonObject(with: data) as? [String: Any] {
+                        if let id = obj["Id"] as? String, !id.isEmpty { continuation.resume(returning: id); return }
+                        if let id = obj["id"] as? String, !id.isEmpty { continuation.resume(returning: id); return }
+                    }
+                    struct Resp: Decodable { let Id: String? }
+                    if let resp = try? JSONDecoder().decode(Resp.self, from: data), let id = resp.Id, !id.isEmpty {
+                        continuation.resume(returning: id); return
+                    }
+                    continuation.resume(throwing: URLError(.cannotParseResponse))
+                }.resume()
+            }
+        }
+    }
+
+    // MARK: - Helpers
+    private static func trimTrailingSlash(_ s: String) -> String {
+        s.hasSuffix("/") ? String(s.dropLast()) : s
+    }
+
+    private static func replaceLocalhostWithServerHostAndPort(in urlString: String, serverBase: String) -> String {
+        guard let abs = URL(string: urlString), let base = URL(string: serverBase) else { return urlString }
+        guard let host = abs.host?.lowercased(), host == "localhost" || host == "127.0.0.1" else { return urlString }
+        var comps = URLComponents(url: abs, resolvingAgainstBaseURL: false)
+        let baseComps = URLComponents(url: base, resolvingAgainstBaseURL: false)
+        // Always use the server host when replacing localhost
+        comps?.host = baseComps?.host
+        // Choose port: prefer the original path's port when present; otherwise use server's port
+        let pathPort = comps?.port
+        let serverPort = baseComps?.port
+        comps?.port = pathPort ?? serverPort
+        // Choose scheme: if we're keeping the path's port, prefer the path's scheme; else use server's scheme
+        if pathPort != nil {
+            // keep original scheme (http/https) tied to the path port
+            // if missing, fall back to server scheme
+            if comps?.scheme == nil {
+                comps?.scheme = baseComps?.scheme
+            }
+        } else {
+            comps?.scheme = baseComps?.scheme
+        }
+        return comps?.url?.absoluteString ?? urlString
+    }
 
-        let (data, response) = try await URLSession.shared.data(for: request)
-        guard let http = response as? HTTPURLResponse else { throw URLError(.badServerResponse) }
-        guard (200...299).contains(http.statusCode) else {
-            let bodyText = String(data: data, encoding: .utf8) ?? "<unreadable body>"
-            print("[OpenLiveStream] HTTP \(http.statusCode) on \(path): \(bodyText)")
-            throw URLError(.badServerResponse)
+    private static func buildAbsoluteURL(base: String, pathOrUrl: String, altBase: String? = nil) -> String? {
+        var value = pathOrUrl
+        if value.contains("localhost") || value.contains("127.0.0.1") {
+            if let host = URL(string: base)?.host {
+                value = value.replacingOccurrences(of: "localhost", with: host)
+                value = value.replacingOccurrences(of: "127.0.0.1", with: host)
+            }
         }
-        // Parse LiveStreamId; Jellyfin returns { "Id": "...", ... }
-        if let obj = try? JSONSerialization.jsonObject(with: data) as? [String: Any] {
-            if let id = obj["Id"] as? String, !id.isEmpty { return id }
-            if let id = obj["id"] as? String, !id.isEmpty { return id }
+        // If already absolute, rebase to AppState base (scheme/host/port and optional base path)
+        if value.hasPrefix("http://") || value.hasPrefix("https://") {
+            guard let absURL = URL(string: value), let baseURL = URL(string: base) else { return value }
+            var comps = URLComponents(url: absURL, resolvingAgainstBaseURL: false)
+            let baseComps = URLComponents(url: baseURL, resolvingAgainstBaseURL: false)
+            comps?.scheme = baseComps?.scheme
+            comps?.host = baseComps?.host
+            comps?.port = baseComps?.port
+            if let bPath = baseComps?.path, !bPath.isEmpty, bPath != "/" {
+                let normalizedBase = bPath.hasSuffix("/") ? String(bPath.dropLast()) : bPath
+                let currentPath = comps?.path ?? absURL.path
+                if currentPath.hasPrefix(normalizedBase) {
+                    comps?.path = currentPath
+                } else {
+                    let curr = currentPath.hasPrefix("/") ? currentPath : "/" + currentPath
+                    comps?.path = normalizedBase + curr
+                }
+            }
+            return comps?.url?.absoluteString ?? value
         }
-        struct Resp: Decodable { let Id: String? }
-        if let resp = try? JSONDecoder().decode(Resp.self, from: data), let id = resp.Id, !id.isEmpty {
-            return id
+        // Ensure leading slash consistency for relative URLs (always use AppState base)
+        let chosenBase = base
+        if value.hasPrefix("/") {
+            return chosenBase + value
+        } else {
+            return chosenBase + "/" + value
         }
-        throw URLError(.cannotParseResponse)
+    }
+
+    private static func needsTranscodingForDirectPath(_ path: String?) -> Bool {
+        guard let p = path?.lowercased() else { return false }
+        // Any TS transport stream path should be transcoded to HLS
+        if p.hasSuffix(".ts") || p.hasSuffix("/stream.ts") { return true }
+        if p.contains("/livetv/livestreamfiles/") && (p.hasSuffix("/stream.ts") || p.hasSuffix(".ts")) {
+            return true
+        }
+        return false
+    }
+
+    private static func ensureApiKeyParam(in urlString: String, token: String) -> String {
+        guard !token.isEmpty else { return urlString }
+        guard var comps = URLComponents(string: urlString) else { return urlString }
+        let hasApiKey = (comps.queryItems ?? []).contains { $0.name.lowercased() == "apikey" || $0.name.lowercased() == "api_key" }
+        if hasApiKey { return urlString }
+        var q = comps.queryItems ?? []
+        q.append(URLQueryItem(name: "api_key", value: token))
+        comps.queryItems = q
+        return comps.url?.absoluteString ?? urlString
     }
 }
diff --git a/LiveFin/Jellyfin Specific/PlaybackInfoService.swift b/LiveFin/Jellyfin Specific/PlaybackInfoService.swift
index 3ce046a..f730e66 100644
--- a/LiveFin/Jellyfin Specific/PlaybackInfoService.swift	
+++ b/LiveFin/Jellyfin Specific/PlaybackInfoService.swift	
@@ -38,6 +38,41 @@ struct JFPlaybackInfoService {
         )
     }
 
+    private static func buildDeviceProfile(maxBitrate: Int?) -> [String: Any] {
+        var profile: [String: Any] = [:]
+        profile["Name"] = "LiveFin iOS"
+        if let max = maxBitrate { profile["MaxStreamingBitrate"] = max }
+        // No direct play profiles to encourage server to consider transcoding when requested
+        profile["DirectPlayProfiles"] = []
+        // Prefer HLS TS for video
+        profile["TranscodingProfiles"] = [[
+            "Container": "ts",
+            "Type": "Video",
+            "AudioCodec": "aac",
+            "VideoCodec": "h264",
+            "Protocol": "hls",
+            "Context": "Streaming",
+            "MaxAudioChannels": "2",
+            "MinSegments": 1,
+            "SegmentLength": 6
+        ]]
+        // Hint common codec constraints for iOS h264
+        profile["CodecProfiles"] = [[
+            "Type": "Video",
+            "Codec": "h264",
+            "Conditions": [[
+                "Condition": "LessThanEqual",
+                "Property": "VideoLevel",
+                "Value": "42"
+            ], [
+                "Condition": "LessThanEqual",
+                "Property": "RefFrames",
+                "Value": "4"
+            ]]
+        ]]
+        return profile
+    }
+
     static func fetchPlaybackInfo(
         itemId: String,
         userId: String,
@@ -58,24 +93,37 @@ struct JFPlaybackInfoService {
         allowVideoStreamCopy: Bool? = nil,
         allowAudioStreamCopy: Bool? = nil,
         mediaSourceId: String? = nil,
+        transcodingUrl: String? = nil,
         debug: Bool = false
     ) async throws -> PlaybackInfoResponse {
         let resolvedDeviceName: String
         if let deviceName { resolvedDeviceName = deviceName } else { resolvedDeviceName = await MainActor.run { UIDevice.current.name } }
-        let url = URL(string: "\(serverURL)/Items/\(itemId)/PlaybackInfo")!
+        let base = serverURL.hasSuffix("/") ? String(serverURL.dropLast()) : serverURL
+        let url = URL(string: "\(base)/Items/\(itemId)/PlaybackInfo")!
         var request = URLRequest(url: url)
         request.httpMethod = "POST"
         request.setValue("application/json", forHTTPHeaderField: "Content-Type")
+        request.setValue("application/json", forHTTPHeaderField: "Accept")
+        // Provide both X-Emby-Token and client identification headers
         request.setValue(accessToken, forHTTPHeaderField: "X-Emby-Token")
         let authHeader = "MediaBrowser Client=\"LiveFin\", Device=\"\(resolvedDeviceName)\", DeviceId=\"\(deviceId)\", Version=\"\(clientVersion)\""
         request.setValue(authHeader, forHTTPHeaderField: "X-Emby-Authorization")
+        request.setValue(userId, forHTTPHeaderField: "X-Emby-User-Id")
+        request.setValue("LiveFin", forHTTPHeaderField: "X-Emby-Client")
+        request.setValue(resolvedDeviceName, forHTTPHeaderField: "X-Emby-Device-Name")
+        request.setValue(deviceId, forHTTPHeaderField: "X-Emby-Device-Id")
+        request.setValue(clientVersion, forHTTPHeaderField: "X-Emby-Client-Version")
+        // Helpful for some proxies/servers when negotiating playback
+        let userAgent = "LiveFin/\(clientVersion) (iOS; \(resolvedDeviceName))"
+        request.setValue(userAgent, forHTTPHeaderField: "User-Agent")
         var body: [String: Any] = [
             "UserId": userId,
             "MaxStreamingBitrate": maxBitrate ?? 8_000_000,
             "AutoOpenLiveStream": true,
             "EnableDirectPlay": enableDirectPlay,
             "EnableDirectStream": enableDirectStream,
-            "EnableTranscoding": enableTranscoding
+            "EnableTranscoding": enableTranscoding,
+            "EnableAdaptiveBitrateStreaming": true
         ]
         if let requireAvc = requireAvc { body["RequireAvc"] = requireAvc }
         if let a = audioCodec { body["AudioCodec"] = a }
@@ -85,6 +133,9 @@ struct JFPlaybackInfoService {
         if let avs = allowVideoStreamCopy { body["AllowVideoStreamCopy"] = avs }
         if let aas = allowAudioStreamCopy { body["AllowAudioStreamCopy"] = aas }
         if let ms = mediaSourceId { body["MediaSourceId"] = ms }
+        if enableTranscoding {
+            body["DeviceProfile"] = buildDeviceProfile(maxBitrate: maxBitrate ?? 8_000_000)
+        }
         request.httpBody = try JSONSerialization.data(withJSONObject: body)
         let (data, response) = try await URLSession.shared.data(for: request)
         guard let httpResponse = response as? HTTPURLResponse else { throw URLError(.badServerResponse) }
@@ -113,24 +164,35 @@ struct JFPlaybackInfoService {
         allowVideoStreamCopy: Bool? = nil,
         allowAudioStreamCopy: Bool? = nil,
         mediaSourceId: String? = nil,
+        transcodingUrl: String? = nil,
         debug: Bool = false
     ) async throws -> (PlaybackInfoResponse, String?, String?, String?) {
         let resolvedDeviceName: String
         if let deviceName { resolvedDeviceName = deviceName } else { resolvedDeviceName = await MainActor.run { UIDevice.current.name } }
-        let url = URL(string: "\(serverURL)/Items/\(itemId)/PlaybackInfo")!
+        let base = serverURL.hasSuffix("/") ? String(serverURL.dropLast()) : serverURL
+        let url = URL(string: "\(base)/Items/\(itemId)/PlaybackInfo")!
         var request = URLRequest(url: url)
         request.httpMethod = "POST"
         request.setValue("application/json", forHTTPHeaderField: "Content-Type")
+        request.setValue("application/json", forHTTPHeaderField: "Accept")
         request.setValue(accessToken, forHTTPHeaderField: "X-Emby-Token")
         let authHeader = "MediaBrowser Client=\"LiveFin\", Device=\"\(resolvedDeviceName)\", DeviceId=\"\(deviceId)\", Version=\"\(clientVersion)\""
         request.setValue(authHeader, forHTTPHeaderField: "X-Emby-Authorization")
+        request.setValue(userId, forHTTPHeaderField: "X-Emby-User-Id")
+        request.setValue("LiveFin", forHTTPHeaderField: "X-Emby-Client")
+        request.setValue(resolvedDeviceName, forHTTPHeaderField: "X-Emby-Device-Name")
+        request.setValue(deviceId, forHTTPHeaderField: "X-Emby-Device-Id")
+        request.setValue(clientVersion, forHTTPHeaderField: "X-Emby-Client-Version")
+        let userAgent = "LiveFin/\(clientVersion) (iOS; \(resolvedDeviceName))"
+        request.setValue(userAgent, forHTTPHeaderField: "User-Agent")
         var body: [String: Any] = [
             "UserId": userId,
             "MaxStreamingBitrate": maxBitrate ?? 8_000_000,
             "AutoOpenLiveStream": true,
             "EnableDirectPlay": enableDirectPlay,
             "EnableDirectStream": enableDirectStream,
-            "EnableTranscoding": enableTranscoding
+            "EnableTranscoding": enableTranscoding,
+            "EnableAdaptiveBitrateStreaming": true
         ]
         if let requireAvc = requireAvc { body["RequireAvc"] = requireAvc }
         if let a = audioCodec { body["AudioCodec"] = a }
@@ -140,6 +202,9 @@ struct JFPlaybackInfoService {
         if let avs = allowVideoStreamCopy { body["AllowVideoStreamCopy"] = avs }
         if let aas = allowAudioStreamCopy { body["AllowAudioStreamCopy"] = aas }
         if let ms = mediaSourceId { body["MediaSourceId"] = ms }
+        if enableTranscoding {
+            body["DeviceProfile"] = buildDeviceProfile(maxBitrate: maxBitrate ?? 8_000_000)
+        }
         request.httpBody = try JSONSerialization.data(withJSONObject: body)
         let (data, response) = try await URLSession.shared.data(for: request)
         guard let httpResponse = response as? HTTPURLResponse, httpResponse.statusCode == 200 else {
@@ -151,10 +216,74 @@ struct JFPlaybackInfoService {
         var transcodingUrl: String? = nil
         var mediaSourceIdOut: String? = nil
         var playSessionId: String? = nil
+        // Extract from raw JSON to ensure we capture TranscodingUrl present in MediaSource or at root
         if let jsonRoot = try? JSONSerialization.jsonObject(with: data) as? [String: Any] {
-            if let msArr = jsonRoot["MediaSources"] as? [[String: Any]], let first = msArr.first {
-                transcodingUrl = first["TranscodingUrl"] as? String
-                mediaSourceIdOut = (first["Id"] as? String) ?? (first["id"] as? String)
+            // Capture root-level TranscodingUrl if present
+            let rootTranscodeRaw: String? = (
+                (jsonRoot["TranscodingUrl"] as? String) ??
+                (jsonRoot["TranscodingURL"] as? String) ??
+                (jsonRoot["transcodingUrl"] as? String) ??
+                (jsonRoot["transcodingURL"] as? String)
+            )?.trimmingCharacters(in: .whitespacesAndNewlines)
+            let rootIsHls = rootTranscodeRaw?.lowercased().contains(".m3u8") == true
+            if let msArr = (jsonRoot["MediaSources"] as? [[String: Any]]) ?? (jsonRoot["mediaSources"] as? [[String: Any]]) {
+                if debug {
+                    print("[PlaybackInfo] MediaSources summary (count=\(msArr.count)):")
+                    for (idx, ms) in msArr.enumerated() {
+                        let mid = (ms["Id"] as? String) ?? (ms["id"] as? String) ?? "<nil>"
+                        let cont = ((ms["Container"] as? String) ?? (ms["container"] as? String) ?? "").lowercased()
+                        let supT = (ms["SupportsTranscoding"] as? Bool) ?? (ms["supportsTranscoding"] as? Bool) ?? false
+                        let turl = (
+                            (ms["TranscodingUrl"] as? String) ??
+                            (ms["TranscodingURL"] as? String) ??
+                            (ms["transcodingUrl"] as? String) ??
+                            (ms["transcodingURL"] as? String)
+                        )
+                        let isHls = turl?.lowercased().contains(".m3u8") == true
+                        print("  [\(idx)] id=\(mid) container=\(cont) supportsTranscoding=\(supT) hasTranscodingUrl=\(turl != nil) hls=\(isHls)")
+                    }
+                }
+                // Prefer an HLS TranscodingUrl; else prefer TS container; else any media-source with transcode
+                var hlsWithTranscode: (url: String, id: String?)? = nil
+                var tsWithTranscode: (url: String, id: String?)? = nil
+                var firstWithTranscode: (url: String, id: String?)? = nil
+                for ms in msArr {
+                    let turlRaw = (
+                        (ms["TranscodingUrl"] as? String) ??
+                        (ms["TranscodingURL"] as? String) ??
+                        (ms["transcodingUrl"] as? String) ??
+                        (ms["transcodingURL"] as? String)
+                    )?.trimmingCharacters(in: .whitespacesAndNewlines)
+                    let mid = (ms["Id"] as? String) ?? (ms["id"] as? String)
+                    let container = (ms["Container"] as? String) ?? (ms["container"] as? String)
+                    guard let turl = turlRaw, !turl.isEmpty else { continue }
+                    if firstWithTranscode == nil { firstWithTranscode = (turl, mid) }
+                    if turl.lowercased().contains(".m3u8") { hlsWithTranscode = (turl, mid) }
+                    if (container?.lowercased() == "ts") { tsWithTranscode = (turl, mid) }
+                }
+                // Selection priority: HLS (.m3u8) > TS container > root-level (prefer HLS) > any media-source with transcode
+                if let chosen = hlsWithTranscode {
+                    transcodingUrl = chosen.url
+                    mediaSourceIdOut = chosen.id
+                } else if let chosen = tsWithTranscode {
+                    transcodingUrl = chosen.url
+                    mediaSourceIdOut = chosen.id
+                } else if let root = rootTranscodeRaw, !root.isEmpty, rootIsHls {
+                    transcodingUrl = root
+                } else if let chosen = firstWithTranscode {
+                    transcodingUrl = chosen.url
+                    mediaSourceIdOut = chosen.id
+                } else if let root = rootTranscodeRaw, !root.isEmpty {
+                    transcodingUrl = root
+                } else {
+                    // No TranscodingUrl found; still capture first media source id for potential follow-up requests
+                    if let first = msArr.first {
+                        mediaSourceIdOut = (first["Id"] as? String) ?? (first["id"] as? String)
+                    }
+                }
+            } else {
+                // No media sources parsed; still allow root-level transcode url to pass through
+                if let root = rootTranscodeRaw, !root.isEmpty { transcodingUrl = root }
             }
             playSessionId = (jsonRoot["PlaySessionId"] as? String) ?? (jsonRoot["playSessionId"] as? String)
         }
